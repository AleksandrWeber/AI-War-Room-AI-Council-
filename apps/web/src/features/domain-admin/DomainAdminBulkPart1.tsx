// @ts-nocheck
import {
  formatAcceptabilityAdminAction,
  formatAcceptabilityDomain,
} from '../../acceptability-ui'
import {
  formatAccessibilityAdminAction,
  formatAccessibilityDomain,
} from '../../accessibility-ui'
import {
  formatAccountabilityAdminAction,
  formatAccountabilityDomain,
} from '../../accountability-ui'
import {
  formatAdaptabilityAdminAction,
  formatAdaptabilityDomain,
} from '../../adaptability-ui'
import {
  formatAdjustabilityAdminAction,
  formatAdjustabilityDomain,
} from '../../adjustability-ui'
import {
  formatAdoptabilityAdminAction,
  formatAdoptabilityDomain,
} from '../../adoptability-ui'
import {
  formatAffordabilityAdminAction,
  formatAffordabilityDomain,
} from '../../affordability-ui'
import {
  formatAppropriatenessAdminAction,
  formatAppropriatenessDomain,
} from '../../appropriateness-ui'
import {
  formatArticulabilityAdminAction,
  formatArticulabilityDomain,
} from '../../articulability-ui'
import {
  formatAssessabilityAdminAction,
  formatAssessabilityDomain,
} from '../../assessability-ui'
import {
  formatAssignabilityAdminAction,
  formatAssignabilityDomain,
} from '../../assignability-ui'
import {
  formatAssuranceAdminAction,
  formatAssuranceDomain,
} from '../../assurance-ui'
import {
  formatAttestationAdminAction,
  formatAttestationDomain,
} from '../../attestation-ui'
import {
  formatAttributabilityAdminAction,
  formatAttributabilityDomain,
} from '../../attributability-ui'
import {
  formatAuditAdminAction,
  formatAuditDomain,
} from '../../audit-trail-ui'
import {
  formatAuditabilityAdminAction,
  formatAuditabilityDomain,
} from '../../auditability-ui'
import {
  formatAuthenticityAdminAction,
  formatAuthenticityDomain,
} from '../../authenticity-ui'
import {
  formatAutomatabilityAdminAction,
  formatAutomatabilityDomain,
} from '../../automatability-ui'
import {
  formatAvailabilityAdminAction,
  formatAvailabilityDomain,
} from '../../availability-ui'
import {
  formatBackupAdminAction,
  formatBackupDomain,
} from '../../backup-ui'
import {
  formatCapacityAdminAction,
  formatCapacityDomain,
} from '../../capacity-ui'
import {
  formatCertifiabilityAdminAction,
  formatCertifiabilityDomain,
} from '../../certifiability-ui'
import {
  formatClarityAdminAction,
  formatClarityDomain,
} from '../../clarity-ui'
import {
  formatCoherenceAdminAction,
  formatCoherenceDomain,
} from '../../coherence-ui'
import {
  formatCommunicabilityAdminAction,
  formatCommunicabilityDomain,
} from '../../communicability-ui'
import {
  formatComparabilityAdminAction,
  formatComparabilityDomain,
} from '../../comparability-ui'
import {
  formatCompatibilityAdminAction,
  formatCompatibilityDomain,
} from '../../compatibility-ui'
import {
  formatComplianceAdminAction,
  formatComplianceDomain,
} from '../../compliance-ui'
import {
  formatComposabilityAdminAction,
  formatComposabilityDomain,
} from '../../composability-ui'
import {
  formatComprehensibilityAdminAction,
  formatComprehensibilityDomain,
} from '../../comprehensibility-ui'
import {
  formatConfigurabilityAdminAction,
  formatConfigurabilityDomain,
} from '../../configurability-ui'
import {
  formatConfirmabilityAdminAction,
  formatConfirmabilityDomain,
} from '../../confirmability-ui'
import {
  formatConformanceAdminAction,
  formatConformanceDomain,
} from '../../conformance-ui'
import {
  formatConnectabilityAdminAction,
  formatConnectabilityDomain,
} from '../../connectability-ui'
import {
  formatConnotabilityAdminAction,
  formatConnotabilityDomain,
} from '../../connotability-ui'
import {
  formatConsistencyAdminAction,
  formatConsistencyDomain,
} from '../../consistency-ui'
import {
  formatConspicuousnessAdminAction,
  formatConspicuousnessDomain,
} from '../../conspicuousness-ui'
import {
  formatControllabilityAdminAction,
  formatControllabilityDomain,
} from '../../controllability-ui'
import {
  formatCredibilityAdminAction,
  formatCredibilityDomain,
} from '../../credibility-ui'
import {
  formatCustomizabilityAdminAction,
  formatCustomizabilityDomain,
} from '../../customizability-ui'
import {
  formatDefensibilityAdminAction,
  formatDefensibilityDomain,
} from '../../defensibility-ui'
import {
  formatDeliverabilityAdminAction,
  formatDeliverabilityDomain,
} from '../../deliverability-ui'
import {
  formatDemonstrabilityAdminAction,
  formatDemonstrabilityDomain,
} from '../../demonstrability-ui'
import {
  formatDependabilityAdminAction,
  formatDependabilityDomain,
} from '../../dependability-ui'
import {
  formatDeployabilityAdminAction,
  formatDeployabilityDomain,
} from '../../deployability-ui'
import {
  formatDescribabilityAdminAction,
  formatDescribabilityDomain,
} from '../../describability-ui'
import {
  formatDesirabilityAdminAction,
  formatDesirabilityDomain,
} from '../../desirability-ui'
import {
  formatDetectabilityAdminAction,
  formatDetectabilityDomain,
} from '../../detectability-ui'
import {
  formatDiscernibilityAdminAction,
  formatDiscernibilityDomain,
} from '../../discernibility-ui'
import {
  formatDiscoverabilityAdminAction,
  formatDiscoverabilityDomain,
} from '../../discoverability-ui'
import {
  formatDistinctivenessAdminAction,
  formatDistinctivenessDomain,
} from '../../distinctiveness-ui'
import {
  formatDistinguishabilityAdminAction,
  formatDistinguishabilityDomain,
} from '../../distinguishability-ui'
import {
  formatDurabilityAdminAction,
  formatDurabilityDomain,
} from '../../durability-ui'
import {
  formatEffectivenessAdminAction,
  formatEffectivenessDomain,
} from '../../effectiveness-ui'
import {
  formatEfficiencyAdminAction,
  formatEfficiencyDomain,
} from '../../efficiency-ui'
import {
  formatElaboratabilityAdminAction,
  formatElaboratabilityDomain,
} from '../../elaboratability-ui'
import {
  formatEnunciabilityAdminAction,
  formatEnunciabilityDomain,
} from '../../enunciability-ui'
import {
  formatEvocatabilityAdminAction,
  formatEvocatabilityDomain,
} from '../../evocatability-ui'
import {
  formatExplainabilityAdminAction,
  formatExplainabilityDomain,
} from '../../explainability-ui'
import {
  formatExpressivenessAdminAction,
  formatExpressivenessDomain,
} from '../../expressiveness-ui'
import {
  formatExtensibilityAdminAction,
  formatExtensibilityDomain,
} from '../../extensibility-ui'
import {
  formatFamiliarityAdminAction,
  formatFamiliarityDomain,
} from '../../familiarity-ui'
import {
  formatFeasibilityAdminAction,
  formatFeasibilityDomain,
} from '../../feasibility-ui'
import {
  formatFlexibilityAdminAction,
  formatFlexibilityDomain,
} from '../../flexibility-ui'
import {
  formatFormulatabilityAdminAction,
  formatFormulatabilityDomain,
} from '../../formulatability-ui'
import {
  formatGovernanceAdminAction,
  formatGovernanceDomain,
} from '../../governance-ui'
import {
  formatIdentifiabilityAdminAction,
  formatIdentifiabilityDomain,
} from '../../identifiability-ui'
import {
  formatIllustratabilityAdminAction,
  formatIllustratabilityDomain,
} from '../../illustratability-ui'
import {
  formatIncidentAdminAction,
  formatIncidentDomain,
} from '../../incident-response-ui'
import {
  formatInspectabilityAdminAction,
  formatInspectabilityDomain,
} from '../../inspectability-ui'
import {
  formatIntegrabilityAdminAction,
  formatIntegrabilityDomain,
} from '../../integrability-ui'
import {
  formatIntegrityAdminAction,
  formatIntegrityDomain,
} from '../../integrity-ui'
import {
  formatIntelligibilityAdminAction,
  formatIntelligibilityDomain,
} from '../../intelligibility-ui'
import {
  formatInterchangeabilityAdminAction,
  formatInterchangeabilityDomain,
} from '../../interchangeability-ui'
import {
  formatInterpretabilityAdminAction,
  formatInterpretabilityDomain,
} from '../../interpretability-ui'
import {
  formatJustifiabilityAdminAction,
  formatJustifiabilityDomain,
} from '../../justifiability-ui'
import {
  formatLearnabilityAdminAction,
  formatLearnabilityDomain,
} from '../../learnability-ui'
import {
  formatLegibilityAdminAction,
  formatLegibilityDomain,
} from '../../legibility-ui'
import {
  formatLinkabilityAdminAction,
  formatLinkabilityDomain,
} from '../../linkability-ui'
import {
  formatLocatabilityAdminAction,
  formatLocatabilityDomain,
} from '../../locatability-ui'
import {
  formatMaintainabilityAdminAction,
  formatMaintainabilityDomain,
} from '../../maintainability-ui'
import {
  formatManageabilityAdminAction,
  formatManageabilityDomain,
} from '../../manageability-ui'
import {
  formatMarketabilityAdminAction,
  formatMarketabilityDomain,
} from '../../marketability-ui'
import {
  formatMaterializabilityAdminAction,
  formatMaterializabilityDomain,
} from '../../materializability-ui'
import {
  formatMeasurabilityAdminAction,
  formatMeasurabilityDomain,
} from '../../measurability-ui'
import {
  formatMemorabilityAdminAction,
  formatMemorabilityDomain,
} from '../../memorability-ui'
import {
  formatModifiabilityAdminAction,
  formatModifiabilityDomain,
} from '../../modifiability-ui'
import {
  formatMonitorabilityAdminAction,
  formatMonitorabilityDomain,
} from '../../monitorability-ui'
import {
  formatNarratabilityAdminAction,
  formatNarratabilityDomain,
} from '../../narratability-ui'
import {
  formatNavigabilityAdminAction,
  formatNavigabilityDomain,
} from '../../navigability-ui'
import {
  formatNegotiabilityAdminAction,
  formatNegotiabilityDomain,
} from '../../negotiability-ui'
import {
  formatNoticeabilityAdminAction,
  formatNoticeabilityDomain,
} from '../../noticeability-ui'
import {
  formatOperabilityAdminAction,
  formatOperabilityDomain,
} from '../../operability-ui'
import {
  formatOptimizationAdminAction,
  formatOptimizationDomain,
} from '../../optimization-ui'
import {
  formatOrchestrabilityAdminAction,
  formatOrchestrabilityDomain,
} from '../../orchestrability-ui'
import {
  formatOversightAdminAction,
  formatOversightDomain,
} from '../../oversight-ui'
import {
  formatParsabilityAdminAction,
  formatParsabilityDomain,
} from '../../parsability-ui'
import {
  formatPerceptibilityAdminAction,
  formatPerceptibilityDomain,
} from '../../perceptibility-ui'
import {
  formatPerformanceAdminAction,
  formatPerformanceDomain,
} from '../../performance-ui'
import {
  formatPersonifiabilityAdminAction,
  formatPersonifiabilityDomain,
} from '../../personifiability-ui'
import {
  formatPortabilityAdminAction,
  formatPortabilityDomain,
} from '../../portability-ui'
import {
  formatPredictabilityAdminAction,
  formatPredictabilityDomain,
} from '../../predictability-ui'
import {
  formatPresentabilityAdminAction,
  formatPresentabilityDomain,
} from '../../presentability-ui'
import {
  formatProfitabilityAdminAction,
  formatProfitabilityDomain,
} from '../../profitability-ui'
import {
  formatProgrammabilityAdminAction,
  formatProgrammabilityDomain,
} from '../../programmability-ui'
import {
  formatProvenanceAdminAction,
  formatProvenanceDomain,
} from '../../provenance-ui'
import {
  formatReadabilityAdminAction,
  formatReadabilityDomain,
} from '../../readability-ui'
import {
  formatRecognizabilityAdminAction,
  formatRecognizabilityDomain,
} from '../../recognizability-ui'
import {
  formatRecoverabilityAdminAction,
  formatRecoverabilityDomain,
} from '../../recoverability-ui'
import {
  formatReferencabilityAdminAction,
  formatReferencabilityDomain,
} from '../../referencability-ui'
import {
  formatReleaseAdminAction,
  formatReleaseDomain,
} from '../../release-ui'
import {
  formatReliabilityAdminAction,
  formatReliabilityDomain,
} from '../../reliability-ui'
import {
  formatRepeatabilityAdminAction,
  formatRepeatabilityDomain,
} from '../../repeatability-ui'
import {
  formatRepresentabilityAdminAction,
  formatRepresentabilityDomain,
} from '../../representability-ui'
import {
  formatReproducibilityAdminAction,
  formatReproducibilityDomain,
} from '../../reproducibility-ui'
import {
  formatResilienceAdminAction,
  formatResilienceDomain,
} from '../../resilience-ui'
import {
  formatResponsivenessAdminAction,
  formatResponsivenessDomain,
} from '../../responsiveness-ui'
import {
  formatRetrievabilityAdminAction,
  formatRetrievabilityDomain,
} from '../../retrievability-ui'
import {
  formatReviewabilityAdminAction,
  formatReviewabilityDomain,
} from '../../reviewability-ui'
import {
  formatScalabilityAdminAction,
  formatScalabilityDomain,
} from '../../scalability-ui'
import {
  formatScannabilityAdminAction,
  formatScannabilityDomain,
} from '../../scannability-ui'
import {
  formatSchedulabilityAdminAction,
  formatSchedulabilityDomain,
} from '../../schedulability-ui'
import {
  formatSignifiabilityAdminAction,
  formatSignifiabilityDomain,
} from '../../signifiability-ui'
import {
  formatSimplicityAdminAction,
  formatSimplicityDomain,
} from '../../simplicity-ui'
import {
  formatSloAdminAction,
  formatSloDomain,
} from '../../slo-ui'
import {
  formatStabilityAdminAction,
  formatStabilityDomain,
} from '../../stability-ui'
import {
  formatSubstantiabilityAdminAction,
  formatSubstantiabilityDomain,
} from '../../substantiability-ui'
import {
  formatSuitabilityAdminAction,
  formatSuitabilityDomain,
} from '../../suitability-ui'
import {
  formatSurvivabilityAdminAction,
  formatSurvivabilityDomain,
} from '../../survivability-ui'
import {
  formatSustainabilityAdminAction,
  formatSustainabilityDomain,
} from '../../sustainability-ui'
import {
  formatSymbolizabilityAdminAction,
  formatSymbolizabilityDomain,
} from '../../symbolizability-ui'
import {
  formatTeachabilityAdminAction,
  formatTeachabilityDomain,
} from '../../teachability-ui'
import {
  formatTraceabilityAdminAction,
  formatTraceabilityDomain,
} from '../../traceability-ui'
import {
  formatTransferabilityAdminAction,
  formatTransferabilityDomain,
} from '../../transferability-ui'
import {
  formatTransparencyAdminAction,
  formatTransparencyDomain,
} from '../../transparency-ui'
import {
  formatTrustworthinessAdminAction,
  formatTrustworthinessDomain,
} from '../../trustworthiness-ui'
import {
  formatTunabilityAdminAction,
  formatTunabilityDomain,
} from '../../tunability-ui'
import {
  formatUnderstandabilityAdminAction,
  formatUnderstandabilityDomain,
} from '../../understandability-ui'
import {
  formatUsabilityAdminAction,
  formatUsabilityDomain,
} from '../../usability-ui'
import {
  formatUtilizationAdminAction,
  formatUtilizationDomain,
} from '../../utilization-ui'
import {
  formatValidityAdminAction,
  formatValidityDomain,
} from '../../validity-ui'
import {
  formatVerifiabilityAdminAction,
  formatVerifiabilityDomain,
} from '../../verifiability-ui'
import {
  formatViabilityAdminAction,
  formatViabilityDomain,
} from '../../viability-ui'
import {
  formatVisualizabilityAdminAction,
  formatVisualizabilityDomain,
} from '../../visualizability-ui'
import {
  formatWarrantabilityAdminAction,
  formatWarrantabilityDomain,
} from '../../warrantability-ui'

import { DomainCoverageAdminPanel } from '@ai-war-room/web-blocks'
import type { DomainAdminBulkProps } from './DomainAdminBulk'

export default function DomainAdminBulkPart1(
  props: DomainAdminBulkProps,
) {
  return (
    <>
        {props.backupAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Backup admin"
            panelClassName="workspace-backup-admin"
            listClassName="workspace-backup-list"
            cardClassName="workspace-backup-card"
            role={props.backupAdminSummary.role}
            guidance={props.backupAdminSummary.guidance}
            stats={[
            {
              label: 'Recoverable records',
              value: props.backupAdminSummary.stats.totalRecords,
              detail: <>{props.backupAdminSummary.stats.recoverableDomains}/
                  {props.backupAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Persistence',
              value: props.backupAdminSummary.stats.postgresConnectivity
                    ? 'PostgreSQL'
                    : 'Unavailable',
              detail: <>{props.backupAdminSummary.stats.redisBackedPersistence
                    ? 'Redis AOF enabled'
                    : 'Redis optional'}</>,
            }
            ]}
            records={props.backupAdminSummary.records}
            availableActions={props.backupAdminSummary.availableActions}
            refreshAction="refresh_backup_summary"
            actionBusy={props.backupAdminAction !== 'idle'}
            formatDomain={formatBackupDomain as (domain: string) => string}
            formatAdminAction={formatBackupAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleBackupAdminAction('refresh_backup_summary')
            }
          />
        ) : null}
        {props.auditAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Audit trail admin"
            panelClassName="workspace-audit-trail-admin"
            listClassName="workspace-audit-trail-list"
            cardClassName="workspace-audit-trail-card"
            role={props.auditAdminSummary.role}
            guidance={props.auditAdminSummary.guidance}
            stats={[
            {
              label: 'Audit records',
              value: props.auditAdminSummary.stats.totalRecords,
              detail: <>{props.auditAdminSummary.stats.coveredDomains}/
                  {props.auditAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Export',
              value: Enabled,
              detail: <>{props.auditAdminSummary.stats.postgresConnectivity
                    ? 'PostgreSQL audit tables'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.auditAdminSummary.records}
            availableActions={props.auditAdminSummary.availableActions}
            refreshAction="refresh_audit_summary"
            actionBusy={props.auditAdminAction !== 'idle'}
            formatDomain={formatAuditDomain as (domain: string) => string}
            formatAdminAction={formatAuditAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleAuditAdminAction('refresh_audit_summary')
            }
          />
        ) : null}
        {props.complianceAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Compliance admin"
            panelClassName="workspace-compliance-admin"
            listClassName="workspace-compliance-list"
            cardClassName="workspace-compliance-card"
            role={props.complianceAdminSummary.role}
            guidance={props.complianceAdminSummary.guidance}
            stats={[
            {
              label: 'Attestation records',
              value: props.complianceAdminSummary.stats.totalRecords,
              detail: <>{props.complianceAdminSummary.stats.coveredDomains}/
                  {props.complianceAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Encryption',
              value: props.complianceAdminSummary.stats.encryptionKeyConfigured
                    ? 'Configured'
                    : 'Default key',
              detail: <>{props.complianceAdminSummary.stats.postgresConnectivity
                    ? 'PostgreSQL policy tables'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.complianceAdminSummary.records}
            availableActions={props.complianceAdminSummary.availableActions}
            refreshAction="refresh_compliance_summary"
            actionBusy={props.complianceAdminAction !== 'idle'}
            formatDomain={formatComplianceDomain as (domain: string) => string}
            formatAdminAction={formatComplianceAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleComplianceAdminAction('refresh_compliance_summary')
            }
          />
        ) : null}
        {props.incidentAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Incident admin"
            panelClassName="workspace-incident-admin"
            listClassName="workspace-incident-list"
            cardClassName="workspace-incident-card"
            role={props.incidentAdminSummary.role}
            guidance={props.incidentAdminSummary.guidance}
            stats={[
            {
              label: 'Incident records',
              value: props.incidentAdminSummary.stats.totalRecords,
              detail: <>{props.incidentAdminSummary.stats.coveredDomains}/
                  {props.incidentAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Observability errors',
              value: props.incidentAdminSummary.stats.observabilityErrorEvents,
              detail: <>{props.incidentAdminSummary.stats.postgresConnectivity
                    ? 'Recent workspace error events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.incidentAdminSummary.records}
            availableActions={props.incidentAdminSummary.availableActions}
            refreshAction="refresh_incident_summary"
            actionBusy={props.incidentAdminAction !== 'idle'}
            formatDomain={formatIncidentDomain as (domain: string) => string}
            formatAdminAction={formatIncidentAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleIncidentAdminAction('refresh_incident_summary')
            }
          />
        ) : null}
        {props.releaseAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Release admin"
            panelClassName="workspace-release-admin"
            listClassName="workspace-release-list"
            cardClassName="workspace-release-card"
            role={props.releaseAdminSummary.role}
            guidance={props.releaseAdminSummary.guidance}
            stats={[
            {
              label: 'Release records',
              value: props.releaseAdminSummary.stats.totalRecords,
              detail: <>{props.releaseAdminSummary.stats.coveredDomains}/
                  {props.releaseAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'API version',
              value: props.releaseAdminSummary.stats.apiVersion,
              detail: <>{props.releaseAdminSummary.stats.postgresConnectivity
                    ? 'Release artifact tables'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.releaseAdminSummary.records}
            availableActions={props.releaseAdminSummary.availableActions}
            refreshAction="refresh_release_summary"
            actionBusy={props.releaseAdminAction !== 'idle'}
            formatDomain={formatReleaseDomain as (domain: string) => string}
            formatAdminAction={formatReleaseAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleReleaseAdminAction('refresh_release_summary')
            }
          />
        ) : null}
        {props.sloAdminSummary ? (
          <DomainCoverageAdminPanel
            title="SLO admin"
            panelClassName="workspace-slo-admin"
            listClassName="workspace-slo-list"
            cardClassName="workspace-slo-card"
            role={props.sloAdminSummary.role}
            guidance={props.sloAdminSummary.guidance}
            stats={[
            {
              label: 'Run success rate',
              value: `${props.sloAdminSummary.stats.successRatePercent}%`,
              detail: <>{props.sloAdminSummary.stats.coveredDomains}/
                  {props.sloAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'SLO signals',
              value: props.sloAdminSummary.stats.totalRecords,
              detail: <>{props.sloAdminSummary.stats.postgresConnectivity
                    ? 'Usage and run outcome signals'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.sloAdminSummary.records}
            availableActions={props.sloAdminSummary.availableActions}
            refreshAction="refresh_slo_summary"
            actionBusy={props.sloAdminAction !== 'idle'}
            formatDomain={formatSloDomain as (domain: string) => string}
            formatAdminAction={formatSloAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleSloAdminAction('refresh_slo_summary')
            }
          />
        ) : null}
        {props.capacityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Capacity admin"
            panelClassName="workspace-capacity-admin"
            listClassName="workspace-capacity-list"
            cardClassName="workspace-capacity-card"
            role={props.capacityAdminSummary.role}
            guidance={props.capacityAdminSummary.guidance}
            stats={[
            {
              label: 'Concurrent load',
              value: `${props.capacityAdminSummary.stats.loadUtilizationPercent}%`,
              detail: <>{props.capacityAdminSummary.stats.coveredDomains}/
                  {props.capacityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Capacity signals',
              value: props.capacityAdminSummary.stats.totalRecords,
              detail: <>{props.capacityAdminSummary.stats.postgresConnectivity
                    ? 'Run load and usage limit signals'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.capacityAdminSummary.records}
            availableActions={props.capacityAdminSummary.availableActions}
            refreshAction="refresh_capacity_summary"
            actionBusy={props.capacityAdminAction !== 'idle'}
            formatDomain={formatCapacityDomain as (domain: string) => string}
            formatAdminAction={formatCapacityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleCapacityAdminAction('refresh_capacity_summary')
            }
          />
        ) : null}
        {props.performanceAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Performance admin"
            panelClassName="workspace-performance-admin"
            listClassName="workspace-performance-list"
            cardClassName="workspace-performance-card"
            role={props.performanceAdminSummary.role}
            guidance={props.performanceAdminSummary.guidance}
            stats={[
            {
              label: 'Average latency',
              value: `${props.performanceAdminSummary.stats.averageLatencyMs}ms`,
              detail: <>{props.performanceAdminSummary.stats.latencySignalPercent}% latency
                  signal coverage</>,
            },
            {
              label: 'Slowest phase',
              value: props.performanceAdminSummary.stats.slowestPipelinePhases[0]
                ? `${props.performanceAdminSummary.stats.slowestPipelinePhases[0].phase}`
                : 'n/a',
              detail: (
                <>
                  {props.performanceAdminSummary.stats.slowestPipelinePhases[0]
                    ? `${props.performanceAdminSummary.stats.slowestPipelinePhases[0].durationMs}ms recent max`
                    : 'No pipeline_phase_completed samples yet'}
                </>
              ),
            },
            {
              label: 'Performance signals',
              value: props.performanceAdminSummary.stats.totalRecords,
              detail: <>{props.performanceAdminSummary.stats.postgresConnectivity
                    ? `${props.performanceAdminSummary.stats.coveredDomains}/${props.performanceAdminSummary.stats.totalDomains} domains covered`
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.performanceAdminSummary.records}
            availableActions={props.performanceAdminSummary.availableActions}
            refreshAction="refresh_performance_summary"
            actionBusy={props.performanceAdminAction !== 'idle'}
            formatDomain={formatPerformanceDomain as (domain: string) => string}
            formatAdminAction={formatPerformanceAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handlePerformanceAdminAction('refresh_performance_summary')
            }
          />
        ) : null}
        {props.resilienceAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Resilience admin"
            panelClassName="workspace-resilience-admin"
            listClassName="workspace-resilience-list"
            cardClassName="workspace-resilience-card"
            role={props.resilienceAdminSummary.role}
            guidance={props.resilienceAdminSummary.guidance}
            stats={[
            {
              label: 'Recovery readiness',
              value: `${props.resilienceAdminSummary.stats.recoveryReadinessPercent}%`,
              detail: <>{props.resilienceAdminSummary.stats.coveredDomains}/
                  {props.resilienceAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Resilience signals',
              value: props.resilienceAdminSummary.stats.totalRecords,
              detail: <>{props.resilienceAdminSummary.stats.postgresConnectivity
                    ? 'Run workflow and migration recovery signals'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.resilienceAdminSummary.records}
            availableActions={props.resilienceAdminSummary.availableActions}
            refreshAction="refresh_resilience_summary"
            actionBusy={props.resilienceAdminAction !== 'idle'}
            formatDomain={formatResilienceDomain as (domain: string) => string}
            formatAdminAction={formatResilienceAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleResilienceAdminAction('refresh_resilience_summary')
            }
          />
        ) : null}
        {props.availabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Availability admin"
            panelClassName="workspace-availability-admin"
            listClassName="workspace-availability-list"
            cardClassName="workspace-availability-card"
            role={props.availabilityAdminSummary.role}
            guidance={props.availabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Run availability',
              value: `${props.availabilityAdminSummary.stats.availabilityPercent}%`,
              detail: <>{props.availabilityAdminSummary.stats.coveredDomains}/
                  {props.availabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Availability signals',
              value: props.availabilityAdminSummary.stats.totalRecords,
              detail: <>{props.availabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcome and usage availability signals'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.availabilityAdminSummary.records}
            availableActions={props.availabilityAdminSummary.availableActions}
            refreshAction="refresh_availability_summary"
            actionBusy={props.availabilityAdminAction !== 'idle'}
            formatDomain={formatAvailabilityDomain as (domain: string) => string}
            formatAdminAction={formatAvailabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleAvailabilityAdminAction('refresh_availability_summary')
            }
          />
        ) : null}
        {props.reliabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Reliability admin"
            panelClassName="workspace-reliability-admin"
            listClassName="workspace-reliability-list"
            cardClassName="workspace-reliability-card"
            role={props.reliabilityAdminSummary.role}
            guidance={props.reliabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Run reliability',
              value: `${props.reliabilityAdminSummary.stats.reliabilityPercent}%`,
              detail: <>{props.reliabilityAdminSummary.stats.coveredDomains}/
                  {props.reliabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Reliability signals',
              value: props.reliabilityAdminSummary.stats.totalRecords,
              detail: <>{props.reliabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, idempotency, and model health signals'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.reliabilityAdminSummary.records}
            availableActions={props.reliabilityAdminSummary.availableActions}
            refreshAction="refresh_reliability_summary"
            actionBusy={props.reliabilityAdminAction !== 'idle'}
            formatDomain={formatReliabilityDomain as (domain: string) => string}
            formatAdminAction={formatReliabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleReliabilityAdminAction('refresh_reliability_summary')
            }
          />
        ) : null}
        {props.stabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Stability admin"
            panelClassName="workspace-stability-admin"
            listClassName="workspace-stability-list"
            cardClassName="workspace-stability-card"
            role={props.stabilityAdminSummary.role}
            guidance={props.stabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Run stability',
              value: `${props.stabilityAdminSummary.stats.stabilityPercent}%`,
              detail: <>{props.stabilityAdminSummary.stats.coveredDomains}/
                  {props.stabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Stability signals',
              value: props.stabilityAdminSummary.stats.totalRecords,
              detail: <>{props.stabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, artifacts, and migration signals'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.stabilityAdminSummary.records}
            availableActions={props.stabilityAdminSummary.availableActions}
            refreshAction="refresh_stability_summary"
            actionBusy={props.stabilityAdminAction !== 'idle'}
            formatDomain={formatStabilityDomain as (domain: string) => string}
            formatAdminAction={formatStabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleStabilityAdminAction('refresh_stability_summary')
            }
          />
        ) : null}
        {props.consistencyAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Consistency admin"
            panelClassName="workspace-consistency-admin"
            listClassName="workspace-consistency-list"
            cardClassName="workspace-consistency-card"
            role={props.consistencyAdminSummary.role}
            guidance={props.consistencyAdminSummary.guidance}
            stats={[
            {
              label: 'Run consistency',
              value: `${props.consistencyAdminSummary.stats.consistencyPercent}%`,
              detail: <>{props.consistencyAdminSummary.stats.coveredDomains}/
                  {props.consistencyAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Consistency signals',
              value: props.consistencyAdminSummary.stats.totalRecords,
              detail: <>{props.consistencyAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, workflows, and idempotency signals'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.consistencyAdminSummary.records}
            availableActions={props.consistencyAdminSummary.availableActions}
            refreshAction="refresh_consistency_summary"
            actionBusy={props.consistencyAdminAction !== 'idle'}
            formatDomain={formatConsistencyDomain as (domain: string) => string}
            formatAdminAction={formatConsistencyAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleConsistencyAdminAction('refresh_consistency_summary')
            }
          />
        ) : null}
        {props.integrityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Integrity admin"
            panelClassName="workspace-integrity-admin"
            listClassName="workspace-integrity-list"
            cardClassName="workspace-integrity-card"
            role={props.integrityAdminSummary.role}
            guidance={props.integrityAdminSummary.guidance}
            stats={[
            {
              label: 'Run integrity',
              value: `${props.integrityAdminSummary.stats.integrityPercent}%`,
              detail: <>{props.integrityAdminSummary.stats.coveredDomains}/
                  {props.integrityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Integrity signals',
              value: props.integrityAdminSummary.stats.totalRecords,
              detail: <>{props.integrityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, artifacts, and shield scan signals'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.integrityAdminSummary.records}
            availableActions={props.integrityAdminSummary.availableActions}
            refreshAction="refresh_integrity_summary"
            actionBusy={props.integrityAdminAction !== 'idle'}
            formatDomain={formatIntegrityDomain as (domain: string) => string}
            formatAdminAction={formatIntegrityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleIntegrityAdminAction('refresh_integrity_summary')
            }
          />
        ) : null}
        {props.durabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Durability admin"
            panelClassName="workspace-durability-admin"
            listClassName="workspace-durability-list"
            cardClassName="workspace-durability-card"
            role={props.durabilityAdminSummary.role}
            guidance={props.durabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Artifact durability',
              value: `${props.durabilityAdminSummary.stats.durabilityPercent}%`,
              detail: <>{props.durabilityAdminSummary.stats.coveredDomains}/
                  {props.durabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Durability signals',
              value: props.durabilityAdminSummary.stats.totalRecords,
              detail: <>{props.durabilityAdminSummary.stats.postgresConnectivity
                    ? 'Artifacts, usage events, and idempotency signals'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.durabilityAdminSummary.records}
            availableActions={props.durabilityAdminSummary.availableActions}
            refreshAction="refresh_durability_summary"
            actionBusy={props.durabilityAdminAction !== 'idle'}
            formatDomain={formatDurabilityDomain as (domain: string) => string}
            formatAdminAction={formatDurabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleDurabilityAdminAction('refresh_durability_summary')
            }
          />
        ) : null}
        {props.recoverabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Recoverability admin"
            panelClassName="workspace-recoverability-admin"
            listClassName="workspace-recoverability-list"
            cardClassName="workspace-recoverability-card"
            role={props.recoverabilityAdminSummary.role}
            guidance={props.recoverabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Run recoverability',
              value: `${props.recoverabilityAdminSummary.stats.recoverabilityPercent}%`,
              detail: <>{props.recoverabilityAdminSummary.stats.coveredDomains}/
                  {props.recoverabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Recovery signals',
              value: props.recoverabilityAdminSummary.stats.totalRecords,
              detail: <>{props.recoverabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, workflows, and recovery signals'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.recoverabilityAdminSummary.records}
            availableActions={props.recoverabilityAdminSummary.availableActions}
            refreshAction="refresh_recoverability_summary"
            actionBusy={props.recoverabilityAdminAction !== 'idle'}
            formatDomain={formatRecoverabilityDomain as (domain: string) => string}
            formatAdminAction={formatRecoverabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleRecoverabilityAdminAction('refresh_recoverability_summary')
            }
          />
        ) : null}
        {props.maintainabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Maintainability admin"
            panelClassName="workspace-maintainability-admin"
            listClassName="workspace-maintainability-list"
            cardClassName="workspace-maintainability-card"
            role={props.maintainabilityAdminSummary.role}
            guidance={props.maintainabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Run maintainability',
              value: `${props.maintainabilityAdminSummary.stats.maintainabilityPercent}%`,
              detail: <>{props.maintainabilityAdminSummary.stats.coveredDomains}/
                  {props.maintainabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Maintainability signals',
              value: props.maintainabilityAdminSummary.stats.totalRecords,
              detail: <>{props.maintainabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, model health, and usage telemetry'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.maintainabilityAdminSummary.records}
            availableActions={props.maintainabilityAdminSummary.availableActions}
            refreshAction="refresh_maintainability_summary"
            actionBusy={props.maintainabilityAdminAction !== 'idle'}
            formatDomain={formatMaintainabilityDomain as (domain: string) => string}
            formatAdminAction={formatMaintainabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleMaintainabilityAdminAction('refresh_maintainability_summary')
            }
          />
        ) : null}
        {props.scalabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Scalability admin"
            panelClassName="workspace-scalability-admin"
            listClassName="workspace-scalability-list"
            cardClassName="workspace-scalability-card"
            role={props.scalabilityAdminSummary.role}
            guidance={props.scalabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Run scalability',
              value: `${props.scalabilityAdminSummary.stats.scalabilityPercent}%`,
              detail: <>{props.scalabilityAdminSummary.stats.coveredDomains}/
                  {props.scalabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Scalability signals',
              value: props.scalabilityAdminSummary.stats.totalRecords,
              detail: <>{props.scalabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run load, usage events, and membership growth'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.scalabilityAdminSummary.records}
            availableActions={props.scalabilityAdminSummary.availableActions}
            refreshAction="refresh_scalability_summary"
            actionBusy={props.scalabilityAdminAction !== 'idle'}
            formatDomain={formatScalabilityDomain as (domain: string) => string}
            formatAdminAction={formatScalabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleScalabilityAdminAction('refresh_scalability_summary')
            }
          />
        ) : null}
        {props.traceabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Traceability admin"
            panelClassName="workspace-traceability-admin"
            listClassName="workspace-traceability-list"
            cardClassName="workspace-traceability-card"
            role={props.traceabilityAdminSummary.role}
            guidance={props.traceabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Artifact lineage',
              value: `${props.traceabilityAdminSummary.stats.traceabilityPercent}%`,
              detail: <>{props.traceabilityAdminSummary.stats.coveredDomains}/
                  {props.traceabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Traceability signals',
              value: props.traceabilityAdminSummary.stats.totalRecords,
              detail: <>{props.traceabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, artifacts, and usage events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.traceabilityAdminSummary.records}
            availableActions={props.traceabilityAdminSummary.availableActions}
            refreshAction="refresh_traceability_summary"
            actionBusy={props.traceabilityAdminAction !== 'idle'}
            formatDomain={formatTraceabilityDomain as (domain: string) => string}
            formatAdminAction={formatTraceabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleTraceabilityAdminAction('refresh_traceability_summary')
            }
          />
        ) : null}
        {props.efficiencyAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Efficiency admin"
            panelClassName="workspace-efficiency-admin"
            listClassName="workspace-efficiency-list"
            cardClassName="workspace-efficiency-card"
            role={props.efficiencyAdminSummary.role}
            guidance={props.efficiencyAdminSummary.guidance}
            stats={[
            {
              label: 'Usage telemetry efficiency',
              value: `${props.efficiencyAdminSummary.stats.efficiencyPercent}%`,
              detail: <>{props.efficiencyAdminSummary.stats.coveredDomains}/
                  {props.efficiencyAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Efficiency signals',
              value: props.efficiencyAdminSummary.stats.totalRecords,
              detail: <>{props.efficiencyAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, usage events, and cost limits'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.efficiencyAdminSummary.records}
            availableActions={props.efficiencyAdminSummary.availableActions}
            refreshAction="refresh_efficiency_summary"
            actionBusy={props.efficiencyAdminAction !== 'idle'}
            formatDomain={formatEfficiencyDomain as (domain: string) => string}
            formatAdminAction={formatEfficiencyAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleEfficiencyAdminAction('refresh_efficiency_summary')
            }
          />
        ) : null}
        {props.optimizationAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Optimization admin"
            panelClassName="workspace-optimization-admin"
            listClassName="workspace-optimization-list"
            cardClassName="workspace-optimization-card"
            role={props.optimizationAdminSummary.role}
            guidance={props.optimizationAdminSummary.guidance}
            stats={[
            {
              label: 'Model health optimization',
              value: `${props.optimizationAdminSummary.stats.optimizationPercent}%`,
              detail: <>{props.optimizationAdminSummary.stats.coveredDomains}/
                  {props.optimizationAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Optimization signals',
              value: props.optimizationAdminSummary.stats.totalRecords,
              detail: <>{props.optimizationAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, usage events, and model health'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.optimizationAdminSummary.records}
            availableActions={props.optimizationAdminSummary.availableActions}
            refreshAction="refresh_optimization_summary"
            actionBusy={props.optimizationAdminAction !== 'idle'}
            formatDomain={formatOptimizationDomain as (domain: string) => string}
            formatAdminAction={formatOptimizationAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleOptimizationAdminAction('refresh_optimization_summary')
            }
          />
        ) : null}
        {props.utilizationAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Utilization admin"
            panelClassName="workspace-utilization-admin"
            listClassName="workspace-utilization-list"
            cardClassName="workspace-utilization-card"
            role={props.utilizationAdminSummary.role}
            guidance={props.utilizationAdminSummary.guidance}
            stats={[
            {
              label: 'Workspace utilization',
              value: `${props.utilizationAdminSummary.stats.utilizationPercent}%`,
              detail: <>{props.utilizationAdminSummary.stats.coveredDomains}/
                  {props.utilizationAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Utilization signals',
              value: props.utilizationAdminSummary.stats.totalRecords,
              detail: <>{props.utilizationAdminSummary.stats.postgresConnectivity
                    ? 'Run load, usage events, and memberships'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.utilizationAdminSummary.records}
            availableActions={props.utilizationAdminSummary.availableActions}
            refreshAction="refresh_utilization_summary"
            actionBusy={props.utilizationAdminAction !== 'idle'}
            formatDomain={formatUtilizationDomain as (domain: string) => string}
            formatAdminAction={formatUtilizationAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleUtilizationAdminAction('refresh_utilization_summary')
            }
          />
        ) : null}
        {props.sustainabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Sustainability admin"
            panelClassName="workspace-sustainability-admin"
            listClassName="workspace-sustainability-list"
            cardClassName="workspace-sustainability-card"
            role={props.sustainabilityAdminSummary.role}
            guidance={props.sustainabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Run sustainability',
              value: `${props.sustainabilityAdminSummary.stats.sustainabilityPercent}%`,
              detail: <>{props.sustainabilityAdminSummary.stats.coveredDomains}/
                  {props.sustainabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Sustainability signals',
              value: props.sustainabilityAdminSummary.stats.totalRecords,
              detail: <>{props.sustainabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, billing records, and usage events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.sustainabilityAdminSummary.records}
            availableActions={props.sustainabilityAdminSummary.availableActions}
            refreshAction="refresh_sustainability_summary"
            actionBusy={props.sustainabilityAdminAction !== 'idle'}
            formatDomain={formatSustainabilityDomain as (domain: string) => string}
            formatAdminAction={formatSustainabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleSustainabilityAdminAction('refresh_sustainability_summary')
            }
          />
        ) : null}
        {props.governanceAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Governance admin"
            panelClassName="workspace-governance-admin"
            listClassName="workspace-governance-list"
            cardClassName="workspace-governance-card"
            role={props.governanceAdminSummary.role}
            guidance={props.governanceAdminSummary.guidance}
            stats={[
            {
              label: 'Credential governance',
              value: `${props.governanceAdminSummary.stats.governancePercent}%`,
              detail: <>{props.governanceAdminSummary.stats.coveredDomains}/
                  {props.governanceAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Governance signals',
              value: props.governanceAdminSummary.stats.totalRecords,
              detail: <>{props.governanceAdminSummary.stats.postgresConnectivity
                    ? 'Memberships, credentials, shield reviews, billing'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.governanceAdminSummary.records}
            availableActions={props.governanceAdminSummary.availableActions}
            refreshAction="refresh_governance_summary"
            actionBusy={props.governanceAdminAction !== 'idle'}
            formatDomain={formatGovernanceDomain as (domain: string) => string}
            formatAdminAction={formatGovernanceAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleGovernanceAdminAction('refresh_governance_summary')
            }
          />
        ) : null}
        {props.oversightAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Oversight admin"
            panelClassName="workspace-oversight-admin"
            listClassName="workspace-oversight-list"
            cardClassName="workspace-oversight-card"
            role={props.oversightAdminSummary.role}
            guidance={props.oversightAdminSummary.guidance}
            stats={[
            {
              label: 'Billing oversight',
              value: `${props.oversightAdminSummary.stats.oversightPercent}%`,
              detail: <>{props.oversightAdminSummary.stats.coveredDomains}/
                  {props.oversightAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Oversight signals',
              value: props.oversightAdminSummary.stats.totalRecords,
              detail: <>{props.oversightAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, invoices, and webhook events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.oversightAdminSummary.records}
            availableActions={props.oversightAdminSummary.availableActions}
            refreshAction="refresh_oversight_summary"
            actionBusy={props.oversightAdminAction !== 'idle'}
            formatDomain={formatOversightDomain as (domain: string) => string}
            formatAdminAction={formatOversightAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleOversightAdminAction('refresh_oversight_summary')
            }
          />
        ) : null}
        {props.assuranceAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Assurance admin"
            panelClassName="workspace-assurance-admin"
            listClassName="workspace-assurance-list"
            cardClassName="workspace-assurance-card"
            role={props.assuranceAdminSummary.role}
            guidance={props.assuranceAdminSummary.guidance}
            stats={[
            {
              label: 'Shield quality assurance',
              value: `${props.assuranceAdminSummary.stats.assurancePercent}%`,
              detail: <>{props.assuranceAdminSummary.stats.coveredDomains}/
                  {props.assuranceAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Assurance signals',
              value: props.assuranceAdminSummary.stats.totalRecords,
              detail: <>{props.assuranceAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, shield reviews, and artifacts'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.assuranceAdminSummary.records}
            availableActions={props.assuranceAdminSummary.availableActions}
            refreshAction="refresh_assurance_summary"
            actionBusy={props.assuranceAdminAction !== 'idle'}
            formatDomain={formatAssuranceDomain as (domain: string) => string}
            formatAdminAction={formatAssuranceAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleAssuranceAdminAction('refresh_assurance_summary')
            }
          />
        ) : null}
        {props.accountabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Accountability admin"
            panelClassName="workspace-accountability-admin"
            listClassName="workspace-accountability-list"
            cardClassName="workspace-accountability-card"
            role={props.accountabilityAdminSummary.role}
            guidance={props.accountabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Idempotency accountability',
              value: `${props.accountabilityAdminSummary.stats.accountabilityPercent}%`,
              detail: <>{props.accountabilityAdminSummary.stats.coveredDomains}/
                  {props.accountabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Accountability signals',
              value: props.accountabilityAdminSummary.stats.totalRecords,
              detail: <>{props.accountabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, idempotency keys, and billing records'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.accountabilityAdminSummary.records}
            availableActions={props.accountabilityAdminSummary.availableActions}
            refreshAction="refresh_accountability_summary"
            actionBusy={props.accountabilityAdminAction !== 'idle'}
            formatDomain={formatAccountabilityDomain as (domain: string) => string}
            formatAdminAction={formatAccountabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleAccountabilityAdminAction('refresh_accountability_summary')
            }
          />
        ) : null}
        {props.transparencyAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Transparency admin"
            panelClassName="workspace-transparency-admin"
            listClassName="workspace-transparency-list"
            cardClassName="workspace-transparency-card"
            role={props.transparencyAdminSummary.role}
            guidance={props.transparencyAdminSummary.guidance}
            stats={[
            {
              label: 'Workflow transparency',
              value: `${props.transparencyAdminSummary.stats.transparencyPercent}%`,
              detail: <>{props.transparencyAdminSummary.stats.coveredDomains}/
                  {props.transparencyAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Transparency signals',
              value: props.transparencyAdminSummary.stats.totalRecords,
              detail: <>{props.transparencyAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, workflows, and billing notifications'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.transparencyAdminSummary.records}
            availableActions={props.transparencyAdminSummary.availableActions}
            refreshAction="refresh_transparency_summary"
            actionBusy={props.transparencyAdminAction !== 'idle'}
            formatDomain={formatTransparencyDomain as (domain: string) => string}
            formatAdminAction={formatTransparencyAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleTransparencyAdminAction('refresh_transparency_summary')
            }
          />
        ) : null}
        {props.confirmabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Confirmability admin"
            panelClassName="workspace-confirmability-admin"
            listClassName="workspace-confirmability-list"
            cardClassName="workspace-confirmability-card"
            role={props.confirmabilityAdminSummary.role}
            guidance={props.confirmabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Billing notification confirmability',
              value: `${props.confirmabilityAdminSummary.stats.confirmabilityPercent}%`,
              detail: <>{props.confirmabilityAdminSummary.stats.coveredDomains}/
                  {props.confirmabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Confirmability signals',
              value: props.confirmabilityAdminSummary.stats.totalRecords,
              detail: <>{props.confirmabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, billing notifications, and usage limits'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.confirmabilityAdminSummary.records}
            availableActions={props.confirmabilityAdminSummary.availableActions}
            refreshAction="refresh_confirmability_summary"
            actionBusy={props.confirmabilityAdminAction !== 'idle'}
            formatDomain={formatConfirmabilityDomain as (domain: string) => string}
            formatAdminAction={formatConfirmabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleConfirmabilityAdminAction('refresh_confirmability_summary')
            }
          />
        ) : null}
        {props.verifiabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Verifiability admin"
            panelClassName="workspace-verifiability-admin"
            listClassName="workspace-verifiability-list"
            cardClassName="workspace-verifiability-card"
            role={props.verifiabilityAdminSummary.role}
            guidance={props.verifiabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Billing invoice verifiability',
              value: `${props.verifiabilityAdminSummary.stats.verifiabilityPercent}%`,
              detail: <>{props.verifiabilityAdminSummary.stats.coveredDomains}/
                  {props.verifiabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Verifiability signals',
              value: props.verifiabilityAdminSummary.stats.totalRecords,
              detail: <>{props.verifiabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, billing invoices, and webhook events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.verifiabilityAdminSummary.records}
            availableActions={props.verifiabilityAdminSummary.availableActions}
            refreshAction="refresh_verifiability_summary"
            actionBusy={props.verifiabilityAdminAction !== 'idle'}
            formatDomain={formatVerifiabilityDomain as (domain: string) => string}
            formatAdminAction={formatVerifiabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleVerifiabilityAdminAction('refresh_verifiability_summary')
            }
          />
        ) : null}
        {props.provenanceAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Provenance admin"
            panelClassName="workspace-provenance-admin"
            listClassName="workspace-provenance-list"
            cardClassName="workspace-provenance-card"
            role={props.provenanceAdminSummary.role}
            guidance={props.provenanceAdminSummary.guidance}
            stats={[
            {
              label: 'Usage provenance',
              value: `${props.provenanceAdminSummary.stats.provenancePercent}%`,
              detail: <>{props.provenanceAdminSummary.stats.coveredDomains}/
                  {props.provenanceAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Provenance signals',
              value: props.provenanceAdminSummary.stats.totalRecords,
              detail: <>{props.provenanceAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, usage events, and artifacts'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.provenanceAdminSummary.records}
            availableActions={props.provenanceAdminSummary.availableActions}
            refreshAction="refresh_provenance_summary"
            actionBusy={props.provenanceAdminAction !== 'idle'}
            formatDomain={formatProvenanceDomain as (domain: string) => string}
            formatAdminAction={formatProvenanceAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleProvenanceAdminAction('refresh_provenance_summary')
            }
          />
        ) : null}
        {props.authenticityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Authenticity admin"
            panelClassName="workspace-authenticity-admin"
            listClassName="workspace-authenticity-list"
            cardClassName="workspace-authenticity-card"
            role={props.authenticityAdminSummary.role}
            guidance={props.authenticityAdminSummary.guidance}
            stats={[
            {
              label: 'Moderator synthesis authenticity',
              value: `${props.authenticityAdminSummary.stats.authenticityPercent}%`,
              detail: <>{props.authenticityAdminSummary.stats.coveredDomains}/
                  {props.authenticityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Authenticity signals',
              value: props.authenticityAdminSummary.stats.totalRecords,
              detail: <>{props.authenticityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, moderator syntheses, and artifacts'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.authenticityAdminSummary.records}
            availableActions={props.authenticityAdminSummary.availableActions}
            refreshAction="refresh_authenticity_summary"
            actionBusy={props.authenticityAdminAction !== 'idle'}
            formatDomain={formatAuthenticityDomain as (domain: string) => string}
            formatAdminAction={formatAuthenticityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleAuthenticityAdminAction('refresh_authenticity_summary')
            }
          />
        ) : null}
        {props.attestationAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Attestation admin"
            panelClassName="workspace-attestation-admin"
            listClassName="workspace-attestation-list"
            cardClassName="workspace-attestation-card"
            role={props.attestationAdminSummary.role}
            guidance={props.attestationAdminSummary.guidance}
            stats={[
            {
              label: 'Provider credential attestation',
              value: `${props.attestationAdminSummary.stats.attestationPercent}%`,
              detail: <>{props.attestationAdminSummary.stats.coveredDomains}/
                  {props.attestationAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Attestation signals',
              value: props.attestationAdminSummary.stats.totalRecords,
              detail: <>{props.attestationAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, provider credentials, and model registry entries'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.attestationAdminSummary.records}
            availableActions={props.attestationAdminSummary.availableActions}
            refreshAction="refresh_attestation_summary"
            actionBusy={props.attestationAdminAction !== 'idle'}
            formatDomain={formatAttestationDomain as (domain: string) => string}
            formatAdminAction={formatAttestationAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleAttestationAdminAction('refresh_attestation_summary')
            }
          />
        ) : null}
        {props.auditabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Auditability admin"
            panelClassName="workspace-auditability-admin"
            listClassName="workspace-auditability-list"
            cardClassName="workspace-auditability-card"
            role={props.auditabilityAdminSummary.role}
            guidance={props.auditabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Usage auditability',
              value: `${props.auditabilityAdminSummary.stats.auditabilityPercent}%`,
              detail: <>{props.auditabilityAdminSummary.stats.coveredDomains}/
                  {props.auditabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Auditability signals',
              value: props.auditabilityAdminSummary.stats.totalRecords,
              detail: <>{props.auditabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, usage events, and webhook events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.auditabilityAdminSummary.records}
            availableActions={props.auditabilityAdminSummary.availableActions}
            refreshAction="refresh_auditability_summary"
            actionBusy={props.auditabilityAdminAction !== 'idle'}
            formatDomain={formatAuditabilityDomain as (domain: string) => string}
            formatAdminAction={formatAuditabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleAuditabilityAdminAction('refresh_auditability_summary')
            }
          />
        ) : null}
        {props.defensibilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Defensibility admin"
            panelClassName="workspace-defensibility-admin"
            listClassName="workspace-defensibility-list"
            cardClassName="workspace-defensibility-card"
            role={props.defensibilityAdminSummary.role}
            guidance={props.defensibilityAdminSummary.guidance}
            stats={[
            {
              label: 'Shield review defensibility',
              value: `${props.defensibilityAdminSummary.stats.defensibilityPercent}%`,
              detail: <>{props.defensibilityAdminSummary.stats.coveredDomains}/
                  {props.defensibilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Defensibility signals',
              value: props.defensibilityAdminSummary.stats.totalRecords,
              detail: <>{props.defensibilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, shield reviews, and artifacts'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.defensibilityAdminSummary.records}
            availableActions={props.defensibilityAdminSummary.availableActions}
            refreshAction="refresh_defensibility_summary"
            actionBusy={props.defensibilityAdminAction !== 'idle'}
            formatDomain={formatDefensibilityDomain as (domain: string) => string}
            formatAdminAction={formatDefensibilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleDefensibilityAdminAction('refresh_defensibility_summary')
            }
          />
        ) : null}
        {props.reproducibilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Reproducibility admin"
            panelClassName="workspace-reproducibility-admin"
            listClassName="workspace-reproducibility-list"
            cardClassName="workspace-reproducibility-card"
            role={props.reproducibilityAdminSummary.role}
            guidance={props.reproducibilityAdminSummary.guidance}
            stats={[
            {
              label: 'Idempotency reproducibility',
              value: `${props.reproducibilityAdminSummary.stats.reproducibilityPercent}%`,
              detail: <>{props.reproducibilityAdminSummary.stats.coveredDomains}/
                  {props.reproducibilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Reproducibility signals',
              value: props.reproducibilityAdminSummary.stats.totalRecords,
              detail: <>{props.reproducibilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, idempotency keys, and run workflows'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.reproducibilityAdminSummary.records}
            availableActions={props.reproducibilityAdminSummary.availableActions}
            refreshAction="refresh_reproducibility_summary"
            actionBusy={props.reproducibilityAdminAction !== 'idle'}
            formatDomain={formatReproducibilityDomain as (domain: string) => string}
            formatAdminAction={formatReproducibilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleReproducibilityAdminAction('refresh_reproducibility_summary')
            }
          />
        ) : null}
        {props.credibilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Credibility admin"
            panelClassName="workspace-credibility-admin"
            listClassName="workspace-credibility-list"
            cardClassName="workspace-credibility-card"
            role={props.credibilityAdminSummary.role}
            guidance={props.credibilityAdminSummary.guidance}
            stats={[
            {
              label: 'Billing invoice credibility',
              value: `${props.credibilityAdminSummary.stats.credibilityPercent}%`,
              detail: <>{props.credibilityAdminSummary.stats.coveredDomains}/
                  {props.credibilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Credibility signals',
              value: props.credibilityAdminSummary.stats.totalRecords,
              detail: <>{props.credibilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, billing invoices, and billing records'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.credibilityAdminSummary.records}
            availableActions={props.credibilityAdminSummary.availableActions}
            refreshAction="refresh_credibility_summary"
            actionBusy={props.credibilityAdminAction !== 'idle'}
            formatDomain={formatCredibilityDomain as (domain: string) => string}
            formatAdminAction={formatCredibilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleCredibilityAdminAction('refresh_credibility_summary')
            }
          />
        ) : null}
        {props.validityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Validity admin"
            panelClassName="workspace-validity-admin"
            listClassName="workspace-validity-list"
            cardClassName="workspace-validity-card"
            role={props.validityAdminSummary.role}
            guidance={props.validityAdminSummary.guidance}
            stats={[
            {
              label: 'Agent output validity',
              value: `${props.validityAdminSummary.stats.validityPercent}%`,
              detail: <>{props.validityAdminSummary.stats.coveredDomains}/
                  {props.validityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Validity signals',
              value: props.validityAdminSummary.stats.totalRecords,
              detail: <>{props.validityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, agent outputs, and artifacts'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.validityAdminSummary.records}
            availableActions={props.validityAdminSummary.availableActions}
            refreshAction="refresh_validity_summary"
            actionBusy={props.validityAdminAction !== 'idle'}
            formatDomain={formatValidityDomain as (domain: string) => string}
            formatAdminAction={formatValidityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleValidityAdminAction('refresh_validity_summary')
            }
          />
        ) : null}
        {props.reviewabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Reviewability admin"
            panelClassName="workspace-reviewability-admin"
            listClassName="workspace-reviewability-list"
            cardClassName="workspace-reviewability-card"
            role={props.reviewabilityAdminSummary.role}
            guidance={props.reviewabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Artifact reviewability',
              value: `${props.reviewabilityAdminSummary.stats.reviewabilityPercent}%`,
              detail: <>{props.reviewabilityAdminSummary.stats.coveredDomains}/
                  {props.reviewabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Reviewability signals',
              value: props.reviewabilityAdminSummary.stats.totalRecords,
              detail: <>{props.reviewabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, artifacts, and billing invoices'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.reviewabilityAdminSummary.records}
            availableActions={props.reviewabilityAdminSummary.availableActions}
            refreshAction="refresh_reviewability_summary"
            actionBusy={props.reviewabilityAdminAction !== 'idle'}
            formatDomain={formatReviewabilityDomain as (domain: string) => string}
            formatAdminAction={formatReviewabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleReviewabilityAdminAction('refresh_reviewability_summary')
            }
          />
        ) : null}
        {props.justifiabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Justifiability admin"
            panelClassName="workspace-justifiability-admin"
            listClassName="workspace-justifiability-list"
            cardClassName="workspace-justifiability-card"
            role={props.justifiabilityAdminSummary.role}
            guidance={props.justifiabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Shield review justifiability',
              value: `${props.justifiabilityAdminSummary.stats.justifiabilityPercent}%`,
              detail: <>{props.justifiabilityAdminSummary.stats.coveredDomains}/
                  {props.justifiabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Justifiability signals',
              value: props.justifiabilityAdminSummary.stats.totalRecords,
              detail: <>{props.justifiabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, shield reviews, and idempotency keys'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.justifiabilityAdminSummary.records}
            availableActions={props.justifiabilityAdminSummary.availableActions}
            refreshAction="refresh_justifiability_summary"
            actionBusy={props.justifiabilityAdminAction !== 'idle'}
            formatDomain={formatJustifiabilityDomain as (domain: string) => string}
            formatAdminAction={formatJustifiabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleJustifiabilityAdminAction('refresh_justifiability_summary')
            }
          />
        ) : null}
        {props.demonstrabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Demonstrability admin"
            panelClassName="workspace-demonstrability-admin"
            listClassName="workspace-demonstrability-list"
            cardClassName="workspace-demonstrability-card"
            role={props.demonstrabilityAdminSummary.role}
            guidance={props.demonstrabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Workflow demonstrability',
              value: `${props.demonstrabilityAdminSummary.stats.demonstrabilityPercent}%`,
              detail: <>{props.demonstrabilityAdminSummary.stats.coveredDomains}/
                  {props.demonstrabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Demonstrability signals',
              value: props.demonstrabilityAdminSummary.stats.totalRecords,
              detail: <>{props.demonstrabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, run workflows, and billing notifications'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.demonstrabilityAdminSummary.records}
            availableActions={props.demonstrabilityAdminSummary.availableActions}
            refreshAction="refresh_demonstrability_summary"
            actionBusy={props.demonstrabilityAdminAction !== 'idle'}
            formatDomain={formatDemonstrabilityDomain as (domain: string) => string}
            formatAdminAction={formatDemonstrabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleDemonstrabilityAdminAction('refresh_demonstrability_summary')
            }
          />
        ) : null}
        {props.explainabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Explainability admin"
            panelClassName="workspace-explainability-admin"
            listClassName="workspace-explainability-list"
            cardClassName="workspace-explainability-card"
            role={props.explainabilityAdminSummary.role}
            guidance={props.explainabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Moderator synthesis explainability',
              value: `${props.explainabilityAdminSummary.stats.explainabilityPercent}%`,
              detail: <>{props.explainabilityAdminSummary.stats.coveredDomains}/
                  {props.explainabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Explainability signals',
              value: props.explainabilityAdminSummary.stats.totalRecords,
              detail: <>{props.explainabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, moderator syntheses, and artifacts'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.explainabilityAdminSummary.records}
            availableActions={props.explainabilityAdminSummary.availableActions}
            refreshAction="refresh_explainability_summary"
            actionBusy={props.explainabilityAdminAction !== 'idle'}
            formatDomain={formatExplainabilityDomain as (domain: string) => string}
            formatAdminAction={formatExplainabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleExplainabilityAdminAction('refresh_explainability_summary')
            }
          />
        ) : null}
        {props.inspectabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Inspectability admin"
            panelClassName="workspace-inspectability-admin"
            listClassName="workspace-inspectability-list"
            cardClassName="workspace-inspectability-card"
            role={props.inspectabilityAdminSummary.role}
            guidance={props.inspectabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Usage inspectability',
              value: `${props.inspectabilityAdminSummary.stats.inspectabilityPercent}%`,
              detail: <>{props.inspectabilityAdminSummary.stats.coveredDomains}/
                  {props.inspectabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Inspectability signals',
              value: props.inspectabilityAdminSummary.stats.totalRecords,
              detail: <>{props.inspectabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, usage events, and meter usage reports'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.inspectabilityAdminSummary.records}
            availableActions={props.inspectabilityAdminSummary.availableActions}
            refreshAction="refresh_inspectability_summary"
            actionBusy={props.inspectabilityAdminAction !== 'idle'}
            formatDomain={formatInspectabilityDomain as (domain: string) => string}
            formatAdminAction={formatInspectabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleInspectabilityAdminAction('refresh_inspectability_summary')
            }
          />
        ) : null}
        {props.warrantabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Warrantability admin"
            panelClassName="workspace-warrantability-admin"
            listClassName="workspace-warrantability-list"
            cardClassName="workspace-warrantability-card"
            role={props.warrantabilityAdminSummary.role}
            guidance={props.warrantabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Shield scan warrantability',
              value: `${props.warrantabilityAdminSummary.stats.warrantabilityPercent}%`,
              detail: <>{props.warrantabilityAdminSummary.stats.coveredDomains}/
                  {props.warrantabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Warrantability signals',
              value: props.warrantabilityAdminSummary.stats.totalRecords,
              detail: <>{props.warrantabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, shield scans, and run workflows'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.warrantabilityAdminSummary.records}
            availableActions={props.warrantabilityAdminSummary.availableActions}
            refreshAction="refresh_warrantability_summary"
            actionBusy={props.warrantabilityAdminAction !== 'idle'}
            formatDomain={formatWarrantabilityDomain as (domain: string) => string}
            formatAdminAction={formatWarrantabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleWarrantabilityAdminAction('refresh_warrantability_summary')
            }
          />
        ) : null}
        {props.substantiabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Substantiability admin"
            panelClassName="workspace-substantiability-admin"
            listClassName="workspace-substantiability-list"
            cardClassName="workspace-substantiability-card"
            role={props.substantiabilityAdminSummary.role}
            guidance={props.substantiabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Billing record substantiability',
              value: `${props.substantiabilityAdminSummary.stats.substantiabilityPercent}%`,
              detail: <>{props.substantiabilityAdminSummary.stats.coveredDomains}/
                  {props.substantiabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Substantiability signals',
              value: props.substantiabilityAdminSummary.stats.totalRecords,
              detail: <>{props.substantiabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, billing records, and idempotency keys'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.substantiabilityAdminSummary.records}
            availableActions={props.substantiabilityAdminSummary.availableActions}
            refreshAction="refresh_substantiability_summary"
            actionBusy={props.substantiabilityAdminAction !== 'idle'}
            formatDomain={formatSubstantiabilityDomain as (domain: string) => string}
            formatAdminAction={formatSubstantiabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleSubstantiabilityAdminAction('refresh_substantiability_summary')
            }
          />
        ) : null}
        {props.certifiabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Certifiability admin"
            panelClassName="workspace-certifiability-admin"
            listClassName="workspace-certifiability-list"
            cardClassName="workspace-certifiability-card"
            role={props.certifiabilityAdminSummary.role}
            guidance={props.certifiabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Provider credential certifiability',
              value: `${props.certifiabilityAdminSummary.stats.certifiabilityPercent}%`,
              detail: <>{props.certifiabilityAdminSummary.stats.coveredDomains}/
                  {props.certifiabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Certifiability signals',
              value: props.certifiabilityAdminSummary.stats.totalRecords,
              detail: <>{props.certifiabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, provider credentials, and billing webhook events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.certifiabilityAdminSummary.records}
            availableActions={props.certifiabilityAdminSummary.availableActions}
            refreshAction="refresh_certifiability_summary"
            actionBusy={props.certifiabilityAdminAction !== 'idle'}
            formatDomain={formatCertifiabilityDomain as (domain: string) => string}
            formatAdminAction={formatCertifiabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleCertifiabilityAdminAction('refresh_certifiability_summary')
            }
          />
        ) : null}
        {props.measurabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Measurability admin"
            panelClassName="workspace-measurability-admin"
            listClassName="workspace-measurability-list"
            cardClassName="workspace-measurability-card"
            role={props.measurabilityAdminSummary.role}
            guidance={props.measurabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Meter usage measurability',
              value: `${props.measurabilityAdminSummary.stats.measurabilityPercent}%`,
              detail: <>{props.measurabilityAdminSummary.stats.coveredDomains}/
                  {props.measurabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Measurability signals',
              value: props.measurabilityAdminSummary.stats.totalRecords,
              detail: <>{props.measurabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, meter usage reports, and usage events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.measurabilityAdminSummary.records}
            availableActions={props.measurabilityAdminSummary.availableActions}
            refreshAction="refresh_measurability_summary"
            actionBusy={props.measurabilityAdminAction !== 'idle'}
            formatDomain={formatMeasurabilityDomain as (domain: string) => string}
            formatAdminAction={formatMeasurabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleMeasurabilityAdminAction('refresh_measurability_summary')
            }
          />
        ) : null}
        {props.assessabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Assessability admin"
            panelClassName="workspace-assessability-admin"
            listClassName="workspace-assessability-list"
            cardClassName="workspace-assessability-card"
            role={props.assessabilityAdminSummary.role}
            guidance={props.assessabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Model health assessability',
              value: `${props.assessabilityAdminSummary.stats.assessabilityPercent}%`,
              detail: <>{props.assessabilityAdminSummary.stats.coveredDomains}/
                  {props.assessabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Assessability signals',
              value: props.assessabilityAdminSummary.stats.totalRecords,
              detail: <>{props.assessabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, model health events, and billing records'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.assessabilityAdminSummary.records}
            availableActions={props.assessabilityAdminSummary.availableActions}
            refreshAction="refresh_assessability_summary"
            actionBusy={props.assessabilityAdminAction !== 'idle'}
            formatDomain={formatAssessabilityDomain as (domain: string) => string}
            formatAdminAction={formatAssessabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleAssessabilityAdminAction('refresh_assessability_summary')
            }
          />
        ) : null}
        {props.assignabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Assignability admin"
            panelClassName="workspace-assignability-admin"
            listClassName="workspace-assignability-list"
            cardClassName="workspace-assignability-card"
            role={props.assignabilityAdminSummary.role}
            guidance={props.assignabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Workspace membership assignability',
              value: `${props.assignabilityAdminSummary.stats.assignabilityPercent}%`,
              detail: <>{props.assignabilityAdminSummary.stats.coveredDomains}/
                  {props.assignabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Assignability signals',
              value: props.assignabilityAdminSummary.stats.totalRecords,
              detail: <>{props.assignabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, workspace memberships, and billing notifications'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.assignabilityAdminSummary.records}
            availableActions={props.assignabilityAdminSummary.availableActions}
            refreshAction="refresh_assignability_summary"
            actionBusy={props.assignabilityAdminAction !== 'idle'}
            formatDomain={formatAssignabilityDomain as (domain: string) => string}
            formatAdminAction={formatAssignabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleAssignabilityAdminAction('refresh_assignability_summary')
            }
          />
        ) : null}
        {props.distinguishabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Distinguishability admin"
            panelClassName="workspace-distinguishability-admin"
            listClassName="workspace-distinguishability-list"
            cardClassName="workspace-distinguishability-card"
            role={props.distinguishabilityAdminSummary.role}
            guidance={props.distinguishabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Moderator synthesis distinguishability',
              value: `${props.distinguishabilityAdminSummary.stats.distinguishabilityPercent}%`,
              detail: <>{props.distinguishabilityAdminSummary.stats.coveredDomains}/
                  {props.distinguishabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Distinguishability signals',
              value: props.distinguishabilityAdminSummary.stats.totalRecords,
              detail: <>{props.distinguishabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, moderator syntheses, and run workflows'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.distinguishabilityAdminSummary.records}
            availableActions={props.distinguishabilityAdminSummary.availableActions}
            refreshAction="refresh_distinguishability_summary"
            actionBusy={props.distinguishabilityAdminAction !== 'idle'}
            formatDomain={formatDistinguishabilityDomain as (domain: string) => string}
            formatAdminAction={formatDistinguishabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleDistinguishabilityAdminAction('refresh_distinguishability_summary')
            }
          />
        ) : null}
        {props.comparabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Comparability admin"
            panelClassName="workspace-comparability-admin"
            listClassName="workspace-comparability-list"
            cardClassName="workspace-comparability-card"
            role={props.comparabilityAdminSummary.role}
            guidance={props.comparabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Billing invoice comparability',
              value: `${props.comparabilityAdminSummary.stats.comparabilityPercent}%`,
              detail: <>{props.comparabilityAdminSummary.stats.coveredDomains}/
                  {props.comparabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Comparability signals',
              value: props.comparabilityAdminSummary.stats.totalRecords,
              detail: <>{props.comparabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, billing invoices, and meter usage reports'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.comparabilityAdminSummary.records}
            availableActions={props.comparabilityAdminSummary.availableActions}
            refreshAction="refresh_comparability_summary"
            actionBusy={props.comparabilityAdminAction !== 'idle'}
            formatDomain={formatComparabilityDomain as (domain: string) => string}
            formatAdminAction={formatComparabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleComparabilityAdminAction('refresh_comparability_summary')
            }
          />
        ) : null}
        {props.identifiabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Identifiability admin"
            panelClassName="workspace-identifiability-admin"
            listClassName="workspace-identifiability-list"
            cardClassName="workspace-identifiability-card"
            role={props.identifiabilityAdminSummary.role}
            guidance={props.identifiabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Idempotency key identifiability',
              value: `${props.identifiabilityAdminSummary.stats.identifiabilityPercent}%`,
              detail: <>{props.identifiabilityAdminSummary.stats.coveredDomains}/
                  {props.identifiabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Identifiability signals',
              value: props.identifiabilityAdminSummary.stats.totalRecords,
              detail: <>{props.identifiabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, idempotency keys, and usage events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.identifiabilityAdminSummary.records}
            availableActions={props.identifiabilityAdminSummary.availableActions}
            refreshAction="refresh_identifiability_summary"
            actionBusy={props.identifiabilityAdminAction !== 'idle'}
            formatDomain={formatIdentifiabilityDomain as (domain: string) => string}
            formatAdminAction={formatIdentifiabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleIdentifiabilityAdminAction('refresh_identifiability_summary')
            }
          />
        ) : null}
        {props.attributabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Attributability admin"
            panelClassName="workspace-attributability-admin"
            listClassName="workspace-attributability-list"
            cardClassName="workspace-attributability-card"
            role={props.attributabilityAdminSummary.role}
            guidance={props.attributabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Agent output attributability',
              value: `${props.attributabilityAdminSummary.stats.attributabilityPercent}%`,
              detail: <>{props.attributabilityAdminSummary.stats.coveredDomains}/
                  {props.attributabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Attributability signals',
              value: props.attributabilityAdminSummary.stats.totalRecords,
              detail: <>{props.attributabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, agent outputs, and artifacts'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.attributabilityAdminSummary.records}
            availableActions={props.attributabilityAdminSummary.availableActions}
            refreshAction="refresh_attributability_summary"
            actionBusy={props.attributabilityAdminAction !== 'idle'}
            formatDomain={formatAttributabilityDomain as (domain: string) => string}
            formatAdminAction={formatAttributabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleAttributabilityAdminAction('refresh_attributability_summary')
            }
          />
        ) : null}
        {props.navigabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Navigability admin"
            panelClassName="workspace-navigability-admin"
            listClassName="workspace-navigability-list"
            cardClassName="workspace-navigability-card"
            role={props.navigabilityAdminSummary.role}
            guidance={props.navigabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Workflow navigability',
              value: `${props.navigabilityAdminSummary.stats.navigabilityPercent}%`,
              detail: <>{props.navigabilityAdminSummary.stats.coveredDomains}/
                  {props.navigabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Navigability signals',
              value: props.navigabilityAdminSummary.stats.totalRecords,
              detail: <>{props.navigabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, run workflows, and billing invoices'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.navigabilityAdminSummary.records}
            availableActions={props.navigabilityAdminSummary.availableActions}
            refreshAction="refresh_navigability_summary"
            actionBusy={props.navigabilityAdminAction !== 'idle'}
            formatDomain={formatNavigabilityDomain as (domain: string) => string}
            formatAdminAction={formatNavigabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleNavigabilityAdminAction('refresh_navigability_summary')
            }
          />
        ) : null}
        {props.discoverabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Discoverability admin"
            panelClassName="workspace-discoverability-admin"
            listClassName="workspace-discoverability-list"
            cardClassName="workspace-discoverability-card"
            role={props.discoverabilityAdminSummary.role}
            guidance={props.discoverabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Meter usage discoverability',
              value: `${props.discoverabilityAdminSummary.stats.discoverabilityPercent}%`,
              detail: <>{props.discoverabilityAdminSummary.stats.coveredDomains}/
                  {props.discoverabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Discoverability signals',
              value: props.discoverabilityAdminSummary.stats.totalRecords,
              detail: <>{props.discoverabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, meter usage reports, and billing notifications'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.discoverabilityAdminSummary.records}
            availableActions={props.discoverabilityAdminSummary.availableActions}
            refreshAction="refresh_discoverability_summary"
            actionBusy={props.discoverabilityAdminAction !== 'idle'}
            formatDomain={formatDiscoverabilityDomain as (domain: string) => string}
            formatAdminAction={formatDiscoverabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleDiscoverabilityAdminAction('refresh_discoverability_summary')
            }
          />
        ) : null}
        {props.retrievabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Retrievability admin"
            panelClassName="workspace-retrievability-admin"
            listClassName="workspace-retrievability-list"
            cardClassName="workspace-retrievability-card"
            role={props.retrievabilityAdminSummary.role}
            guidance={props.retrievabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Shield scan retrievability',
              value: `${props.retrievabilityAdminSummary.stats.retrievabilityPercent}%`,
              detail: <>{props.retrievabilityAdminSummary.stats.coveredDomains}/
                  {props.retrievabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Retrievability signals',
              value: props.retrievabilityAdminSummary.stats.totalRecords,
              detail: <>{props.retrievabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, shield scans, and idempotency keys'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.retrievabilityAdminSummary.records}
            availableActions={props.retrievabilityAdminSummary.availableActions}
            refreshAction="refresh_retrievability_summary"
            actionBusy={props.retrievabilityAdminAction !== 'idle'}
            formatDomain={formatRetrievabilityDomain as (domain: string) => string}
            formatAdminAction={formatRetrievabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleRetrievabilityAdminAction('refresh_retrievability_summary')
            }
          />
        ) : null}
        {props.locatabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Locatability admin"
            panelClassName="workspace-locatability-admin"
            listClassName="workspace-locatability-list"
            cardClassName="workspace-locatability-card"
            role={props.locatabilityAdminSummary.role}
            guidance={props.locatabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Provider credential locatability',
              value: `${props.locatabilityAdminSummary.stats.locatabilityPercent}%`,
              detail: <>{props.locatabilityAdminSummary.stats.coveredDomains}/
                  {props.locatabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Locatability signals',
              value: props.locatabilityAdminSummary.stats.totalRecords,
              detail: <>{props.locatabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, provider credentials, and usage events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.locatabilityAdminSummary.records}
            availableActions={props.locatabilityAdminSummary.availableActions}
            refreshAction="refresh_locatability_summary"
            actionBusy={props.locatabilityAdminAction !== 'idle'}
            formatDomain={formatLocatabilityDomain as (domain: string) => string}
            formatAdminAction={formatLocatabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleLocatabilityAdminAction('refresh_locatability_summary')
            }
          />
        ) : null}
        {props.referencabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Referencability admin"
            panelClassName="workspace-referencability-admin"
            listClassName="workspace-referencability-list"
            cardClassName="workspace-referencability-card"
            role={props.referencabilityAdminSummary.role}
            guidance={props.referencabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Artifact referencability',
              value: `${props.referencabilityAdminSummary.stats.referencabilityPercent}%`,
              detail: <>{props.referencabilityAdminSummary.stats.coveredDomains}/
                  {props.referencabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Referencability signals',
              value: props.referencabilityAdminSummary.stats.totalRecords,
              detail: <>{props.referencabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, artifacts, and billing records'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.referencabilityAdminSummary.records}
            availableActions={props.referencabilityAdminSummary.availableActions}
            refreshAction="refresh_referencability_summary"
            actionBusy={props.referencabilityAdminAction !== 'idle'}
            formatDomain={formatReferencabilityDomain as (domain: string) => string}
            formatAdminAction={formatReferencabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleReferencabilityAdminAction('refresh_referencability_summary')
            }
          />
        ) : null}
        {props.portabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Portability admin"
            panelClassName="workspace-portability-admin"
            listClassName="workspace-portability-list"
            cardClassName="workspace-portability-card"
            role={props.portabilityAdminSummary.role}
            guidance={props.portabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Artifact portability',
              value: `${props.portabilityAdminSummary.stats.portabilityPercent}%`,
              detail: <>{props.portabilityAdminSummary.stats.coveredDomains}/
                  {props.portabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Portability signals',
              value: props.portabilityAdminSummary.stats.totalRecords,
              detail: <>{props.portabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, artifacts, and agent outputs'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.portabilityAdminSummary.records}
            availableActions={props.portabilityAdminSummary.availableActions}
            refreshAction="refresh_portability_summary"
            actionBusy={props.portabilityAdminAction !== 'idle'}
            formatDomain={formatPortabilityDomain as (domain: string) => string}
            formatAdminAction={formatPortabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handlePortabilityAdminAction('refresh_portability_summary')
            }
          />
        ) : null}
        {props.transferabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Transferability admin"
            panelClassName="workspace-transferability-admin"
            listClassName="workspace-transferability-list"
            cardClassName="workspace-transferability-card"
            role={props.transferabilityAdminSummary.role}
            guidance={props.transferabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Billing record transferability',
              value: `${props.transferabilityAdminSummary.stats.transferabilityPercent}%`,
              detail: <>{props.transferabilityAdminSummary.stats.coveredDomains}/
                  {props.transferabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Transferability signals',
              value: props.transferabilityAdminSummary.stats.totalRecords,
              detail: <>{props.transferabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, billing records, and billing invoices'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.transferabilityAdminSummary.records}
            availableActions={props.transferabilityAdminSummary.availableActions}
            refreshAction="refresh_transferability_summary"
            actionBusy={props.transferabilityAdminAction !== 'idle'}
            formatDomain={formatTransferabilityDomain as (domain: string) => string}
            formatAdminAction={formatTransferabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleTransferabilityAdminAction('refresh_transferability_summary')
            }
          />
        ) : null}
        {props.interchangeabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Interchangeability admin"
            panelClassName="workspace-interchangeability-admin"
            listClassName="workspace-interchangeability-list"
            cardClassName="workspace-interchangeability-card"
            role={props.interchangeabilityAdminSummary.role}
            guidance={props.interchangeabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Meter usage interchangeability',
              value: `${props.interchangeabilityAdminSummary.stats.interchangeabilityPercent}%`,
              detail: <>{props.interchangeabilityAdminSummary.stats.coveredDomains}/
                  {props.interchangeabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Interchangeability signals',
              value: props.interchangeabilityAdminSummary.stats.totalRecords,
              detail: <>{props.interchangeabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, meter usage reports, and idempotency keys'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.interchangeabilityAdminSummary.records}
            availableActions={props.interchangeabilityAdminSummary.availableActions}
            refreshAction="refresh_interchangeability_summary"
            actionBusy={props.interchangeabilityAdminAction !== 'idle'}
            formatDomain={formatInterchangeabilityDomain as (domain: string) => string}
            formatAdminAction={formatInterchangeabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleInterchangeabilityAdminAction('refresh_interchangeability_summary')
            }
          />
        ) : null}
        {props.linkabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Linkability admin"
            panelClassName="workspace-linkability-admin"
            listClassName="workspace-linkability-list"
            cardClassName="workspace-linkability-card"
            role={props.linkabilityAdminSummary.role}
            guidance={props.linkabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Workflow linkability',
              value: `${props.linkabilityAdminSummary.stats.linkabilityPercent}%`,
              detail: <>{props.linkabilityAdminSummary.stats.coveredDomains}/
                  {props.linkabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Linkability signals',
              value: props.linkabilityAdminSummary.stats.totalRecords,
              detail: <>{props.linkabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, run workflows, and artifacts'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.linkabilityAdminSummary.records}
            availableActions={props.linkabilityAdminSummary.availableActions}
            refreshAction="refresh_linkability_summary"
            actionBusy={props.linkabilityAdminAction !== 'idle'}
            formatDomain={formatLinkabilityDomain as (domain: string) => string}
            formatAdminAction={formatLinkabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleLinkabilityAdminAction('refresh_linkability_summary')
            }
          />
        ) : null}
        {props.connectabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Connectability admin"
            panelClassName="workspace-connectability-admin"
            listClassName="workspace-connectability-list"
            cardClassName="workspace-connectability-card"
            role={props.connectabilityAdminSummary.role}
            guidance={props.connectabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Usage event connectability',
              value: `${props.connectabilityAdminSummary.stats.connectabilityPercent}%`,
              detail: <>{props.connectabilityAdminSummary.stats.coveredDomains}/
                  {props.connectabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Connectability signals',
              value: props.connectabilityAdminSummary.stats.totalRecords,
              detail: <>{props.connectabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, usage events, and billing webhook events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.connectabilityAdminSummary.records}
            availableActions={props.connectabilityAdminSummary.availableActions}
            refreshAction="refresh_connectability_summary"
            actionBusy={props.connectabilityAdminAction !== 'idle'}
            formatDomain={formatConnectabilityDomain as (domain: string) => string}
            formatAdminAction={formatConnectabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleConnectabilityAdminAction('refresh_connectability_summary')
            }
          />
        ) : null}
        {props.modifiabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Modifiability admin"
            panelClassName="workspace-modifiability-admin"
            listClassName="workspace-modifiability-list"
            cardClassName="workspace-modifiability-card"
            role={props.modifiabilityAdminSummary.role}
            guidance={props.modifiabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Idempotency key modifiability',
              value: `${props.modifiabilityAdminSummary.stats.modifiabilityPercent}%`,
              detail: <>{props.modifiabilityAdminSummary.stats.coveredDomains}/
                  {props.modifiabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Modifiability signals',
              value: props.modifiabilityAdminSummary.stats.totalRecords,
              detail: <>{props.modifiabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, idempotency keys, and workspace memberships'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.modifiabilityAdminSummary.records}
            availableActions={props.modifiabilityAdminSummary.availableActions}
            refreshAction="refresh_modifiability_summary"
            actionBusy={props.modifiabilityAdminAction !== 'idle'}
            formatDomain={formatModifiabilityDomain as (domain: string) => string}
            formatAdminAction={formatModifiabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleModifiabilityAdminAction('refresh_modifiability_summary')
            }
          />
        ) : null}
        {props.extensibilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Extensibility admin"
            panelClassName="workspace-extensibility-admin"
            listClassName="workspace-extensibility-list"
            cardClassName="workspace-extensibility-card"
            role={props.extensibilityAdminSummary.role}
            guidance={props.extensibilityAdminSummary.guidance}
            stats={[
            {
              label: 'Agent output extensibility',
              value: `${props.extensibilityAdminSummary.stats.extensibilityPercent}%`,
              detail: <>{props.extensibilityAdminSummary.stats.coveredDomains}/
                  {props.extensibilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Extensibility signals',
              value: props.extensibilityAdminSummary.stats.totalRecords,
              detail: <>{props.extensibilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, agent outputs, and artifacts'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.extensibilityAdminSummary.records}
            availableActions={props.extensibilityAdminSummary.availableActions}
            refreshAction="refresh_extensibility_summary"
            actionBusy={props.extensibilityAdminAction !== 'idle'}
            formatDomain={formatExtensibilityDomain as (domain: string) => string}
            formatAdminAction={formatExtensibilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleExtensibilityAdminAction('refresh_extensibility_summary')
            }
          />
        ) : null}
        {props.flexibilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Flexibility admin"
            panelClassName="workspace-flexibility-admin"
            listClassName="workspace-flexibility-list"
            cardClassName="workspace-flexibility-card"
            role={props.flexibilityAdminSummary.role}
            guidance={props.flexibilityAdminSummary.guidance}
            stats={[
            {
              label: 'Workflow flexibility',
              value: `${props.flexibilityAdminSummary.stats.flexibilityPercent}%`,
              detail: <>{props.flexibilityAdminSummary.stats.coveredDomains}/
                  {props.flexibilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Flexibility signals',
              value: props.flexibilityAdminSummary.stats.totalRecords,
              detail: <>{props.flexibilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, run workflows, and usage events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.flexibilityAdminSummary.records}
            availableActions={props.flexibilityAdminSummary.availableActions}
            refreshAction="refresh_flexibility_summary"
            actionBusy={props.flexibilityAdminAction !== 'idle'}
            formatDomain={formatFlexibilityDomain as (domain: string) => string}
            formatAdminAction={formatFlexibilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleFlexibilityAdminAction('refresh_flexibility_summary')
            }
          />
        ) : null}
        {props.adaptabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Adaptability admin"
            panelClassName="workspace-adaptability-admin"
            listClassName="workspace-adaptability-list"
            cardClassName="workspace-adaptability-card"
            role={props.adaptabilityAdminSummary.role}
            guidance={props.adaptabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Billing webhook adaptability',
              value: `${props.adaptabilityAdminSummary.stats.adaptabilityPercent}%`,
              detail: <>{props.adaptabilityAdminSummary.stats.coveredDomains}/
                  {props.adaptabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Adaptability signals',
              value: props.adaptabilityAdminSummary.stats.totalRecords,
              detail: <>{props.adaptabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, billing webhook events, and billing notifications'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.adaptabilityAdminSummary.records}
            availableActions={props.adaptabilityAdminSummary.availableActions}
            refreshAction="refresh_adaptability_summary"
            actionBusy={props.adaptabilityAdminAction !== 'idle'}
            formatDomain={formatAdaptabilityDomain as (domain: string) => string}
            formatAdminAction={formatAdaptabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleAdaptabilityAdminAction('refresh_adaptability_summary')
            }
          />
        ) : null}
        {props.compatibilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Compatibility admin"
            panelClassName="workspace-compatibility-admin"
            listClassName="workspace-compatibility-list"
            cardClassName="workspace-compatibility-card"
            role={props.compatibilityAdminSummary.role}
            guidance={props.compatibilityAdminSummary.guidance}
            stats={[
            {
              label: 'Provider credential compatibility',
              value: `${props.compatibilityAdminSummary.stats.compatibilityPercent}%`,
              detail: <>{props.compatibilityAdminSummary.stats.coveredDomains}/
                  {props.compatibilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Compatibility signals',
              value: props.compatibilityAdminSummary.stats.totalRecords,
              detail: <>{props.compatibilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, provider credentials, and billing records'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.compatibilityAdminSummary.records}
            availableActions={props.compatibilityAdminSummary.availableActions}
            refreshAction="refresh_compatibility_summary"
            actionBusy={props.compatibilityAdminAction !== 'idle'}
            formatDomain={formatCompatibilityDomain as (domain: string) => string}
            formatAdminAction={formatCompatibilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleCompatibilityAdminAction('refresh_compatibility_summary')
            }
          />
        ) : null}
        {props.adjustabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Adjustability admin"
            panelClassName="workspace-adjustability-admin"
            listClassName="workspace-adjustability-list"
            cardClassName="workspace-adjustability-card"
            role={props.adjustabilityAdminSummary.role}
            guidance={props.adjustabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Billing invoice adjustability',
              value: `${props.adjustabilityAdminSummary.stats.adjustabilityPercent}%`,
              detail: <>{props.adjustabilityAdminSummary.stats.coveredDomains}/
                  {props.adjustabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Adjustability signals',
              value: props.adjustabilityAdminSummary.stats.totalRecords,
              detail: <>{props.adjustabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, billing invoices, and workspace memberships'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.adjustabilityAdminSummary.records}
            availableActions={props.adjustabilityAdminSummary.availableActions}
            refreshAction="refresh_adjustability_summary"
            actionBusy={props.adjustabilityAdminAction !== 'idle'}
            formatDomain={formatAdjustabilityDomain as (domain: string) => string}
            formatAdminAction={formatAdjustabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleAdjustabilityAdminAction('refresh_adjustability_summary')
            }
          />
        ) : null}
        {props.tunabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Tunability admin"
            panelClassName="workspace-tunability-admin"
            listClassName="workspace-tunability-list"
            cardClassName="workspace-tunability-card"
            role={props.tunabilityAdminSummary.role}
            guidance={props.tunabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Usage event tunability',
              value: `${props.tunabilityAdminSummary.stats.tunabilityPercent}%`,
              detail: <>{props.tunabilityAdminSummary.stats.coveredDomains}/
                  {props.tunabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Tunability signals',
              value: props.tunabilityAdminSummary.stats.totalRecords,
              detail: <>{props.tunabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, usage events, and idempotency keys'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.tunabilityAdminSummary.records}
            availableActions={props.tunabilityAdminSummary.availableActions}
            refreshAction="refresh_tunability_summary"
            actionBusy={props.tunabilityAdminAction !== 'idle'}
            formatDomain={formatTunabilityDomain as (domain: string) => string}
            formatAdminAction={formatTunabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleTunabilityAdminAction('refresh_tunability_summary')
            }
          />
        ) : null}
        {props.operabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Operability admin"
            panelClassName="workspace-operability-admin"
            listClassName="workspace-operability-list"
            cardClassName="workspace-operability-card"
            role={props.operabilityAdminSummary.role}
            guidance={props.operabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Billing notification operability',
              value: `${props.operabilityAdminSummary.stats.operabilityPercent}%`,
              detail: <>{props.operabilityAdminSummary.stats.coveredDomains}/
                  {props.operabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Operability signals',
              value: props.operabilityAdminSummary.stats.totalRecords,
              detail: <>{props.operabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, billing notifications, and billing records'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.operabilityAdminSummary.records}
            availableActions={props.operabilityAdminSummary.availableActions}
            refreshAction="refresh_operability_summary"
            actionBusy={props.operabilityAdminAction !== 'idle'}
            formatDomain={formatOperabilityDomain as (domain: string) => string}
            formatAdminAction={formatOperabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleOperabilityAdminAction('refresh_operability_summary')
            }
          />
        ) : null}
        {props.customizabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Customizability admin"
            panelClassName="workspace-customizability-admin"
            listClassName="workspace-customizability-list"
            cardClassName="workspace-customizability-card"
            role={props.customizabilityAdminSummary.role}
            guidance={props.customizabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Workflow customizability',
              value: `${props.customizabilityAdminSummary.stats.customizabilityPercent}%`,
              detail: <>{props.customizabilityAdminSummary.stats.coveredDomains}/
                  {props.customizabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Customizability signals',
              value: props.customizabilityAdminSummary.stats.totalRecords,
              detail: <>{props.customizabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, run workflows, and usage events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.customizabilityAdminSummary.records}
            availableActions={props.customizabilityAdminSummary.availableActions}
            refreshAction="refresh_customizability_summary"
            actionBusy={props.customizabilityAdminAction !== 'idle'}
            formatDomain={formatCustomizabilityDomain as (domain: string) => string}
            formatAdminAction={formatCustomizabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleCustomizabilityAdminAction('refresh_customizability_summary')
            }
          />
        ) : null}
        {props.configurabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Configurability admin"
            panelClassName="workspace-configurability-admin"
            listClassName="workspace-configurability-list"
            cardClassName="workspace-configurability-card"
            role={props.configurabilityAdminSummary.role}
            guidance={props.configurabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Provider credential configurability',
              value: `${props.configurabilityAdminSummary.stats.configurabilityPercent}%`,
              detail: <>{props.configurabilityAdminSummary.stats.coveredDomains}/
                  {props.configurabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Configurability signals',
              value: props.configurabilityAdminSummary.stats.totalRecords,
              detail: <>{props.configurabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, provider credentials, and meter usage reports'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.configurabilityAdminSummary.records}
            availableActions={props.configurabilityAdminSummary.availableActions}
            refreshAction="refresh_configurability_summary"
            actionBusy={props.configurabilityAdminAction !== 'idle'}
            formatDomain={formatConfigurabilityDomain as (domain: string) => string}
            formatAdminAction={formatConfigurabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleConfigurabilityAdminAction('refresh_configurability_summary')
            }
          />
        ) : null}
        {props.integrabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Integrability admin"
            panelClassName="workspace-integrability-admin"
            listClassName="workspace-integrability-list"
            cardClassName="workspace-integrability-card"
            role={props.integrabilityAdminSummary.role}
            guidance={props.integrabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Billing webhook integrability',
              value: `${props.integrabilityAdminSummary.stats.integrabilityPercent}%`,
              detail: <>{props.integrabilityAdminSummary.stats.coveredDomains}/
                  {props.integrabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Integrability signals',
              value: props.integrabilityAdminSummary.stats.totalRecords,
              detail: <>{props.integrabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, billing webhook events, and workspace memberships'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.integrabilityAdminSummary.records}
            availableActions={props.integrabilityAdminSummary.availableActions}
            refreshAction="refresh_integrability_summary"
            actionBusy={props.integrabilityAdminAction !== 'idle'}
            formatDomain={formatIntegrabilityDomain as (domain: string) => string}
            formatAdminAction={formatIntegrabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleIntegrabilityAdminAction('refresh_integrability_summary')
            }
          />
        ) : null}
        {props.controllabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Controllability admin"
            panelClassName="workspace-controllability-admin"
            listClassName="workspace-controllability-list"
            cardClassName="workspace-controllability-card"
            role={props.controllabilityAdminSummary.role}
            guidance={props.controllabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Idempotency key controllability',
              value: `${props.controllabilityAdminSummary.stats.controllabilityPercent}%`,
              detail: <>{props.controllabilityAdminSummary.stats.coveredDomains}/
                  {props.controllabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Controllability signals',
              value: props.controllabilityAdminSummary.stats.totalRecords,
              detail: <>{props.controllabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, idempotency keys, and usage events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.controllabilityAdminSummary.records}
            availableActions={props.controllabilityAdminSummary.availableActions}
            refreshAction="refresh_controllability_summary"
            actionBusy={props.controllabilityAdminAction !== 'idle'}
            formatDomain={formatControllabilityDomain as (domain: string) => string}
            formatAdminAction={formatControllabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleControllabilityAdminAction('refresh_controllability_summary')
            }
          />
        ) : null}
        {props.manageabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Manageability admin"
            panelClassName="workspace-manageability-admin"
            listClassName="workspace-manageability-list"
            cardClassName="workspace-manageability-card"
            role={props.manageabilityAdminSummary.role}
            guidance={props.manageabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Billing notification manageability',
              value: `${props.manageabilityAdminSummary.stats.manageabilityPercent}%`,
              detail: <>{props.manageabilityAdminSummary.stats.coveredDomains}/
                  {props.manageabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Manageability signals',
              value: props.manageabilityAdminSummary.stats.totalRecords,
              detail: <>{props.manageabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, billing notifications, and idempotency keys'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.manageabilityAdminSummary.records}
            availableActions={props.manageabilityAdminSummary.availableActions}
            refreshAction="refresh_manageability_summary"
            actionBusy={props.manageabilityAdminAction !== 'idle'}
            formatDomain={formatManageabilityDomain as (domain: string) => string}
            formatAdminAction={formatManageabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleManageabilityAdminAction('refresh_manageability_summary')
            }
          />
        ) : null}
        {props.deployabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Deployability admin"
            panelClassName="workspace-deployability-admin"
            listClassName="workspace-deployability-list"
            cardClassName="workspace-deployability-card"
            role={props.deployabilityAdminSummary.role}
            guidance={props.deployabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Provider credential deployability',
              value: `${props.deployabilityAdminSummary.stats.deployabilityPercent}%`,
              detail: <>{props.deployabilityAdminSummary.stats.coveredDomains}/
                  {props.deployabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Deployability signals',
              value: props.deployabilityAdminSummary.stats.totalRecords,
              detail: <>{props.deployabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, provider credentials, and usage events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.deployabilityAdminSummary.records}
            availableActions={props.deployabilityAdminSummary.availableActions}
            refreshAction="refresh_deployability_summary"
            actionBusy={props.deployabilityAdminAction !== 'idle'}
            formatDomain={formatDeployabilityDomain as (domain: string) => string}
            formatAdminAction={formatDeployabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleDeployabilityAdminAction('refresh_deployability_summary')
            }
          />
        ) : null}
        {props.programmabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Programmability admin"
            panelClassName="workspace-programmability-admin"
            listClassName="workspace-programmability-list"
            cardClassName="workspace-programmability-card"
            role={props.programmabilityAdminSummary.role}
            guidance={props.programmabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Workflow programmability',
              value: `${props.programmabilityAdminSummary.stats.programmabilityPercent}%`,
              detail: <>{props.programmabilityAdminSummary.stats.coveredDomains}/
                  {props.programmabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Programmability signals',
              value: props.programmabilityAdminSummary.stats.totalRecords,
              detail: <>{props.programmabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, run workflows, and artifacts'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.programmabilityAdminSummary.records}
            availableActions={props.programmabilityAdminSummary.availableActions}
            refreshAction="refresh_programmability_summary"
            actionBusy={props.programmabilityAdminAction !== 'idle'}
            formatDomain={formatProgrammabilityDomain as (domain: string) => string}
            formatAdminAction={formatProgrammabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleProgrammabilityAdminAction('refresh_programmability_summary')
            }
          />
        ) : null}
        {props.predictabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Predictability admin"
            panelClassName="workspace-predictability-admin"
            listClassName="workspace-predictability-list"
            cardClassName="workspace-predictability-card"
            role={props.predictabilityAdminSummary.role}
            guidance={props.predictabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Moderator synthesis predictability',
              value: `${props.predictabilityAdminSummary.stats.predictabilityPercent}%`,
              detail: <>{props.predictabilityAdminSummary.stats.coveredDomains}/
                  {props.predictabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Predictability signals',
              value: props.predictabilityAdminSummary.stats.totalRecords,
              detail: <>{props.predictabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, moderator syntheses, and billing invoices'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.predictabilityAdminSummary.records}
            availableActions={props.predictabilityAdminSummary.availableActions}
            refreshAction="refresh_predictability_summary"
            actionBusy={props.predictabilityAdminAction !== 'idle'}
            formatDomain={formatPredictabilityDomain as (domain: string) => string}
            formatAdminAction={formatPredictabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handlePredictabilityAdminAction('refresh_predictability_summary')
            }
          />
        ) : null}
        {props.monitorabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Monitorability admin"
            panelClassName="workspace-monitorability-admin"
            listClassName="workspace-monitorability-list"
            cardClassName="workspace-monitorability-card"
            role={props.monitorabilityAdminSummary.role}
            guidance={props.monitorabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Usage event monitorability',
              value: `${props.monitorabilityAdminSummary.stats.monitorabilityPercent}%`,
              detail: <>{props.monitorabilityAdminSummary.stats.coveredDomains}/
                  {props.monitorabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Monitorability signals',
              value: props.monitorabilityAdminSummary.stats.totalRecords,
              detail: <>{props.monitorabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, usage events, and billing records'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.monitorabilityAdminSummary.records}
            availableActions={props.monitorabilityAdminSummary.availableActions}
            refreshAction="refresh_monitorability_summary"
            actionBusy={props.monitorabilityAdminAction !== 'idle'}
            formatDomain={formatMonitorabilityDomain as (domain: string) => string}
            formatAdminAction={formatMonitorabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleMonitorabilityAdminAction('refresh_monitorability_summary')
            }
          />
        ) : null}
        {props.automatabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Automatability admin"
            panelClassName="workspace-automatability-admin"
            listClassName="workspace-automatability-list"
            cardClassName="workspace-automatability-card"
            role={props.automatabilityAdminSummary.role}
            guidance={props.automatabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Agent output automatability',
              value: `${props.automatabilityAdminSummary.stats.automatabilityPercent}%`,
              detail: <>{props.automatabilityAdminSummary.stats.coveredDomains}/
                  {props.automatabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Automatability signals',
              value: props.automatabilityAdminSummary.stats.totalRecords,
              detail: <>{props.automatabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, agent outputs, and artifacts'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.automatabilityAdminSummary.records}
            availableActions={props.automatabilityAdminSummary.availableActions}
            refreshAction="refresh_automatability_summary"
            actionBusy={props.automatabilityAdminAction !== 'idle'}
            formatDomain={formatAutomatabilityDomain as (domain: string) => string}
            formatAdminAction={formatAutomatabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleAutomatabilityAdminAction('refresh_automatability_summary')
            }
          />
        ) : null}
        {props.schedulabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Schedulability admin"
            panelClassName="workspace-schedulability-admin"
            listClassName="workspace-schedulability-list"
            cardClassName="workspace-schedulability-card"
            role={props.schedulabilityAdminSummary.role}
            guidance={props.schedulabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Meter usage schedulability',
              value: `${props.schedulabilityAdminSummary.stats.schedulabilityPercent}%`,
              detail: <>{props.schedulabilityAdminSummary.stats.coveredDomains}/
                  {props.schedulabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Schedulability signals',
              value: props.schedulabilityAdminSummary.stats.totalRecords,
              detail: <>{props.schedulabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, meter usage reports, and usage events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.schedulabilityAdminSummary.records}
            availableActions={props.schedulabilityAdminSummary.availableActions}
            refreshAction="refresh_schedulability_summary"
            actionBusy={props.schedulabilityAdminAction !== 'idle'}
            formatDomain={formatSchedulabilityDomain as (domain: string) => string}
            formatAdminAction={formatSchedulabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleSchedulabilityAdminAction('refresh_schedulability_summary')
            }
          />
        ) : null}
        {props.orchestrabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Orchestrability admin"
            panelClassName="workspace-orchestrability-admin"
            listClassName="workspace-orchestrability-list"
            cardClassName="workspace-orchestrability-card"
            role={props.orchestrabilityAdminSummary.role}
            guidance={props.orchestrabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Workflow orchestrability',
              value: `${props.orchestrabilityAdminSummary.stats.orchestrabilityPercent}%`,
              detail: <>{props.orchestrabilityAdminSummary.stats.coveredDomains}/
                  {props.orchestrabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Orchestrability signals',
              value: props.orchestrabilityAdminSummary.stats.totalRecords,
              detail: <>{props.orchestrabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, run workflows, and billing notifications'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.orchestrabilityAdminSummary.records}
            availableActions={props.orchestrabilityAdminSummary.availableActions}
            refreshAction="refresh_orchestrability_summary"
            actionBusy={props.orchestrabilityAdminAction !== 'idle'}
            formatDomain={formatOrchestrabilityDomain as (domain: string) => string}
            formatAdminAction={formatOrchestrabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleOrchestrabilityAdminAction('refresh_orchestrability_summary')
            }
          />
        ) : null}
        {props.trustworthinessAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Trustworthiness admin"
            panelClassName="workspace-trustworthiness-admin"
            listClassName="workspace-trustworthiness-list"
            cardClassName="workspace-trustworthiness-card"
            role={props.trustworthinessAdminSummary.role}
            guidance={props.trustworthinessAdminSummary.guidance}
            stats={[
            {
              label: 'Shield scan trustworthiness',
              value: `${props.trustworthinessAdminSummary.stats.trustworthinessPercent}%`,
              detail: <>{props.trustworthinessAdminSummary.stats.coveredDomains}/
                  {props.trustworthinessAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Trustworthiness signals',
              value: props.trustworthinessAdminSummary.stats.totalRecords,
              detail: <>{props.trustworthinessAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, shield scans, and provider credentials'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.trustworthinessAdminSummary.records}
            availableActions={props.trustworthinessAdminSummary.availableActions}
            refreshAction="refresh_trustworthiness_summary"
            actionBusy={props.trustworthinessAdminAction !== 'idle'}
            formatDomain={formatTrustworthinessDomain as (domain: string) => string}
            formatAdminAction={formatTrustworthinessAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleTrustworthinessAdminAction('refresh_trustworthiness_summary')
            }
          />
        ) : null}
        {props.composabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Composability admin"
            panelClassName="workspace-composability-admin"
            listClassName="workspace-composability-list"
            cardClassName="workspace-composability-card"
            role={props.composabilityAdminSummary.role}
            guidance={props.composabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Workflow composability',
              value: `${props.composabilityAdminSummary.stats.composabilityPercent}%`,
              detail: <>{props.composabilityAdminSummary.stats.coveredDomains}/
                  {props.composabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Composability signals',
              value: props.composabilityAdminSummary.stats.totalRecords,
              detail: <>{props.composabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, run workflows, and agent outputs'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.composabilityAdminSummary.records}
            availableActions={props.composabilityAdminSummary.availableActions}
            refreshAction="refresh_composability_summary"
            actionBusy={props.composabilityAdminAction !== 'idle'}
            formatDomain={formatComposabilityDomain as (domain: string) => string}
            formatAdminAction={formatComposabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleComposabilityAdminAction('refresh_composability_summary')
            }
          />
        ) : null}
        {props.dependabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Dependability admin"
            panelClassName="workspace-dependability-admin"
            listClassName="workspace-dependability-list"
            cardClassName="workspace-dependability-card"
            role={props.dependabilityAdminSummary.role}
            guidance={props.dependabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Billing record dependability',
              value: `${props.dependabilityAdminSummary.stats.dependabilityPercent}%`,
              detail: <>{props.dependabilityAdminSummary.stats.coveredDomains}/
                  {props.dependabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Dependability signals',
              value: props.dependabilityAdminSummary.stats.totalRecords,
              detail: <>{props.dependabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, billing records, and billing invoices'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.dependabilityAdminSummary.records}
            availableActions={props.dependabilityAdminSummary.availableActions}
            refreshAction="refresh_dependability_summary"
            actionBusy={props.dependabilityAdminAction !== 'idle'}
            formatDomain={formatDependabilityDomain as (domain: string) => string}
            formatAdminAction={formatDependabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleDependabilityAdminAction('refresh_dependability_summary')
            }
          />
        ) : null}
        {props.responsivenessAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Responsiveness admin"
            panelClassName="workspace-responsiveness-admin"
            listClassName="workspace-responsiveness-list"
            cardClassName="workspace-responsiveness-card"
            role={props.responsivenessAdminSummary.role}
            guidance={props.responsivenessAdminSummary.guidance}
            stats={[
            {
              label: 'Usage event responsiveness',
              value: `${props.responsivenessAdminSummary.stats.responsivenessPercent}%`,
              detail: <>{props.responsivenessAdminSummary.stats.coveredDomains}/
                  {props.responsivenessAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Responsiveness signals',
              value: props.responsivenessAdminSummary.stats.totalRecords,
              detail: <>{props.responsivenessAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, usage events, and meter usage reports'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.responsivenessAdminSummary.records}
            availableActions={props.responsivenessAdminSummary.availableActions}
            refreshAction="refresh_responsiveness_summary"
            actionBusy={props.responsivenessAdminAction !== 'idle'}
            formatDomain={formatResponsivenessDomain as (domain: string) => string}
            formatAdminAction={formatResponsivenessAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleResponsivenessAdminAction('refresh_responsiveness_summary')
            }
          />
        ) : null}
        {props.repeatabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Repeatability admin"
            panelClassName="workspace-repeatability-admin"
            listClassName="workspace-repeatability-list"
            cardClassName="workspace-repeatability-card"
            role={props.repeatabilityAdminSummary.role}
            guidance={props.repeatabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Artifact repeatability',
              value: `${props.repeatabilityAdminSummary.stats.repeatabilityPercent}%`,
              detail: <>{props.repeatabilityAdminSummary.stats.coveredDomains}/
                  {props.repeatabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Repeatability signals',
              value: props.repeatabilityAdminSummary.stats.totalRecords,
              detail: <>{props.repeatabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, artifacts, and run workflows'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.repeatabilityAdminSummary.records}
            availableActions={props.repeatabilityAdminSummary.availableActions}
            refreshAction="refresh_repeatability_summary"
            actionBusy={props.repeatabilityAdminAction !== 'idle'}
            formatDomain={formatRepeatabilityDomain as (domain: string) => string}
            formatAdminAction={formatRepeatabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleRepeatabilityAdminAction('refresh_repeatability_summary')
            }
          />
        ) : null}
        {props.survivabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Survivability admin"
            panelClassName="workspace-survivability-admin"
            listClassName="workspace-survivability-list"
            cardClassName="workspace-survivability-card"
            role={props.survivabilityAdminSummary.role}
            guidance={props.survivabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Billing record survivability',
              value: `${props.survivabilityAdminSummary.stats.survivabilityPercent}%`,
              detail: <>{props.survivabilityAdminSummary.stats.coveredDomains}/
                  {props.survivabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Survivability signals',
              value: props.survivabilityAdminSummary.stats.totalRecords,
              detail: <>{props.survivabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, billing records, and meter usage reports'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.survivabilityAdminSummary.records}
            availableActions={props.survivabilityAdminSummary.availableActions}
            refreshAction="refresh_survivability_summary"
            actionBusy={props.survivabilityAdminAction !== 'idle'}
            formatDomain={formatSurvivabilityDomain as (domain: string) => string}
            formatAdminAction={formatSurvivabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleSurvivabilityAdminAction('refresh_survivability_summary')
            }
          />
        ) : null}
        {props.appropriatenessAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Appropriateness admin"
            panelClassName="workspace-appropriateness-admin"
            listClassName="workspace-appropriateness-list"
            cardClassName="workspace-appropriateness-card"
            role={props.appropriatenessAdminSummary.role}
            guidance={props.appropriatenessAdminSummary.guidance}
            stats={[
            {
              label: 'Billing invoice appropriateness',
              value: `${props.appropriatenessAdminSummary.stats.appropriatenessPercent}%`,
              detail: <>{props.appropriatenessAdminSummary.stats.coveredDomains}/
                  {props.appropriatenessAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Appropriateness signals',
              value: props.appropriatenessAdminSummary.stats.totalRecords,
              detail: <>{props.appropriatenessAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, billing invoices, and billing records'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.appropriatenessAdminSummary.records}
            availableActions={props.appropriatenessAdminSummary.availableActions}
            refreshAction="refresh_appropriateness_summary"
            actionBusy={props.appropriatenessAdminAction !== 'idle'}
            formatDomain={formatAppropriatenessDomain as (domain: string) => string}
            formatAdminAction={formatAppropriatenessAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleAppropriatenessAdminAction('refresh_appropriateness_summary')
            }
          />
        ) : null}
        {props.effectivenessAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Effectiveness admin"
            panelClassName="workspace-effectiveness-admin"
            listClassName="workspace-effectiveness-list"
            cardClassName="workspace-effectiveness-card"
            role={props.effectivenessAdminSummary.role}
            guidance={props.effectivenessAdminSummary.guidance}
            stats={[
            {
              label: 'Agent output effectiveness',
              value: `${props.effectivenessAdminSummary.stats.effectivenessPercent}%`,
              detail: <>{props.effectivenessAdminSummary.stats.coveredDomains}/
                  {props.effectivenessAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Effectiveness signals',
              value: props.effectivenessAdminSummary.stats.totalRecords,
              detail: <>{props.effectivenessAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, agent outputs, and moderator syntheses'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.effectivenessAdminSummary.records}
            availableActions={props.effectivenessAdminSummary.availableActions}
            refreshAction="refresh_effectiveness_summary"
            actionBusy={props.effectivenessAdminAction !== 'idle'}
            formatDomain={formatEffectivenessDomain as (domain: string) => string}
            formatAdminAction={formatEffectivenessAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleEffectivenessAdminAction('refresh_effectiveness_summary')
            }
          />
        ) : null}
        {props.accessibilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Accessibility admin"
            panelClassName="workspace-accessibility-admin"
            listClassName="workspace-accessibility-list"
            cardClassName="workspace-accessibility-card"
            role={props.accessibilityAdminSummary.role}
            guidance={props.accessibilityAdminSummary.guidance}
            stats={[
            {
              label: 'Idempotency key accessibility',
              value: `${props.accessibilityAdminSummary.stats.accessibilityPercent}%`,
              detail: <>{props.accessibilityAdminSummary.stats.coveredDomains}/
                  {props.accessibilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Accessibility signals',
              value: props.accessibilityAdminSummary.stats.totalRecords,
              detail: <>{props.accessibilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, idempotency keys, and usage events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.accessibilityAdminSummary.records}
            availableActions={props.accessibilityAdminSummary.availableActions}
            refreshAction="refresh_accessibility_summary"
            actionBusy={props.accessibilityAdminAction !== 'idle'}
            formatDomain={formatAccessibilityDomain as (domain: string) => string}
            formatAdminAction={formatAccessibilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleAccessibilityAdminAction('refresh_accessibility_summary')
            }
          />
        ) : null}
        {props.usabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Usability admin"
            panelClassName="workspace-usability-admin"
            listClassName="workspace-usability-list"
            cardClassName="workspace-usability-card"
            role={props.usabilityAdminSummary.role}
            guidance={props.usabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Membership usability',
              value: `${props.usabilityAdminSummary.stats.usabilityPercent}%`,
              detail: <>{props.usabilityAdminSummary.stats.coveredDomains}/
                  {props.usabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Usability signals',
              value: props.usabilityAdminSummary.stats.totalRecords,
              detail: <>{props.usabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, workspace memberships, and usage events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.usabilityAdminSummary.records}
            availableActions={props.usabilityAdminSummary.availableActions}
            refreshAction="refresh_usability_summary"
            actionBusy={props.usabilityAdminAction !== 'idle'}
            formatDomain={formatUsabilityDomain as (domain: string) => string}
            formatAdminAction={formatUsabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleUsabilityAdminAction('refresh_usability_summary')
            }
          />
        ) : null}
        {props.acceptabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Acceptability admin"
            panelClassName="workspace-acceptability-admin"
            listClassName="workspace-acceptability-list"
            cardClassName="workspace-acceptability-card"
            role={props.acceptabilityAdminSummary.role}
            guidance={props.acceptabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Billing record acceptability',
              value: `${props.acceptabilityAdminSummary.stats.acceptabilityPercent}%`,
              detail: <>{props.acceptabilityAdminSummary.stats.coveredDomains}/
                  {props.acceptabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Acceptability signals',
              value: props.acceptabilityAdminSummary.stats.totalRecords,
              detail: <>{props.acceptabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, billing records, and billing invoices'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.acceptabilityAdminSummary.records}
            availableActions={props.acceptabilityAdminSummary.availableActions}
            refreshAction="refresh_acceptability_summary"
            actionBusy={props.acceptabilityAdminAction !== 'idle'}
            formatDomain={formatAcceptabilityDomain as (domain: string) => string}
            formatAdminAction={formatAcceptabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleAcceptabilityAdminAction('refresh_acceptability_summary')
            }
          />
        ) : null}
        {props.adoptabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Adoptability admin"
            panelClassName="workspace-adoptability-admin"
            listClassName="workspace-adoptability-list"
            cardClassName="workspace-adoptability-card"
            role={props.adoptabilityAdminSummary.role}
            guidance={props.adoptabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Usage event adoptability',
              value: `${props.adoptabilityAdminSummary.stats.adoptabilityPercent}%`,
              detail: <>{props.adoptabilityAdminSummary.stats.coveredDomains}/
                  {props.adoptabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Adoptability signals',
              value: props.adoptabilityAdminSummary.stats.totalRecords,
              detail: <>{props.adoptabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, usage events, and workspace memberships'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.adoptabilityAdminSummary.records}
            availableActions={props.adoptabilityAdminSummary.availableActions}
            refreshAction="refresh_adoptability_summary"
            actionBusy={props.adoptabilityAdminAction !== 'idle'}
            formatDomain={formatAdoptabilityDomain as (domain: string) => string}
            formatAdminAction={formatAdoptabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleAdoptabilityAdminAction('refresh_adoptability_summary')
            }
          />
        ) : null}
        {props.conformanceAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Conformance admin"
            panelClassName="workspace-conformance-admin"
            listClassName="workspace-conformance-list"
            cardClassName="workspace-conformance-card"
            role={props.conformanceAdminSummary.role}
            guidance={props.conformanceAdminSummary.guidance}
            stats={[
            {
              label: 'Shield scan conformance',
              value: `${props.conformanceAdminSummary.stats.conformancePercent}%`,
              detail: <>{props.conformanceAdminSummary.stats.coveredDomains}/
                  {props.conformanceAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Conformance signals',
              value: props.conformanceAdminSummary.stats.totalRecords,
              detail: <>{props.conformanceAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, shield scans, and billing webhook events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.conformanceAdminSummary.records}
            availableActions={props.conformanceAdminSummary.availableActions}
            refreshAction="refresh_conformance_summary"
            actionBusy={props.conformanceAdminAction !== 'idle'}
            formatDomain={formatConformanceDomain as (domain: string) => string}
            formatAdminAction={formatConformanceAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleConformanceAdminAction('refresh_conformance_summary')
            }
          />
        ) : null}
        {props.feasibilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Feasibility admin"
            panelClassName="workspace-feasibility-admin"
            listClassName="workspace-feasibility-list"
            cardClassName="workspace-feasibility-card"
            role={props.feasibilityAdminSummary.role}
            guidance={props.feasibilityAdminSummary.guidance}
            stats={[
            {
              label: 'Provider credential feasibility',
              value: `${props.feasibilityAdminSummary.stats.feasibilityPercent}%`,
              detail: <>{props.feasibilityAdminSummary.stats.coveredDomains}/
                  {props.feasibilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Feasibility signals',
              value: props.feasibilityAdminSummary.stats.totalRecords,
              detail: <>{props.feasibilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, provider credentials, and usage events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.feasibilityAdminSummary.records}
            availableActions={props.feasibilityAdminSummary.availableActions}
            refreshAction="refresh_feasibility_summary"
            actionBusy={props.feasibilityAdminAction !== 'idle'}
            formatDomain={formatFeasibilityDomain as (domain: string) => string}
            formatAdminAction={formatFeasibilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleFeasibilityAdminAction('refresh_feasibility_summary')
            }
          />
        ) : null}
        {props.viabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Viability admin"
            panelClassName="workspace-viability-admin"
            listClassName="workspace-viability-list"
            cardClassName="workspace-viability-card"
            role={props.viabilityAdminSummary.role}
            guidance={props.viabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Billing invoice viability',
              value: `${props.viabilityAdminSummary.stats.viabilityPercent}%`,
              detail: <>{props.viabilityAdminSummary.stats.coveredDomains}/
                  {props.viabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Viability signals',
              value: props.viabilityAdminSummary.stats.totalRecords,
              detail: <>{props.viabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, billing invoices, and billing records'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.viabilityAdminSummary.records}
            availableActions={props.viabilityAdminSummary.availableActions}
            refreshAction="refresh_viability_summary"
            actionBusy={props.viabilityAdminAction !== 'idle'}
            formatDomain={formatViabilityDomain as (domain: string) => string}
            formatAdminAction={formatViabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleViabilityAdminAction('refresh_viability_summary')
            }
          />
        ) : null}
        {props.profitabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Profitability admin"
            panelClassName="workspace-profitability-admin"
            listClassName="workspace-profitability-list"
            cardClassName="workspace-profitability-card"
            role={props.profitabilityAdminSummary.role}
            guidance={props.profitabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Billing record profitability',
              value: `${props.profitabilityAdminSummary.stats.profitabilityPercent}%`,
              detail: <>{props.profitabilityAdminSummary.stats.coveredDomains}/
                  {props.profitabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Profitability signals',
              value: props.profitabilityAdminSummary.stats.totalRecords,
              detail: <>{props.profitabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, billing records, and billing invoices'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.profitabilityAdminSummary.records}
            availableActions={props.profitabilityAdminSummary.availableActions}
            refreshAction="refresh_profitability_summary"
            actionBusy={props.profitabilityAdminAction !== 'idle'}
            formatDomain={formatProfitabilityDomain as (domain: string) => string}
            formatAdminAction={formatProfitabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleProfitabilityAdminAction('refresh_profitability_summary')
            }
          />
        ) : null}
        {props.suitabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Suitability admin"
            panelClassName="workspace-suitability-admin"
            listClassName="workspace-suitability-list"
            cardClassName="workspace-suitability-card"
            role={props.suitabilityAdminSummary.role}
            guidance={props.suitabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Agent output suitability',
              value: `${props.suitabilityAdminSummary.stats.suitabilityPercent}%`,
              detail: <>{props.suitabilityAdminSummary.stats.coveredDomains}/
                  {props.suitabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Suitability signals',
              value: props.suitabilityAdminSummary.stats.totalRecords,
              detail: <>{props.suitabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, agent outputs, and artifacts'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.suitabilityAdminSummary.records}
            availableActions={props.suitabilityAdminSummary.availableActions}
            refreshAction="refresh_suitability_summary"
            actionBusy={props.suitabilityAdminAction !== 'idle'}
            formatDomain={formatSuitabilityDomain as (domain: string) => string}
            formatAdminAction={formatSuitabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleSuitabilityAdminAction('refresh_suitability_summary')
            }
          />
        ) : null}
        {props.marketabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Marketability admin"
            panelClassName="workspace-marketability-admin"
            listClassName="workspace-marketability-list"
            cardClassName="workspace-marketability-card"
            role={props.marketabilityAdminSummary.role}
            guidance={props.marketabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Membership marketability',
              value: `${props.marketabilityAdminSummary.stats.marketabilityPercent}%`,
              detail: <>{props.marketabilityAdminSummary.stats.coveredDomains}/
                  {props.marketabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Marketability signals',
              value: props.marketabilityAdminSummary.stats.totalRecords,
              detail: <>{props.marketabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, workspace memberships, and meter usage reports'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.marketabilityAdminSummary.records}
            availableActions={props.marketabilityAdminSummary.availableActions}
            refreshAction="refresh_marketability_summary"
            actionBusy={props.marketabilityAdminAction !== 'idle'}
            formatDomain={formatMarketabilityDomain as (domain: string) => string}
            formatAdminAction={formatMarketabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleMarketabilityAdminAction('refresh_marketability_summary')
            }
          />
        ) : null}
        {props.desirabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Desirability admin"
            panelClassName="workspace-desirability-admin"
            listClassName="workspace-desirability-list"
            cardClassName="workspace-desirability-card"
            role={props.desirabilityAdminSummary.role}
            guidance={props.desirabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Usage event desirability',
              value: `${props.desirabilityAdminSummary.stats.desirabilityPercent}%`,
              detail: <>{props.desirabilityAdminSummary.stats.coveredDomains}/
                  {props.desirabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Desirability signals',
              value: props.desirabilityAdminSummary.stats.totalRecords,
              detail: <>{props.desirabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, usage events, and billing notifications'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.desirabilityAdminSummary.records}
            availableActions={props.desirabilityAdminSummary.availableActions}
            refreshAction="refresh_desirability_summary"
            actionBusy={props.desirabilityAdminAction !== 'idle'}
            formatDomain={formatDesirabilityDomain as (domain: string) => string}
            formatAdminAction={formatDesirabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleDesirabilityAdminAction('refresh_desirability_summary')
            }
          />
        ) : null}
        {props.affordabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Affordability admin"
            panelClassName="workspace-affordability-admin"
            listClassName="workspace-affordability-list"
            cardClassName="workspace-affordability-card"
            role={props.affordabilityAdminSummary.role}
            guidance={props.affordabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Billing invoice affordability',
              value: `${props.affordabilityAdminSummary.stats.affordabilityPercent}%`,
              detail: <>{props.affordabilityAdminSummary.stats.coveredDomains}/
                  {props.affordabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Affordability signals',
              value: props.affordabilityAdminSummary.stats.totalRecords,
              detail: <>{props.affordabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, billing invoices, and billing records'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.affordabilityAdminSummary.records}
            availableActions={props.affordabilityAdminSummary.availableActions}
            refreshAction="refresh_affordability_summary"
            actionBusy={props.affordabilityAdminAction !== 'idle'}
            formatDomain={formatAffordabilityDomain as (domain: string) => string}
            formatAdminAction={formatAffordabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleAffordabilityAdminAction('refresh_affordability_summary')
            }
          />
        ) : null}
        {props.teachabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Teachability admin"
            panelClassName="workspace-teachability-admin"
            listClassName="workspace-teachability-list"
            cardClassName="workspace-teachability-card"
            role={props.teachabilityAdminSummary.role}
            guidance={props.teachabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Workflow teachability',
              value: `${props.teachabilityAdminSummary.stats.teachabilityPercent}%`,
              detail: <>{props.teachabilityAdminSummary.stats.coveredDomains}/
                  {props.teachabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Teachability signals',
              value: props.teachabilityAdminSummary.stats.totalRecords,
              detail: <>{props.teachabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, run workflows, and agent outputs'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.teachabilityAdminSummary.records}
            availableActions={props.teachabilityAdminSummary.availableActions}
            refreshAction="refresh_teachability_summary"
            actionBusy={props.teachabilityAdminAction !== 'idle'}
            formatDomain={formatTeachabilityDomain as (domain: string) => string}
            formatAdminAction={formatTeachabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleTeachabilityAdminAction('refresh_teachability_summary')
            }
          />
        ) : null}
        {props.memorabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Memorability admin"
            panelClassName="workspace-memorability-admin"
            listClassName="workspace-memorability-list"
            cardClassName="workspace-memorability-card"
            role={props.memorabilityAdminSummary.role}
            guidance={props.memorabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Artifact memorability',
              value: `${props.memorabilityAdminSummary.stats.memorabilityPercent}%`,
              detail: <>{props.memorabilityAdminSummary.stats.coveredDomains}/
                  {props.memorabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Memorability signals',
              value: props.memorabilityAdminSummary.stats.totalRecords,
              detail: <>{props.memorabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, artifacts, and run workflows'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.memorabilityAdminSummary.records}
            availableActions={props.memorabilityAdminSummary.availableActions}
            refreshAction="refresh_memorability_summary"
            actionBusy={props.memorabilityAdminAction !== 'idle'}
            formatDomain={formatMemorabilityDomain as (domain: string) => string}
            formatAdminAction={formatMemorabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleMemorabilityAdminAction('refresh_memorability_summary')
            }
          />
        ) : null}
        {props.understandabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Understandability admin"
            panelClassName="workspace-understandability-admin"
            listClassName="workspace-understandability-list"
            cardClassName="workspace-understandability-card"
            role={props.understandabilityAdminSummary.role}
            guidance={props.understandabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Moderator synthesis understandability',
              value: `${props.understandabilityAdminSummary.stats.understandabilityPercent}%`,
              detail: <>{props.understandabilityAdminSummary.stats.coveredDomains}/
                  {props.understandabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Understandability signals',
              value: props.understandabilityAdminSummary.stats.totalRecords,
              detail: <>{props.understandabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, moderator syntheses, and agent outputs'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.understandabilityAdminSummary.records}
            availableActions={props.understandabilityAdminSummary.availableActions}
            refreshAction="refresh_understandability_summary"
            actionBusy={props.understandabilityAdminAction !== 'idle'}
            formatDomain={formatUnderstandabilityDomain as (domain: string) => string}
            formatAdminAction={formatUnderstandabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleUnderstandabilityAdminAction('refresh_understandability_summary')
            }
          />
        ) : null}
        {props.deliverabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Deliverability admin"
            panelClassName="workspace-deliverability-admin"
            listClassName="workspace-deliverability-list"
            cardClassName="workspace-deliverability-card"
            role={props.deliverabilityAdminSummary.role}
            guidance={props.deliverabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Billing notification deliverability',
              value: `${props.deliverabilityAdminSummary.stats.deliverabilityPercent}%`,
              detail: <>{props.deliverabilityAdminSummary.stats.coveredDomains}/
                  {props.deliverabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Deliverability signals',
              value: props.deliverabilityAdminSummary.stats.totalRecords,
              detail: <>{props.deliverabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, billing notifications, and billing webhook events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.deliverabilityAdminSummary.records}
            availableActions={props.deliverabilityAdminSummary.availableActions}
            refreshAction="refresh_deliverability_summary"
            actionBusy={props.deliverabilityAdminAction !== 'idle'}
            formatDomain={formatDeliverabilityDomain as (domain: string) => string}
            formatAdminAction={formatDeliverabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleDeliverabilityAdminAction('refresh_deliverability_summary')
            }
          />
        ) : null}
        {props.learnabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Learnability admin"
            panelClassName="workspace-learnability-admin"
            listClassName="workspace-learnability-list"
            cardClassName="workspace-learnability-card"
            role={props.learnabilityAdminSummary.role}
            guidance={props.learnabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Agent output learnability',
              value: `${props.learnabilityAdminSummary.stats.learnabilityPercent}%`,
              detail: <>{props.learnabilityAdminSummary.stats.coveredDomains}/
                  {props.learnabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Learnability signals',
              value: props.learnabilityAdminSummary.stats.totalRecords,
              detail: <>{props.learnabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, agent outputs, and artifacts'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.learnabilityAdminSummary.records}
            availableActions={props.learnabilityAdminSummary.availableActions}
            refreshAction="refresh_learnability_summary"
            actionBusy={props.learnabilityAdminAction !== 'idle'}
            formatDomain={formatLearnabilityDomain as (domain: string) => string}
            formatAdminAction={formatLearnabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleLearnabilityAdminAction('refresh_learnability_summary')
            }
          />
        ) : null}
        {props.comprehensibilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Comprehensibility admin"
            panelClassName="workspace-comprehensibility-admin"
            listClassName="workspace-comprehensibility-list"
            cardClassName="workspace-comprehensibility-card"
            role={props.comprehensibilityAdminSummary.role}
            guidance={props.comprehensibilityAdminSummary.guidance}
            stats={[
            {
              label: 'Agent output comprehensibility',
              value: `${props.comprehensibilityAdminSummary.stats.comprehensibilityPercent}%`,
              detail: <>{props.comprehensibilityAdminSummary.stats.coveredDomains}/
                  {props.comprehensibilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Comprehensibility signals',
              value: props.comprehensibilityAdminSummary.stats.totalRecords,
              detail: <>{props.comprehensibilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, agent outputs, and moderator syntheses'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.comprehensibilityAdminSummary.records}
            availableActions={props.comprehensibilityAdminSummary.availableActions}
            refreshAction="refresh_comprehensibility_summary"
            actionBusy={props.comprehensibilityAdminAction !== 'idle'}
            formatDomain={formatComprehensibilityDomain as (domain: string) => string}
            formatAdminAction={formatComprehensibilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleComprehensibilityAdminAction('refresh_comprehensibility_summary')
            }
          />
        ) : null}
        {props.negotiabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Negotiability admin"
            panelClassName="workspace-negotiability-admin"
            listClassName="workspace-negotiability-list"
            cardClassName="workspace-negotiability-card"
            role={props.negotiabilityAdminSummary.role}
            guidance={props.negotiabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Billing invoice negotiability',
              value: `${props.negotiabilityAdminSummary.stats.negotiabilityPercent}%`,
              detail: <>{props.negotiabilityAdminSummary.stats.coveredDomains}/
                  {props.negotiabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Negotiability signals',
              value: props.negotiabilityAdminSummary.stats.totalRecords,
              detail: <>{props.negotiabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, billing invoices, and billing records'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.negotiabilityAdminSummary.records}
            availableActions={props.negotiabilityAdminSummary.availableActions}
            refreshAction="refresh_negotiability_summary"
            actionBusy={props.negotiabilityAdminAction !== 'idle'}
            formatDomain={formatNegotiabilityDomain as (domain: string) => string}
            formatAdminAction={formatNegotiabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleNegotiabilityAdminAction('refresh_negotiability_summary')
            }
          />
        ) : null}
        {props.simplicityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Simplicity admin"
            panelClassName="workspace-simplicity-admin"
            listClassName="workspace-simplicity-list"
            cardClassName="workspace-simplicity-card"
            role={props.simplicityAdminSummary.role}
            guidance={props.simplicityAdminSummary.guidance}
            stats={[
            {
              label: 'Workflow simplicity',
              value: `${props.simplicityAdminSummary.stats.simplicityPercent}%`,
              detail: <>{props.simplicityAdminSummary.stats.coveredDomains}/
                  {props.simplicityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Simplicity signals',
              value: props.simplicityAdminSummary.stats.totalRecords,
              detail: <>{props.simplicityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, run workflows, and idempotency keys'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.simplicityAdminSummary.records}
            availableActions={props.simplicityAdminSummary.availableActions}
            refreshAction="refresh_simplicity_summary"
            actionBusy={props.simplicityAdminAction !== 'idle'}
            formatDomain={formatSimplicityDomain as (domain: string) => string}
            formatAdminAction={formatSimplicityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleSimplicityAdminAction('refresh_simplicity_summary')
            }
          />
        ) : null}
        {props.clarityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Clarity admin"
            panelClassName="workspace-clarity-admin"
            listClassName="workspace-clarity-list"
            cardClassName="workspace-clarity-card"
            role={props.clarityAdminSummary.role}
            guidance={props.clarityAdminSummary.guidance}
            stats={[
            {
              label: 'Moderator synthesis clarity',
              value: `${props.clarityAdminSummary.stats.clarityPercent}%`,
              detail: <>{props.clarityAdminSummary.stats.coveredDomains}/
                  {props.clarityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Clarity signals',
              value: props.clarityAdminSummary.stats.totalRecords,
              detail: <>{props.clarityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, moderator syntheses, and agent outputs'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.clarityAdminSummary.records}
            availableActions={props.clarityAdminSummary.availableActions}
            refreshAction="refresh_clarity_summary"
            actionBusy={props.clarityAdminAction !== 'idle'}
            formatDomain={formatClarityDomain as (domain: string) => string}
            formatAdminAction={formatClarityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleClarityAdminAction('refresh_clarity_summary')
            }
          />
        ) : null}
        {props.readabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Readability admin"
            panelClassName="workspace-readability-admin"
            listClassName="workspace-readability-list"
            cardClassName="workspace-readability-card"
            role={props.readabilityAdminSummary.role}
            guidance={props.readabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Artifact readability',
              value: `${props.readabilityAdminSummary.stats.readabilityPercent}%`,
              detail: <>{props.readabilityAdminSummary.stats.coveredDomains}/
                  {props.readabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Readability signals',
              value: props.readabilityAdminSummary.stats.totalRecords,
              detail: <>{props.readabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, artifacts, and agent outputs'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.readabilityAdminSummary.records}
            availableActions={props.readabilityAdminSummary.availableActions}
            refreshAction="refresh_readability_summary"
            actionBusy={props.readabilityAdminAction !== 'idle'}
            formatDomain={formatReadabilityDomain as (domain: string) => string}
            formatAdminAction={formatReadabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleReadabilityAdminAction('refresh_readability_summary')
            }
          />
        ) : null}
        {props.familiarityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Familiarity admin"
            panelClassName="workspace-familiarity-admin"
            listClassName="workspace-familiarity-list"
            cardClassName="workspace-familiarity-card"
            role={props.familiarityAdminSummary.role}
            guidance={props.familiarityAdminSummary.guidance}
            stats={[
            {
              label: 'Membership familiarity',
              value: `${props.familiarityAdminSummary.stats.familiarityPercent}%`,
              detail: <>{props.familiarityAdminSummary.stats.coveredDomains}/
                  {props.familiarityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Familiarity signals',
              value: props.familiarityAdminSummary.stats.totalRecords,
              detail: <>{props.familiarityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, workspace memberships, and usage events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.familiarityAdminSummary.records}
            availableActions={props.familiarityAdminSummary.availableActions}
            refreshAction="refresh_familiarity_summary"
            actionBusy={props.familiarityAdminAction !== 'idle'}
            formatDomain={formatFamiliarityDomain as (domain: string) => string}
            formatAdminAction={formatFamiliarityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleFamiliarityAdminAction('refresh_familiarity_summary')
            }
          />
        ) : null}
        {props.coherenceAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Coherence admin"
            panelClassName="workspace-coherence-admin"
            listClassName="workspace-coherence-list"
            cardClassName="workspace-coherence-card"
            role={props.coherenceAdminSummary.role}
            guidance={props.coherenceAdminSummary.guidance}
            stats={[
            {
              label: 'Workflow coherence',
              value: `${props.coherenceAdminSummary.stats.coherencePercent}%`,
              detail: <>{props.coherenceAdminSummary.stats.coveredDomains}/
                  {props.coherenceAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Coherence signals',
              value: props.coherenceAdminSummary.stats.totalRecords,
              detail: <>{props.coherenceAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, run workflows, and agent outputs'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.coherenceAdminSummary.records}
            availableActions={props.coherenceAdminSummary.availableActions}
            refreshAction="refresh_coherence_summary"
            actionBusy={props.coherenceAdminAction !== 'idle'}
            formatDomain={formatCoherenceDomain as (domain: string) => string}
            formatAdminAction={formatCoherenceAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleCoherenceAdminAction('refresh_coherence_summary')
            }
          />
        ) : null}
        {props.parsabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Parsability admin"
            panelClassName="workspace-parsability-admin"
            listClassName="workspace-parsability-list"
            cardClassName="workspace-parsability-card"
            role={props.parsabilityAdminSummary.role}
            guidance={props.parsabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Idempotency key parsability',
              value: `${props.parsabilityAdminSummary.stats.parsabilityPercent}%`,
              detail: <>{props.parsabilityAdminSummary.stats.coveredDomains}/
                  {props.parsabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Parsability signals',
              value: props.parsabilityAdminSummary.stats.totalRecords,
              detail: <>{props.parsabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, idempotency keys, and usage events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.parsabilityAdminSummary.records}
            availableActions={props.parsabilityAdminSummary.availableActions}
            refreshAction="refresh_parsability_summary"
            actionBusy={props.parsabilityAdminAction !== 'idle'}
            formatDomain={formatParsabilityDomain as (domain: string) => string}
            formatAdminAction={formatParsabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleParsabilityAdminAction('refresh_parsability_summary')
            }
          />
        ) : null}
        {props.legibilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Legibility admin"
            panelClassName="workspace-legibility-admin"
            listClassName="workspace-legibility-list"
            cardClassName="workspace-legibility-card"
            role={props.legibilityAdminSummary.role}
            guidance={props.legibilityAdminSummary.guidance}
            stats={[
            {
              label: 'Artifact legibility',
              value: `${props.legibilityAdminSummary.stats.legibilityPercent}%`,
              detail: <>{props.legibilityAdminSummary.stats.coveredDomains}/
                  {props.legibilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Legibility signals',
              value: props.legibilityAdminSummary.stats.totalRecords,
              detail: <>{props.legibilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, artifacts, and run workflows'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.legibilityAdminSummary.records}
            availableActions={props.legibilityAdminSummary.availableActions}
            refreshAction="refresh_legibility_summary"
            actionBusy={props.legibilityAdminAction !== 'idle'}
            formatDomain={formatLegibilityDomain as (domain: string) => string}
            formatAdminAction={formatLegibilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleLegibilityAdminAction('refresh_legibility_summary')
            }
          />
        ) : null}
        {props.intelligibilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Intelligibility admin"
            panelClassName="workspace-intelligibility-admin"
            listClassName="workspace-intelligibility-list"
            cardClassName="workspace-intelligibility-card"
            role={props.intelligibilityAdminSummary.role}
            guidance={props.intelligibilityAdminSummary.guidance}
            stats={[
            {
              label: 'Moderator synthesis intelligibility',
              value: `${props.intelligibilityAdminSummary.stats.intelligibilityPercent}%`,
              detail: <>{props.intelligibilityAdminSummary.stats.coveredDomains}/
                  {props.intelligibilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Intelligibility signals',
              value: props.intelligibilityAdminSummary.stats.totalRecords,
              detail: <>{props.intelligibilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, moderator syntheses, and agent outputs'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.intelligibilityAdminSummary.records}
            availableActions={props.intelligibilityAdminSummary.availableActions}
            refreshAction="refresh_intelligibility_summary"
            actionBusy={props.intelligibilityAdminAction !== 'idle'}
            formatDomain={formatIntelligibilityDomain as (domain: string) => string}
            formatAdminAction={formatIntelligibilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleIntelligibilityAdminAction('refresh_intelligibility_summary')
            }
          />
        ) : null}
        {props.noticeabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Noticeability admin"
            panelClassName="workspace-noticeability-admin"
            listClassName="workspace-noticeability-list"
            cardClassName="workspace-noticeability-card"
            role={props.noticeabilityAdminSummary.role}
            guidance={props.noticeabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Billing notification noticeability',
              value: `${props.noticeabilityAdminSummary.stats.noticeabilityPercent}%`,
              detail: <>{props.noticeabilityAdminSummary.stats.coveredDomains}/
                  {props.noticeabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Noticeability signals',
              value: props.noticeabilityAdminSummary.stats.totalRecords,
              detail: <>{props.noticeabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, billing notifications, and billing webhook events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.noticeabilityAdminSummary.records}
            availableActions={props.noticeabilityAdminSummary.availableActions}
            refreshAction="refresh_noticeability_summary"
            actionBusy={props.noticeabilityAdminAction !== 'idle'}
            formatDomain={formatNoticeabilityDomain as (domain: string) => string}
            formatAdminAction={formatNoticeabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleNoticeabilityAdminAction('refresh_noticeability_summary')
            }
          />
        ) : null}
        {props.perceptibilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Perceptibility admin"
            panelClassName="workspace-perceptibility-admin"
            listClassName="workspace-perceptibility-list"
            cardClassName="workspace-perceptibility-card"
            role={props.perceptibilityAdminSummary.role}
            guidance={props.perceptibilityAdminSummary.guidance}
            stats={[
            {
              label: 'Usage event perceptibility',
              value: `${props.perceptibilityAdminSummary.stats.perceptibilityPercent}%`,
              detail: <>{props.perceptibilityAdminSummary.stats.coveredDomains}/
                  {props.perceptibilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Perceptibility signals',
              value: props.perceptibilityAdminSummary.stats.totalRecords,
              detail: <>{props.perceptibilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, usage events, and meter usage reports'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.perceptibilityAdminSummary.records}
            availableActions={props.perceptibilityAdminSummary.availableActions}
            refreshAction="refresh_perceptibility_summary"
            actionBusy={props.perceptibilityAdminAction !== 'idle'}
            formatDomain={formatPerceptibilityDomain as (domain: string) => string}
            formatAdminAction={formatPerceptibilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handlePerceptibilityAdminAction('refresh_perceptibility_summary')
            }
          />
        ) : null}
        {props.scannabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Scannability admin"
            panelClassName="workspace-scannability-admin"
            listClassName="workspace-scannability-list"
            cardClassName="workspace-scannability-card"
            role={props.scannabilityAdminSummary.role}
            guidance={props.scannabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Shield scan scannability',
              value: `${props.scannabilityAdminSummary.stats.scannabilityPercent}%`,
              detail: <>{props.scannabilityAdminSummary.stats.coveredDomains}/
                  {props.scannabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Scannability signals',
              value: props.scannabilityAdminSummary.stats.totalRecords,
              detail: <>{props.scannabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, shield scans, and provider credentials'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.scannabilityAdminSummary.records}
            availableActions={props.scannabilityAdminSummary.availableActions}
            refreshAction="refresh_scannability_summary"
            actionBusy={props.scannabilityAdminAction !== 'idle'}
            formatDomain={formatScannabilityDomain as (domain: string) => string}
            formatAdminAction={formatScannabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleScannabilityAdminAction('refresh_scannability_summary')
            }
          />
        ) : null}
        {props.interpretabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Interpretability admin"
            panelClassName="workspace-interpretability-admin"
            listClassName="workspace-interpretability-list"
            cardClassName="workspace-interpretability-card"
            role={props.interpretabilityAdminSummary.role}
            guidance={props.interpretabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Agent output interpretability',
              value: `${props.interpretabilityAdminSummary.stats.interpretabilityPercent}%`,
              detail: <>{props.interpretabilityAdminSummary.stats.coveredDomains}/
                  {props.interpretabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Interpretability signals',
              value: props.interpretabilityAdminSummary.stats.totalRecords,
              detail: <>{props.interpretabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, agent outputs, and moderator syntheses'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.interpretabilityAdminSummary.records}
            availableActions={props.interpretabilityAdminSummary.availableActions}
            refreshAction="refresh_interpretability_summary"
            actionBusy={props.interpretabilityAdminAction !== 'idle'}
            formatDomain={formatInterpretabilityDomain as (domain: string) => string}
            formatAdminAction={formatInterpretabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleInterpretabilityAdminAction('refresh_interpretability_summary')
            }
          />
        ) : null}
        {props.recognizabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Recognizability admin"
            panelClassName="workspace-recognizability-admin"
            listClassName="workspace-recognizability-list"
            cardClassName="workspace-recognizability-card"
            role={props.recognizabilityAdminSummary.role}
            guidance={props.recognizabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Artifact recognizability',
              value: `${props.recognizabilityAdminSummary.stats.recognizabilityPercent}%`,
              detail: <>{props.recognizabilityAdminSummary.stats.coveredDomains}/
                  {props.recognizabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Recognizability signals',
              value: props.recognizabilityAdminSummary.stats.totalRecords,
              detail: <>{props.recognizabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, artifacts, and run workflows'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.recognizabilityAdminSummary.records}
            availableActions={props.recognizabilityAdminSummary.availableActions}
            refreshAction="refresh_recognizability_summary"
            actionBusy={props.recognizabilityAdminAction !== 'idle'}
            formatDomain={formatRecognizabilityDomain as (domain: string) => string}
            formatAdminAction={formatRecognizabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleRecognizabilityAdminAction('refresh_recognizability_summary')
            }
          />
        ) : null}
        {props.describabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Describability admin"
            panelClassName="workspace-describability-admin"
            listClassName="workspace-describability-list"
            cardClassName="workspace-describability-card"
            role={props.describabilityAdminSummary.role}
            guidance={props.describabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Workflow describability',
              value: `${props.describabilityAdminSummary.stats.describabilityPercent}%`,
              detail: <>{props.describabilityAdminSummary.stats.coveredDomains}/
                  {props.describabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Describability signals',
              value: props.describabilityAdminSummary.stats.totalRecords,
              detail: <>{props.describabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, run workflows, and agent outputs'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.describabilityAdminSummary.records}
            availableActions={props.describabilityAdminSummary.availableActions}
            refreshAction="refresh_describability_summary"
            actionBusy={props.describabilityAdminAction !== 'idle'}
            formatDomain={formatDescribabilityDomain as (domain: string) => string}
            formatAdminAction={formatDescribabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleDescribabilityAdminAction('refresh_describability_summary')
            }
          />
        ) : null}
        {props.detectabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Detectability admin"
            panelClassName="workspace-detectability-admin"
            listClassName="workspace-detectability-list"
            cardClassName="workspace-detectability-card"
            role={props.detectabilityAdminSummary.role}
            guidance={props.detectabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Billing webhook detectability',
              value: `${props.detectabilityAdminSummary.stats.detectabilityPercent}%`,
              detail: <>{props.detectabilityAdminSummary.stats.coveredDomains}/
                  {props.detectabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Detectability signals',
              value: props.detectabilityAdminSummary.stats.totalRecords,
              detail: <>{props.detectabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, billing webhook events, and billing notifications'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.detectabilityAdminSummary.records}
            availableActions={props.detectabilityAdminSummary.availableActions}
            refreshAction="refresh_detectability_summary"
            actionBusy={props.detectabilityAdminAction !== 'idle'}
            formatDomain={formatDetectabilityDomain as (domain: string) => string}
            formatAdminAction={formatDetectabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleDetectabilityAdminAction('refresh_detectability_summary')
            }
          />
        ) : null}
        {props.conspicuousnessAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Conspicuousness admin"
            panelClassName="workspace-conspicuousness-admin"
            listClassName="workspace-conspicuousness-list"
            cardClassName="workspace-conspicuousness-card"
            role={props.conspicuousnessAdminSummary.role}
            guidance={props.conspicuousnessAdminSummary.guidance}
            stats={[
            {
              label: 'Membership conspicuousness',
              value: `${props.conspicuousnessAdminSummary.stats.conspicuousnessPercent}%`,
              detail: <>{props.conspicuousnessAdminSummary.stats.coveredDomains}/
                  {props.conspicuousnessAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Conspicuousness signals',
              value: props.conspicuousnessAdminSummary.stats.totalRecords,
              detail: <>{props.conspicuousnessAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, workspace memberships, and usage events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.conspicuousnessAdminSummary.records}
            availableActions={props.conspicuousnessAdminSummary.availableActions}
            refreshAction="refresh_conspicuousness_summary"
            actionBusy={props.conspicuousnessAdminAction !== 'idle'}
            formatDomain={formatConspicuousnessDomain as (domain: string) => string}
            formatAdminAction={formatConspicuousnessAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleConspicuousnessAdminAction('refresh_conspicuousness_summary')
            }
          />
        ) : null}
        {props.distinctivenessAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Distinctiveness admin"
            panelClassName="workspace-distinctiveness-admin"
            listClassName="workspace-distinctiveness-list"
            cardClassName="workspace-distinctiveness-card"
            role={props.distinctivenessAdminSummary.role}
            guidance={props.distinctivenessAdminSummary.guidance}
            stats={[
            {
              label: 'Idempotency key distinctiveness',
              value: `${props.distinctivenessAdminSummary.stats.distinctivenessPercent}%`,
              detail: <>{props.distinctivenessAdminSummary.stats.coveredDomains}/
                  {props.distinctivenessAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Distinctiveness signals',
              value: props.distinctivenessAdminSummary.stats.totalRecords,
              detail: <>{props.distinctivenessAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, idempotency keys, and usage events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.distinctivenessAdminSummary.records}
            availableActions={props.distinctivenessAdminSummary.availableActions}
            refreshAction="refresh_distinctiveness_summary"
            actionBusy={props.distinctivenessAdminAction !== 'idle'}
            formatDomain={formatDistinctivenessDomain as (domain: string) => string}
            formatAdminAction={formatDistinctivenessAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleDistinctivenessAdminAction('refresh_distinctiveness_summary')
            }
          />
        ) : null}
        {props.discernibilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Discernibility admin"
            panelClassName="workspace-discernibility-admin"
            listClassName="workspace-discernibility-list"
            cardClassName="workspace-discernibility-card"
            role={props.discernibilityAdminSummary.role}
            guidance={props.discernibilityAdminSummary.guidance}
            stats={[
            {
              label: 'Moderator synthesis discernibility',
              value: `${props.discernibilityAdminSummary.stats.discernibilityPercent}%`,
              detail: <>{props.discernibilityAdminSummary.stats.coveredDomains}/
                  {props.discernibilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Discernibility signals',
              value: props.discernibilityAdminSummary.stats.totalRecords,
              detail: <>{props.discernibilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, moderator syntheses, and agent outputs'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.discernibilityAdminSummary.records}
            availableActions={props.discernibilityAdminSummary.availableActions}
            refreshAction="refresh_discernibility_summary"
            actionBusy={props.discernibilityAdminAction !== 'idle'}
            formatDomain={formatDiscernibilityDomain as (domain: string) => string}
            formatAdminAction={formatDiscernibilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleDiscernibilityAdminAction('refresh_discernibility_summary')
            }
          />
        ) : null}
        {props.representabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Representability admin"
            panelClassName="workspace-representability-admin"
            listClassName="workspace-representability-list"
            cardClassName="workspace-representability-card"
            role={props.representabilityAdminSummary.role}
            guidance={props.representabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Billing invoice representability',
              value: `${props.representabilityAdminSummary.stats.representabilityPercent}%`,
              detail: <>{props.representabilityAdminSummary.stats.coveredDomains}/
                  {props.representabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Representability signals',
              value: props.representabilityAdminSummary.stats.totalRecords,
              detail: <>{props.representabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, billing invoices, and billing records'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.representabilityAdminSummary.records}
            availableActions={props.representabilityAdminSummary.availableActions}
            refreshAction="refresh_representability_summary"
            actionBusy={props.representabilityAdminAction !== 'idle'}
            formatDomain={formatRepresentabilityDomain as (domain: string) => string}
            formatAdminAction={formatRepresentabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleRepresentabilityAdminAction('refresh_representability_summary')
            }
          />
        ) : null}
        {props.elaboratabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Elaboratability admin"
            panelClassName="workspace-elaboratability-admin"
            listClassName="workspace-elaboratability-list"
            cardClassName="workspace-elaboratability-card"
            role={props.elaboratabilityAdminSummary.role}
            guidance={props.elaboratabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Workflow elaboratability',
              value: `${props.elaboratabilityAdminSummary.stats.elaboratabilityPercent}%`,
              detail: <>{props.elaboratabilityAdminSummary.stats.coveredDomains}/
                  {props.elaboratabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Elaboratability signals',
              value: props.elaboratabilityAdminSummary.stats.totalRecords,
              detail: <>{props.elaboratabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, run workflows, and agent outputs'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.elaboratabilityAdminSummary.records}
            availableActions={props.elaboratabilityAdminSummary.availableActions}
            refreshAction="refresh_elaboratability_summary"
            actionBusy={props.elaboratabilityAdminAction !== 'idle'}
            formatDomain={formatElaboratabilityDomain as (domain: string) => string}
            formatAdminAction={formatElaboratabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleElaboratabilityAdminAction('refresh_elaboratability_summary')
            }
          />
        ) : null}
        {props.articulabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Articulability admin"
            panelClassName="workspace-articulability-admin"
            listClassName="workspace-articulability-list"
            cardClassName="workspace-articulability-card"
            role={props.articulabilityAdminSummary.role}
            guidance={props.articulabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Artifact articulability',
              value: `${props.articulabilityAdminSummary.stats.articulabilityPercent}%`,
              detail: <>{props.articulabilityAdminSummary.stats.coveredDomains}/
                  {props.articulabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Articulability signals',
              value: props.articulabilityAdminSummary.stats.totalRecords,
              detail: <>{props.articulabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, artifacts, and run workflows'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.articulabilityAdminSummary.records}
            availableActions={props.articulabilityAdminSummary.availableActions}
            refreshAction="refresh_articulability_summary"
            actionBusy={props.articulabilityAdminAction !== 'idle'}
            formatDomain={formatArticulabilityDomain as (domain: string) => string}
            formatAdminAction={formatArticulabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleArticulabilityAdminAction('refresh_articulability_summary')
            }
          />
        ) : null}
        {props.communicabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Communicability admin"
            panelClassName="workspace-communicability-admin"
            listClassName="workspace-communicability-list"
            cardClassName="workspace-communicability-card"
            role={props.communicabilityAdminSummary.role}
            guidance={props.communicabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Moderator synthesis communicability',
              value: `${props.communicabilityAdminSummary.stats.communicabilityPercent}%`,
              detail: <>{props.communicabilityAdminSummary.stats.coveredDomains}/
                  {props.communicabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Communicability signals',
              value: props.communicabilityAdminSummary.stats.totalRecords,
              detail: <>{props.communicabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, moderator syntheses, and agent outputs'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.communicabilityAdminSummary.records}
            availableActions={props.communicabilityAdminSummary.availableActions}
            refreshAction="refresh_communicability_summary"
            actionBusy={props.communicabilityAdminAction !== 'idle'}
            formatDomain={formatCommunicabilityDomain as (domain: string) => string}
            formatAdminAction={formatCommunicabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleCommunicabilityAdminAction('refresh_communicability_summary')
            }
          />
        ) : null}
        {props.expressivenessAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Expressiveness admin"
            panelClassName="workspace-expressiveness-admin"
            listClassName="workspace-expressiveness-list"
            cardClassName="workspace-expressiveness-card"
            role={props.expressivenessAdminSummary.role}
            guidance={props.expressivenessAdminSummary.guidance}
            stats={[
            {
              label: 'Agent output expressiveness',
              value: `${props.expressivenessAdminSummary.stats.expressivenessPercent}%`,
              detail: <>{props.expressivenessAdminSummary.stats.coveredDomains}/
                  {props.expressivenessAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Expressiveness signals',
              value: props.expressivenessAdminSummary.stats.totalRecords,
              detail: <>{props.expressivenessAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, agent outputs, and moderator syntheses'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.expressivenessAdminSummary.records}
            availableActions={props.expressivenessAdminSummary.availableActions}
            refreshAction="refresh_expressiveness_summary"
            actionBusy={props.expressivenessAdminAction !== 'idle'}
            formatDomain={formatExpressivenessDomain as (domain: string) => string}
            formatAdminAction={formatExpressivenessAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleExpressivenessAdminAction('refresh_expressiveness_summary')
            }
          />
        ) : null}
        {props.illustratabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Illustratability admin"
            panelClassName="workspace-illustratability-admin"
            listClassName="workspace-illustratability-list"
            cardClassName="workspace-illustratability-card"
            role={props.illustratabilityAdminSummary.role}
            guidance={props.illustratabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Shield scan illustratability',
              value: `${props.illustratabilityAdminSummary.stats.illustratabilityPercent}%`,
              detail: <>{props.illustratabilityAdminSummary.stats.coveredDomains}/
                  {props.illustratabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Illustratability signals',
              value: props.illustratabilityAdminSummary.stats.totalRecords,
              detail: <>{props.illustratabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, shield scans, and provider credentials'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.illustratabilityAdminSummary.records}
            availableActions={props.illustratabilityAdminSummary.availableActions}
            refreshAction="refresh_illustratability_summary"
            actionBusy={props.illustratabilityAdminAction !== 'idle'}
            formatDomain={formatIllustratabilityDomain as (domain: string) => string}
            formatAdminAction={formatIllustratabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleIllustratabilityAdminAction('refresh_illustratability_summary')
            }
          />
        ) : null}
        {props.narratabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Narratability admin"
            panelClassName="workspace-narratability-admin"
            listClassName="workspace-narratability-list"
            cardClassName="workspace-narratability-card"
            role={props.narratabilityAdminSummary.role}
            guidance={props.narratabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Membership narratability',
              value: `${props.narratabilityAdminSummary.stats.narratabilityPercent}%`,
              detail: <>{props.narratabilityAdminSummary.stats.coveredDomains}/
                  {props.narratabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Narratability signals',
              value: props.narratabilityAdminSummary.stats.totalRecords,
              detail: <>{props.narratabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, workspace memberships, and usage events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.narratabilityAdminSummary.records}
            availableActions={props.narratabilityAdminSummary.availableActions}
            refreshAction="refresh_narratability_summary"
            actionBusy={props.narratabilityAdminAction !== 'idle'}
            formatDomain={formatNarratabilityDomain as (domain: string) => string}
            formatAdminAction={formatNarratabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleNarratabilityAdminAction('refresh_narratability_summary')
            }
          />
        ) : null}
        {props.formulatabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Formulatability admin"
            panelClassName="workspace-formulatability-admin"
            listClassName="workspace-formulatability-list"
            cardClassName="workspace-formulatability-card"
            role={props.formulatabilityAdminSummary.role}
            guidance={props.formulatabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Idempotency key formulatability',
              value: `${props.formulatabilityAdminSummary.stats.formulatabilityPercent}%`,
              detail: <>{props.formulatabilityAdminSummary.stats.coveredDomains}/
                  {props.formulatabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Formulatability signals',
              value: props.formulatabilityAdminSummary.stats.totalRecords,
              detail: <>{props.formulatabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, idempotency keys, and usage events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.formulatabilityAdminSummary.records}
            availableActions={props.formulatabilityAdminSummary.availableActions}
            refreshAction="refresh_formulatability_summary"
            actionBusy={props.formulatabilityAdminAction !== 'idle'}
            formatDomain={formatFormulatabilityDomain as (domain: string) => string}
            formatAdminAction={formatFormulatabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleFormulatabilityAdminAction('refresh_formulatability_summary')
            }
          />
        ) : null}
        {props.enunciabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Enunciability admin"
            panelClassName="workspace-enunciability-admin"
            listClassName="workspace-enunciability-list"
            cardClassName="workspace-enunciability-card"
            role={props.enunciabilityAdminSummary.role}
            guidance={props.enunciabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Billing notification enunciability',
              value: `${props.enunciabilityAdminSummary.stats.enunciabilityPercent}%`,
              detail: <>{props.enunciabilityAdminSummary.stats.coveredDomains}/
                  {props.enunciabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Enunciability signals',
              value: props.enunciabilityAdminSummary.stats.totalRecords,
              detail: <>{props.enunciabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, billing notifications, and billing webhook events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.enunciabilityAdminSummary.records}
            availableActions={props.enunciabilityAdminSummary.availableActions}
            refreshAction="refresh_enunciability_summary"
            actionBusy={props.enunciabilityAdminAction !== 'idle'}
            formatDomain={formatEnunciabilityDomain as (domain: string) => string}
            formatAdminAction={formatEnunciabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleEnunciabilityAdminAction('refresh_enunciability_summary')
            }
          />
        ) : null}
        {props.presentabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Presentability admin"
            panelClassName="workspace-presentability-admin"
            listClassName="workspace-presentability-list"
            cardClassName="workspace-presentability-card"
            role={props.presentabilityAdminSummary.role}
            guidance={props.presentabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Usage event presentability',
              value: `${props.presentabilityAdminSummary.stats.presentabilityPercent}%`,
              detail: <>{props.presentabilityAdminSummary.stats.coveredDomains}/
                  {props.presentabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Presentability signals',
              value: props.presentabilityAdminSummary.stats.totalRecords,
              detail: <>{props.presentabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, usage events, and meter usage reports'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.presentabilityAdminSummary.records}
            availableActions={props.presentabilityAdminSummary.availableActions}
            refreshAction="refresh_presentability_summary"
            actionBusy={props.presentabilityAdminAction !== 'idle'}
            formatDomain={formatPresentabilityDomain as (domain: string) => string}
            formatAdminAction={formatPresentabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handlePresentabilityAdminAction('refresh_presentability_summary')
            }
          />
        ) : null}
        {props.connotabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Connotability admin"
            panelClassName="workspace-connotability-admin"
            listClassName="workspace-connotability-list"
            cardClassName="workspace-connotability-card"
            role={props.connotabilityAdminSummary.role}
            guidance={props.connotabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Meter usage connotability',
              value: `${props.connotabilityAdminSummary.stats.connotabilityPercent}%`,
              detail: <>{props.connotabilityAdminSummary.stats.coveredDomains}/
                  {props.connotabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Connotability signals',
              value: props.connotabilityAdminSummary.stats.totalRecords,
              detail: <>{props.connotabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, meter usage reports, and usage events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.connotabilityAdminSummary.records}
            availableActions={props.connotabilityAdminSummary.availableActions}
            refreshAction="refresh_connotability_summary"
            actionBusy={props.connotabilityAdminAction !== 'idle'}
            formatDomain={formatConnotabilityDomain as (domain: string) => string}
            formatAdminAction={formatConnotabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleConnotabilityAdminAction('refresh_connotability_summary')
            }
          />
        ) : null}
        {props.signifiabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Signifiability admin"
            panelClassName="workspace-signifiability-admin"
            listClassName="workspace-signifiability-list"
            cardClassName="workspace-signifiability-card"
            role={props.signifiabilityAdminSummary.role}
            guidance={props.signifiabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Billing webhook signifiability',
              value: `${props.signifiabilityAdminSummary.stats.signifiabilityPercent}%`,
              detail: <>{props.signifiabilityAdminSummary.stats.coveredDomains}/
                  {props.signifiabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Signifiability signals',
              value: props.signifiabilityAdminSummary.stats.totalRecords,
              detail: <>{props.signifiabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, billing webhook events, and billing records'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.signifiabilityAdminSummary.records}
            availableActions={props.signifiabilityAdminSummary.availableActions}
            refreshAction="refresh_signifiability_summary"
            actionBusy={props.signifiabilityAdminAction !== 'idle'}
            formatDomain={formatSignifiabilityDomain as (domain: string) => string}
            formatAdminAction={formatSignifiabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleSignifiabilityAdminAction('refresh_signifiability_summary')
            }
          />
        ) : null}
        {props.evocatabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Evocatability admin"
            panelClassName="workspace-evocatability-admin"
            listClassName="workspace-evocatability-list"
            cardClassName="workspace-evocatability-card"
            role={props.evocatabilityAdminSummary.role}
            guidance={props.evocatabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Model health evocatability',
              value: `${props.evocatabilityAdminSummary.stats.evocatabilityPercent}%`,
              detail: <>{props.evocatabilityAdminSummary.stats.coveredDomains}/
                  {props.evocatabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Evocatability signals',
              value: props.evocatabilityAdminSummary.stats.totalRecords,
              detail: <>{props.evocatabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, model health events, and billing records'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.evocatabilityAdminSummary.records}
            availableActions={props.evocatabilityAdminSummary.availableActions}
            refreshAction="refresh_evocatability_summary"
            actionBusy={props.evocatabilityAdminAction !== 'idle'}
            formatDomain={formatEvocatabilityDomain as (domain: string) => string}
            formatAdminAction={formatEvocatabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleEvocatabilityAdminAction('refresh_evocatability_summary')
            }
          />
        ) : null}
        {props.visualizabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Visualizability admin"
            panelClassName="workspace-visualizability-admin"
            listClassName="workspace-visualizability-list"
            cardClassName="workspace-visualizability-card"
            role={props.visualizabilityAdminSummary.role}
            guidance={props.visualizabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Model registry visualizability',
              value: `${props.visualizabilityAdminSummary.stats.visualizabilityPercent}%`,
              detail: <>{props.visualizabilityAdminSummary.stats.coveredDomains}/
                  {props.visualizabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Visualizability signals',
              value: props.visualizabilityAdminSummary.stats.totalRecords,
              detail: <>{props.visualizabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, model registry entries, and model health events'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.visualizabilityAdminSummary.records}
            availableActions={props.visualizabilityAdminSummary.availableActions}
            refreshAction="refresh_visualizability_summary"
            actionBusy={props.visualizabilityAdminAction !== 'idle'}
            formatDomain={formatVisualizabilityDomain as (domain: string) => string}
            formatAdminAction={formatVisualizabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleVisualizabilityAdminAction('refresh_visualizability_summary')
            }
          />
        ) : null}
        {props.symbolizabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Symbolizability admin"
            panelClassName="workspace-symbolizability-admin"
            listClassName="workspace-symbolizability-list"
            cardClassName="workspace-symbolizability-card"
            role={props.symbolizabilityAdminSummary.role}
            guidance={props.symbolizabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Billing record symbolizability',
              value: `${props.symbolizabilityAdminSummary.stats.symbolizabilityPercent}%`,
              detail: <>{props.symbolizabilityAdminSummary.stats.coveredDomains}/
                  {props.symbolizabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Symbolizability signals',
              value: props.symbolizabilityAdminSummary.stats.totalRecords,
              detail: <>{props.symbolizabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, billing records, and billing invoices'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.symbolizabilityAdminSummary.records}
            availableActions={props.symbolizabilityAdminSummary.availableActions}
            refreshAction="refresh_symbolizability_summary"
            actionBusy={props.symbolizabilityAdminAction !== 'idle'}
            formatDomain={formatSymbolizabilityDomain as (domain: string) => string}
            formatAdminAction={formatSymbolizabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleSymbolizabilityAdminAction('refresh_symbolizability_summary')
            }
          />
        ) : null}
        {props.materializabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Materializability admin"
            panelClassName="workspace-materializability-admin"
            listClassName="workspace-materializability-list"
            cardClassName="workspace-materializability-card"
            role={props.materializabilityAdminSummary.role}
            guidance={props.materializabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Workflow materializability',
              value: `${props.materializabilityAdminSummary.stats.materializabilityPercent}%`,
              detail: <>{props.materializabilityAdminSummary.stats.coveredDomains}/
                  {props.materializabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Materializability signals',
              value: props.materializabilityAdminSummary.stats.totalRecords,
              detail: <>{props.materializabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, run workflows, and artifacts'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.materializabilityAdminSummary.records}
            availableActions={props.materializabilityAdminSummary.availableActions}
            refreshAction="refresh_materializability_summary"
            actionBusy={props.materializabilityAdminAction !== 'idle'}
            formatDomain={formatMaterializabilityDomain as (domain: string) => string}
            formatAdminAction={formatMaterializabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handleMaterializabilityAdminAction('refresh_materializability_summary')
            }
          />
        ) : null}
        {props.personifiabilityAdminSummary ? (
          <DomainCoverageAdminPanel
            title="Personifiability admin"
            panelClassName="workspace-personifiability-admin"
            listClassName="workspace-personifiability-list"
            cardClassName="workspace-personifiability-card"
            role={props.personifiabilityAdminSummary.role}
            guidance={props.personifiabilityAdminSummary.guidance}
            stats={[
            {
              label: 'Agent output personifiability',
              value: `${props.personifiabilityAdminSummary.stats.personifiabilityPercent}%`,
              detail: <>{props.personifiabilityAdminSummary.stats.coveredDomains}/
                  {props.personifiabilityAdminSummary.stats.totalDomains} domains covered</>,
            },
            {
              label: 'Personifiability signals',
              value: props.personifiabilityAdminSummary.stats.totalRecords,
              detail: <>{props.personifiabilityAdminSummary.stats.postgresConnectivity
                    ? 'Run outcomes, agent outputs, and moderator syntheses'
                    : 'PostgreSQL unavailable'}</>,
            }
            ]}
            records={props.personifiabilityAdminSummary.records}
            availableActions={props.personifiabilityAdminSummary.availableActions}
            refreshAction="refresh_personifiability_summary"
            actionBusy={props.personifiabilityAdminAction !== 'idle'}
            formatDomain={formatPersonifiabilityDomain as (domain: string) => string}
            formatAdminAction={formatPersonifiabilityAdminAction as (action: string) => string}
            onRefresh={() =>
              void props.handlePersonifiabilityAdminAction('refresh_personifiability_summary')
            }
          />
        ) : null}
    </>
  )
}
