export const milestones = [
  {
    name: 'viability',
    Name: 'Viability',
    percent: 'viabilityPercent',
    metricDomain: 'billing_invoices',
    metricLabel: 'Billing invoice viability',
    metricDetail: 'billing invoice viability',
    action: 'refresh_viability_summary',
    tables: ['billing_invoices', 'billing_records', 'billing_notifications'],
    check1: {
      key: 'billingInvoicesTableExists',
      name: 'billing_invoice_viability',
      label: 'Billing invoice viability',
      table: 'billing_invoices',
    },
    check2: {
      key: 'billingRecordsTableExists',
      name: 'billing_record_viability',
      label: 'Billing record viability',
      table: 'billing_records',
    },
    check3Key: 'billingNotificationsTableExists',
    coverageKeys: {
      billing_invoices: 'billingInvoicesTableExists',
      billing_records: 'billingRecordsTableExists',
      billing_notifications: 'billingNotificationsTableExists',
    },
    readiness: 'viability_readiness_signal',
    readinessLabel: 'Viability readiness signal',
    readinessDetail:
      'Billing invoices, billing records, and billing notifications support viability readiness.',
    cap1: 'supportsBillingInvoiceViabilitySignals',
    cap2: 'supportsBillingRecordViabilitySignals',
    guidance:
      'Production viability rollout validates billing invoice viability, billing record viability signals, billing notification coverage, and viability readiness before production viability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'billing_invoices',
        tableName: 'billing_invoices',
        requiredTables: ['billing_invoices'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_invoices WHERE workspace_id = $1`,
      },
      {
        domain: 'billing_records',
        tableName: 'billing_records',
        requiredTables: ['billing_records'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_records WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      billing_invoices: 'Billing invoices',
      billing_records: 'Billing records',
    },
    signalsHint: 'Run outcomes, billing invoices, and billing records',
  },
  {
    name: 'feasibility',
    Name: 'Feasibility',
    percent: 'feasibilityPercent',
    metricDomain: 'workspace_provider_credentials',
    metricLabel: 'Provider credential feasibility',
    metricDetail: 'provider credential feasibility',
    action: 'refresh_feasibility_summary',
    tables: [
      'workspace_provider_credentials',
      'usage_events',
      'billing_meter_usage_reports',
    ],
    check1: {
      key: 'workspaceProviderCredentialsTableExists',
      name: 'provider_credential_feasibility',
      label: 'Provider credential feasibility',
      table: 'workspace_provider_credentials',
    },
    check2: {
      key: 'usageEventsTableExists',
      name: 'usage_event_feasibility',
      label: 'Usage event feasibility',
      table: 'usage_events',
    },
    check3Key: 'billingMeterUsageReportsTableExists',
    coverageKeys: {
      workspace_provider_credentials: 'workspaceProviderCredentialsTableExists',
      usage_events: 'usageEventsTableExists',
      billing_meter_usage_reports: 'billingMeterUsageReportsTableExists',
    },
    readiness: 'feasibility_readiness_signal',
    readinessLabel: 'Feasibility readiness signal',
    readinessDetail:
      'Provider credentials, usage events, and meter usage reports support feasibility readiness.',
    cap1: 'supportsProviderCredentialFeasibilitySignals',
    cap2: 'supportsUsageEventFeasibilitySignals',
    guidance:
      'Production feasibility rollout validates provider credential feasibility, usage event feasibility signals, meter usage coverage, and feasibility readiness before production feasibility tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'workspace_provider_credentials',
        tableName: 'workspace_provider_credentials',
        requiredTables: ['workspace_provider_credentials'],
        sql: `SELECT COUNT(*)::text AS count FROM workspace_provider_credentials WHERE workspace_id = $1`,
      },
      {
        domain: 'usage_events',
        tableName: 'usage_events',
        requiredTables: ['usage_events'],
        sql: `SELECT COUNT(*)::text AS count FROM usage_events WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      workspace_provider_credentials: 'Provider credentials',
      usage_events: 'Usage events',
    },
    signalsHint: 'Run outcomes, provider credentials, and usage events',
  },
  {
    name: 'conformance',
    Name: 'Conformance',
    percent: 'conformancePercent',
    metricDomain: 'shield_scans',
    metricLabel: 'Shield scan conformance',
    metricDetail: 'shield scan conformance',
    action: 'refresh_conformance_summary',
    tables: ['shield_scans', 'billing_webhook_events', 'idempotency_keys'],
    check1: {
      key: 'shieldScansTableExists',
      name: 'shield_scan_conformance',
      label: 'Shield scan conformance',
      table: 'shield_scans',
    },
    check2: {
      key: 'billingWebhookEventsTableExists',
      name: 'billing_webhook_conformance',
      label: 'Billing webhook conformance',
      table: 'billing_webhook_events',
    },
    check3Key: 'idempotencyKeysTableExists',
    coverageKeys: {
      shield_scans: 'shieldScansTableExists',
      billing_webhook_events: 'billingWebhookEventsTableExists',
      idempotency_keys: 'idempotencyKeysTableExists',
    },
    readiness: 'conformance_readiness_signal',
    readinessLabel: 'Conformance readiness signal',
    readinessDetail:
      'Shield scans, billing webhook events, and idempotency keys support conformance readiness.',
    cap1: 'supportsShieldScanConformanceSignals',
    cap2: 'supportsBillingWebhookConformanceSignals',
    guidance:
      'Production conformance rollout validates shield scan conformance, billing webhook conformance signals, idempotency key coverage, and conformance readiness before production conformance tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'shield_scans',
        tableName: 'shield_scans',
        requiredTables: ['shield_scans', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM shield_scans ss INNER JOIN runs r ON r.run_id = ss.run_id WHERE r.workspace_id = $1`,
      },
      {
        domain: 'billing_webhook_events',
        tableName: 'billing_webhook_events',
        requiredTables: ['billing_webhook_events'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_webhook_events WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      shield_scans: 'Shield scans',
      billing_webhook_events: 'Billing webhook events',
    },
    signalsHint: 'Run outcomes, shield scans, and billing webhook events',
  },
  {
    name: 'adoptability',
    Name: 'Adoptability',
    percent: 'adoptabilityPercent',
    metricDomain: 'usage_events',
    metricLabel: 'Usage event adoptability',
    metricDetail: 'usage event adoptability',
    action: 'refresh_adoptability_summary',
    tables: ['usage_events', 'workspace_memberships', 'billing_notifications'],
    check1: {
      key: 'usageEventsTableExists',
      name: 'usage_event_adoptability',
      label: 'Usage event adoptability',
      table: 'usage_events',
    },
    check2: {
      key: 'workspaceMembershipsTableExists',
      name: 'membership_adoptability',
      label: 'Membership adoptability',
      table: 'workspace_memberships',
    },
    check3Key: 'billingNotificationsTableExists',
    coverageKeys: {
      usage_events: 'usageEventsTableExists',
      workspace_memberships: 'workspaceMembershipsTableExists',
      billing_notifications: 'billingNotificationsTableExists',
    },
    readiness: 'adoption_readiness_signal',
    readinessLabel: 'Adoption readiness signal',
    readinessDetail:
      'Usage events, workspace memberships, and billing notifications support adoption readiness.',
    cap1: 'supportsUsageEventAdoptabilitySignals',
    cap2: 'supportsMembershipAdoptabilitySignals',
    guidance:
      'Production adoptability rollout validates usage event adoptability, membership adoptability signals, billing notification coverage, and adoption readiness before production adoptability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'usage_events',
        tableName: 'usage_events',
        requiredTables: ['usage_events'],
        sql: `SELECT COUNT(*)::text AS count FROM usage_events WHERE workspace_id = $1`,
      },
      {
        domain: 'workspace_memberships',
        tableName: 'workspace_memberships',
        requiredTables: ['workspace_memberships'],
        sql: `SELECT COUNT(*)::text AS count FROM workspace_memberships WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      usage_events: 'Usage events',
      workspace_memberships: 'Workspace memberships',
    },
    signalsHint: 'Run outcomes, usage events, and workspace memberships',
  },
  {
    name: 'acceptability',
    Name: 'Acceptability',
    percent: 'acceptabilityPercent',
    metricDomain: 'billing_records',
    metricLabel: 'Billing record acceptability',
    metricDetail: 'billing record acceptability',
    action: 'refresh_acceptability_summary',
    tables: [
      'billing_records',
      'billing_invoices',
      'workspace_usage_limits',
    ],
    check1: {
      key: 'billingRecordsTableExists',
      name: 'billing_record_acceptability',
      label: 'Billing record acceptability',
      table: 'billing_records',
    },
    check2: {
      key: 'billingInvoicesTableExists',
      name: 'billing_invoice_acceptability',
      label: 'Billing invoice acceptability',
      table: 'billing_invoices',
    },
    check3Key: 'workspaceUsageLimitsTableExists',
    coverageKeys: {
      billing_records: 'billingRecordsTableExists',
      billing_invoices: 'billingInvoicesTableExists',
      workspace_usage_limits: 'workspaceUsageLimitsTableExists',
    },
    readiness: 'acceptance_readiness_signal',
    readinessLabel: 'Acceptance readiness signal',
    readinessDetail:
      'Billing records, billing invoices, and workspace usage limits support acceptance readiness.',
    cap1: 'supportsBillingRecordAcceptabilitySignals',
    cap2: 'supportsBillingInvoiceAcceptabilitySignals',
    guidance:
      'Production acceptability rollout validates billing record acceptability, billing invoice acceptability signals, workspace limit coverage, and acceptance readiness before production acceptability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'billing_records',
        tableName: 'billing_records',
        requiredTables: ['billing_records'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_records WHERE workspace_id = $1`,
      },
      {
        domain: 'billing_invoices',
        tableName: 'billing_invoices',
        requiredTables: ['billing_invoices'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_invoices WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      billing_records: 'Billing records',
      billing_invoices: 'Billing invoices',
    },
    signalsHint: 'Run outcomes, billing records, and billing invoices',
  },
]

export const patchMeta = milestones.map((m, index) => ({
  ...m,
  version: `v5.${126 + index}`,
  domainFormatter: `format${m.Name}Domain`,
}))
