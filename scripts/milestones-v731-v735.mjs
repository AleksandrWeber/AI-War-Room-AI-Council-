export const milestones = [
  {
    name: 'bibliographizability',
    Name: 'Bibliographizability',
    percent: 'bibliographizabilityPercent',
    metricDomain: 'shield_scans',
    metricLabel: 'Shield scan bibliographizability',
    metricDetail: 'shield scan bibliographizability',
    action: 'refresh_bibliographizability_summary',
    tables: [
      'shield_scans',
      'workspace_provider_credentials',
      'billing_webhook_events',
    ],
    check1: {
      key: 'shieldScansTableExists',
      name: 'shield_scan_bibliographizability',
      label: 'Shield scan bibliographizability',
      table: 'shield_scans',
    },
    check2: {
      key: 'workspaceProviderCredentialsTableExists',
      name: 'provider_credential_bibliographizability',
      label: 'Provider credential bibliographizability',
      table: 'workspace_provider_credentials',
    },
    check3Key: 'billingWebhookEventsTableExists',
    coverageKeys: {
      shield_scans: 'shieldScansTableExists',
      workspace_provider_credentials: 'workspaceProviderCredentialsTableExists',
      billing_webhook_events: 'billingWebhookEventsTableExists',
    },
    readiness: 'bibliographization_readiness_signal',
    readinessLabel: 'Bibliographization readiness signal',
    readinessDetail:
      'Shield scans, workspace provider credentials, and billing webhook events support bibliographization readiness.',
    cap1: 'supportsShieldScanBibliographizabilitySignals',
    cap2: 'supportsProviderCredentialBibliographizabilitySignals',
    guidance:
      'Production bibliographizability rollout validates shield scan bibliographizability, provider credential bibliographizability signals, billing webhook coverage, and bibliographization readiness before production bibliographizability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'shield_scans',
        tableName: 'shield_scans',
        requiredTables: ['shield_scans', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM shield_scans ss INNER JOIN runs r ON r.run_id = ss.run_id WHERE r.workspace_id = $1`,
      },
      {
        domain: 'workspace_provider_credentials',
        tableName: 'workspace_provider_credentials',
        requiredTables: ['workspace_provider_credentials'],
        sql: `SELECT COUNT(*)::text AS count FROM workspace_provider_credentials WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      shield_scans: 'Shield scans',
      workspace_provider_credentials: 'Provider credentials',
    },
    signalsHint: 'Run outcomes, shield scans, and provider credentials',
  },
  {
    name: 'referencizability',
    Name: 'Referencizability',
    percent: 'referencizabilityPercent',
    metricDomain: 'idempotency_keys',
    metricLabel: 'Idempotency key referencizability',
    metricDetail: 'idempotency key referencizability',
    action: 'refresh_referencizability_summary',
    tables: ['idempotency_keys', 'usage_events', 'billing_webhook_events'],
    check1: {
      key: 'idempotencyKeysTableExists',
      name: 'idempotency_key_referencizability',
      label: 'Idempotency key referencizability',
      table: 'idempotency_keys',
    },
    check2: {
      key: 'usageEventsTableExists',
      name: 'usage_event_referencizability',
      label: 'Usage event referencizability',
      table: 'usage_events',
    },
    check3Key: 'billingWebhookEventsTableExists',
    coverageKeys: {
      idempotency_keys: 'idempotencyKeysTableExists',
      usage_events: 'usageEventsTableExists',
      billing_webhook_events: 'billingWebhookEventsTableExists',
    },
    readiness: 'referencization_readiness_signal',
    readinessLabel: 'Referencization readiness signal',
    readinessDetail:
      'Idempotency keys, usage events, and billing webhook events support referencization readiness.',
    cap1: 'supportsIdempotencyKeyReferencizabilitySignals',
    cap2: 'supportsUsageEventReferencizabilitySignals',
    guidance:
      'Production referencizability rollout validates idempotency key referencizability, usage event referencizability signals, billing webhook coverage, and referencization readiness before production referencizability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'idempotency_keys',
        tableName: 'idempotency_keys',
        requiredTables: ['idempotency_keys'],
        sql: `SELECT COUNT(*)::text AS count FROM idempotency_keys WHERE workspace_id = $1`,
      },
      {
        domain: 'usage_events',
        tableName: 'usage_events',
        requiredTables: ['usage_events'],
        sql: `SELECT COUNT(*)::text AS count FROM usage_events WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      idempotency_keys: 'Idempotency keys',
      usage_events: 'Usage events',
    },
    signalsHint: 'Run outcomes, idempotency keys, and usage events',
  },
  {
    name: 'documentizability',
    Name: 'Documentizability',
    percent: 'documentizabilityPercent',
    metricDomain: 'workspace_memberships',
    metricLabel: 'Membership documentizability',
    metricDetail: 'membership documentizability',
    action: 'refresh_documentizability_summary',
    tables: ['workspace_memberships', 'usage_events', 'billing_notifications'],
    check1: {
      key: 'workspaceMembershipsTableExists',
      name: 'membership_documentizability',
      label: 'Membership documentizability',
      table: 'workspace_memberships',
    },
    check2: {
      key: 'usageEventsTableExists',
      name: 'usage_event_documentizability',
      label: 'Usage event documentizability',
      table: 'usage_events',
    },
    check3Key: 'billingNotificationsTableExists',
    coverageKeys: {
      workspace_memberships: 'workspaceMembershipsTableExists',
      usage_events: 'usageEventsTableExists',
      billing_notifications: 'billingNotificationsTableExists',
    },
    readiness: 'documentization_readiness_signal',
    readinessLabel: 'Documentization readiness signal',
    readinessDetail:
      'Workspace memberships, usage events, and billing notifications support documentization readiness.',
    cap1: 'supportsMembershipDocumentizabilitySignals',
    cap2: 'supportsUsageEventDocumentizabilitySignals',
    guidance:
      'Production documentizability rollout validates membership documentizability, usage event documentizability signals, billing notification coverage, and documentization readiness before production documentizability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'workspace_memberships',
        tableName: 'workspace_memberships',
        requiredTables: ['workspace_memberships'],
        sql: `SELECT COUNT(*)::text AS count FROM workspace_memberships WHERE workspace_id = $1`,
      },
      {
        domain: 'usage_events',
        tableName: 'usage_events',
        requiredTables: ['usage_events'],
        sql: `SELECT COUNT(*)::text AS count FROM usage_events WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      workspace_memberships: 'Workspace memberships',
      usage_events: 'Usage events',
    },
    signalsHint: 'Run outcomes, workspace memberships, and usage events',
  },
  {
    name: 'annotationizability',
    Name: 'Annotationizability',
    percent: 'annotationizabilityPercent',
    metricDomain: 'billing_invoices',
    metricLabel: 'Billing invoice annotationizability',
    metricDetail: 'billing invoice annotationizability',
    action: 'refresh_annotationizability_summary',
    tables: ['billing_invoices', 'billing_records', 'billing_webhook_events'],
    check1: {
      key: 'billingInvoicesTableExists',
      name: 'billing_invoice_annotationizability',
      label: 'Billing invoice annotationizability',
      table: 'billing_invoices',
    },
    check2: {
      key: 'billingRecordsTableExists',
      name: 'billing_record_annotationizability',
      label: 'Billing record annotationizability',
      table: 'billing_records',
    },
    check3Key: 'billingWebhookEventsTableExists',
    coverageKeys: {
      billing_invoices: 'billingInvoicesTableExists',
      billing_records: 'billingRecordsTableExists',
      billing_webhook_events: 'billingWebhookEventsTableExists',
    },
    readiness: 'annotationization_readiness_signal',
    readinessLabel: 'Annotationization readiness signal',
    readinessDetail:
      'Billing invoices, billing records, and billing webhook events support annotationization readiness.',
    cap1: 'supportsBillingInvoiceAnnotationizabilitySignals',
    cap2: 'supportsBillingRecordAnnotationizabilitySignals',
    guidance:
      'Production annotationizability rollout validates billing invoice annotationizability, billing record annotationizability signals, billing webhook coverage, and annotationization readiness before production annotationizability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'billing_invoices',
        tableName: 'billing_invoices',
        requiredTables: ['billing_invoices'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_invoices WHERE workspace_id = $1`,
      },
      {
        domain: 'billing_records',
        tableName: 'billing_records',
        requiredTables: ['billing_records'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_records WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      billing_invoices: 'Billing invoices',
      billing_records: 'Billing records',
    },
    signalsHint: 'Run outcomes, billing invoices, and billing records',
  },
  {
    name: 'citationizability',
    Name: 'Citationizability',
    percent: 'citationizabilityPercent',
    metricDomain: 'billing_notifications',
    metricLabel: 'Billing notification citationizability',
    metricDetail: 'billing notification citationizability',
    action: 'refresh_citationizability_summary',
    tables: ['billing_notifications', 'billing_webhook_events', 'usage_events'],
    check1: {
      key: 'billingNotificationsTableExists',
      name: 'billing_notification_citationizability',
      label: 'Billing notification citationizability',
      table: 'billing_notifications',
    },
    check2: {
      key: 'billingWebhookEventsTableExists',
      name: 'billing_webhook_citationizability',
      label: 'Billing webhook citationizability',
      table: 'billing_webhook_events',
    },
    check3Key: 'usageEventsTableExists',
    coverageKeys: {
      billing_notifications: 'billingNotificationsTableExists',
      billing_webhook_events: 'billingWebhookEventsTableExists',
      usage_events: 'usageEventsTableExists',
    },
    readiness: 'citationization_readiness_signal',
    readinessLabel: 'Citationization readiness signal',
    readinessDetail:
      'Billing notifications, billing webhook events, and usage events support citationization readiness.',
    cap1: 'supportsBillingNotificationCitationizabilitySignals',
    cap2: 'supportsBillingWebhookCitationizabilitySignals',
    guidance:
      'Production citationizability rollout validates billing notification citationizability, billing webhook citationizability signals, usage event coverage, and citationization readiness before production citationizability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'billing_notifications',
        tableName: 'billing_notifications',
        requiredTables: ['billing_notifications'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_notifications WHERE workspace_id = $1`,
      },
      {
        domain: 'billing_webhook_events',
        tableName: 'billing_webhook_events',
        requiredTables: ['billing_webhook_events'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_webhook_events WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      billing_notifications: 'Billing notifications',
      billing_webhook_events: 'Billing webhook events',
    },
    signalsHint: 'Run outcomes, billing notifications, and billing webhook events',
  },
]

export const patchMeta = milestones.map((m, index) => ({
  ...m,
  version: `v5.${231 + index}`,
  domainFormatter: `format${m.Name}Domain`,
}))
