export const milestones = [
  {
    name: 'expressiveness',
    Name: 'Expressiveness',
    percent: 'expressivenessPercent',
    metricDomain: 'agent_outputs',
    metricLabel: 'Agent output expressiveness',
    metricDetail: 'agent output expressiveness',
    action: 'refresh_expressiveness_summary',
    tables: ['agent_outputs', 'moderator_syntheses', 'artifacts'],
    check1: {
      key: 'agentOutputsTableExists',
      name: 'agent_output_expressiveness',
      label: 'Agent output expressiveness',
      table: 'agent_outputs',
    },
    check2: {
      key: 'moderatorSynthesesTableExists',
      name: 'synthesis_expressiveness',
      label: 'Synthesis expressiveness',
      table: 'moderator_syntheses',
    },
    check3Key: 'artifactsTableExists',
    coverageKeys: {
      agent_outputs: 'agentOutputsTableExists',
      moderator_syntheses: 'moderatorSynthesesTableExists',
      artifacts: 'artifactsTableExists',
    },
    readiness: 'expression_readiness_signal',
    readinessLabel: 'Expression readiness signal',
    readinessDetail:
      'Agent outputs, moderator syntheses, and persisted artifacts support expression readiness.',
    cap1: 'supportsAgentOutputExpressivenessSignals',
    cap2: 'supportsSynthesisExpressivenessSignals',
    guidance:
      'Production expressiveness rollout validates agent output expressiveness, synthesis expressiveness signals, artifact coverage, and expression readiness before production expressiveness tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'agent_outputs',
        tableName: 'agent_outputs',
        requiredTables: ['agent_outputs', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM agent_outputs ao INNER JOIN runs r ON r.run_id = ao.run_id WHERE r.workspace_id = $1`,
      },
      {
        domain: 'moderator_syntheses',
        tableName: 'moderator_syntheses',
        requiredTables: ['moderator_syntheses', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM moderator_syntheses ms INNER JOIN runs r ON r.run_id = ms.run_id WHERE r.workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      agent_outputs: 'Agent outputs',
      moderator_syntheses: 'Moderator syntheses',
    },
    signalsHint: 'Run outcomes, agent outputs, and moderator syntheses',
  },
  {
    name: 'communicability',
    Name: 'Communicability',
    percent: 'communicabilityPercent',
    metricDomain: 'moderator_syntheses',
    metricLabel: 'Moderator synthesis communicability',
    metricDetail: 'moderator synthesis communicability',
    action: 'refresh_communicability_summary',
    tables: ['moderator_syntheses', 'agent_outputs', 'artifacts'],
    check1: {
      key: 'moderatorSynthesesTableExists',
      name: 'synthesis_communicability',
      label: 'Synthesis communicability',
      table: 'moderator_syntheses',
    },
    check2: {
      key: 'agentOutputsTableExists',
      name: 'agent_output_communicability',
      label: 'Agent output communicability',
      table: 'agent_outputs',
    },
    check3Key: 'artifactsTableExists',
    coverageKeys: {
      moderator_syntheses: 'moderatorSynthesesTableExists',
      agent_outputs: 'agentOutputsTableExists',
      artifacts: 'artifactsTableExists',
    },
    readiness: 'communication_readiness_signal',
    readinessLabel: 'Communication readiness signal',
    readinessDetail:
      'Moderator syntheses, agent outputs, and persisted artifacts support communication readiness.',
    cap1: 'supportsSynthesisCommunicabilitySignals',
    cap2: 'supportsAgentOutputCommunicabilitySignals',
    guidance:
      'Production communicability rollout validates synthesis communicability, agent output communicability signals, artifact coverage, and communication readiness before production communicability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'moderator_syntheses',
        tableName: 'moderator_syntheses',
        requiredTables: ['moderator_syntheses', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM moderator_syntheses ms INNER JOIN runs r ON r.run_id = ms.run_id WHERE r.workspace_id = $1`,
      },
      {
        domain: 'agent_outputs',
        tableName: 'agent_outputs',
        requiredTables: ['agent_outputs', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM agent_outputs ao INNER JOIN runs r ON r.run_id = ao.run_id WHERE r.workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      moderator_syntheses: 'Moderator syntheses',
      agent_outputs: 'Agent outputs',
    },
    signalsHint: 'Run outcomes, moderator syntheses, and agent outputs',
  },
  {
    name: 'articulability',
    Name: 'Articulability',
    percent: 'articulabilityPercent',
    metricDomain: 'artifacts',
    metricLabel: 'Artifact articulability',
    metricDetail: 'artifact articulability',
    action: 'refresh_articulability_summary',
    tables: ['artifacts', 'run_workflows', 'billing_notifications'],
    check1: {
      key: 'artifactsTableExists',
      name: 'artifact_articulability',
      label: 'Artifact articulability',
      table: 'artifacts',
    },
    check2: {
      key: 'runWorkflowsTableExists',
      name: 'workflow_articulability',
      label: 'Workflow articulability',
      table: 'run_workflows',
    },
    check3Key: 'billingNotificationsTableExists',
    coverageKeys: {
      artifacts: 'artifactsTableExists',
      run_workflows: 'runWorkflowsTableExists',
      billing_notifications: 'billingNotificationsTableExists',
    },
    readiness: 'articulation_readiness_signal',
    readinessLabel: 'Articulation readiness signal',
    readinessDetail:
      'Artifacts, run workflows, and billing notifications support articulation readiness.',
    cap1: 'supportsArtifactArticulabilitySignals',
    cap2: 'supportsWorkflowArticulabilitySignals',
    guidance:
      'Production articulability rollout validates artifact articulability, workflow articulability signals, billing notification coverage, and articulation readiness before production articulability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'artifacts',
        tableName: 'artifacts',
        requiredTables: ['artifacts'],
        sql: `SELECT COUNT(*)::text AS count FROM artifacts WHERE workspace_id = $1`,
      },
      {
        domain: 'run_workflows',
        tableName: 'run_workflows',
        requiredTables: ['run_workflows'],
        sql: `SELECT COUNT(*)::text AS count FROM run_workflows WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      artifacts: 'Artifacts',
      run_workflows: 'Run workflows',
    },
    signalsHint: 'Run outcomes, artifacts, and run workflows',
  },
  {
    name: 'elaboratability',
    Name: 'Elaboratability',
    percent: 'elaboratabilityPercent',
    metricDomain: 'run_workflows',
    metricLabel: 'Workflow elaboratability',
    metricDetail: 'workflow elaboratability',
    action: 'refresh_elaboratability_summary',
    tables: ['run_workflows', 'agent_outputs', 'moderator_syntheses'],
    check1: {
      key: 'runWorkflowsTableExists',
      name: 'workflow_elaboratability',
      label: 'Workflow elaboratability',
      table: 'run_workflows',
    },
    check2: {
      key: 'agentOutputsTableExists',
      name: 'agent_output_elaboratability',
      label: 'Agent output elaboratability',
      table: 'agent_outputs',
    },
    check3Key: 'moderatorSynthesesTableExists',
    coverageKeys: {
      run_workflows: 'runWorkflowsTableExists',
      agent_outputs: 'agentOutputsTableExists',
      moderator_syntheses: 'moderatorSynthesesTableExists',
    },
    readiness: 'elaboration_readiness_signal',
    readinessLabel: 'Elaboration readiness signal',
    readinessDetail:
      'Run workflows, agent outputs, and moderator syntheses support elaboration readiness.',
    cap1: 'supportsWorkflowElaboratabilitySignals',
    cap2: 'supportsAgentOutputElaboratabilitySignals',
    guidance:
      'Production elaboratability rollout validates workflow elaboratability, agent output elaboratability signals, synthesis coverage, and elaboration readiness before production elaboratability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'run_workflows',
        tableName: 'run_workflows',
        requiredTables: ['run_workflows'],
        sql: `SELECT COUNT(*)::text AS count FROM run_workflows WHERE workspace_id = $1`,
      },
      {
        domain: 'agent_outputs',
        tableName: 'agent_outputs',
        requiredTables: ['agent_outputs', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM agent_outputs ao INNER JOIN runs r ON r.run_id = ao.run_id WHERE r.workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      run_workflows: 'Run workflows',
      agent_outputs: 'Agent outputs',
    },
    signalsHint: 'Run outcomes, run workflows, and agent outputs',
  },
  {
    name: 'representability',
    Name: 'Representability',
    percent: 'representabilityPercent',
    metricDomain: 'billing_invoices',
    metricLabel: 'Billing invoice representability',
    metricDetail: 'billing invoice representability',
    action: 'refresh_representability_summary',
    tables: ['billing_invoices', 'billing_records', 'billing_webhook_events'],
    check1: {
      key: 'billingInvoicesTableExists',
      name: 'billing_invoice_representability',
      label: 'Billing invoice representability',
      table: 'billing_invoices',
    },
    check2: {
      key: 'billingRecordsTableExists',
      name: 'billing_record_representability',
      label: 'Billing record representability',
      table: 'billing_records',
    },
    check3Key: 'billingWebhookEventsTableExists',
    coverageKeys: {
      billing_invoices: 'billingInvoicesTableExists',
      billing_records: 'billingRecordsTableExists',
      billing_webhook_events: 'billingWebhookEventsTableExists',
    },
    readiness: 'representation_readiness_signal',
    readinessLabel: 'Representation readiness signal',
    readinessDetail:
      'Billing invoices, billing records, and billing webhook events support representation readiness.',
    cap1: 'supportsBillingInvoiceRepresentabilitySignals',
    cap2: 'supportsBillingRecordRepresentabilitySignals',
    guidance:
      'Production representability rollout validates billing invoice representability, billing record representability signals, billing webhook coverage, and representation readiness before production representability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'billing_invoices',
        tableName: 'billing_invoices',
        requiredTables: ['billing_invoices'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_invoices WHERE workspace_id = $1`,
      },
      {
        domain: 'billing_records',
        tableName: 'billing_records',
        requiredTables: ['billing_records'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_records WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      billing_invoices: 'Billing invoices',
      billing_records: 'Billing records',
    },
    signalsHint: 'Run outcomes, billing invoices, and billing records',
  },
]

export const patchMeta = milestones.map((m, index) => ({
  ...m,
  version: `v5.${161 + index}`,
  domainFormatter: `format${m.Name}Domain`,
}))
