export const milestones = [
  {
    name: 'inspectability',
    Name: 'Inspectability',
    percent: 'inspectabilityPercent',
    metricDomain: 'usage_events',
    metricLabel: 'Usage inspectability',
    metricDetail: 'usage inspectability',
    action: 'refresh_inspectability_summary',
    tables: ['usage_events', 'billing_meter_usage_reports', 'workspace_usage_limits'],
    check1: {
      key: 'usageEventsTableExists',
      name: 'usage_inspectability',
      label: 'Usage inspectability',
      table: 'usage_events',
    },
    check2: {
      key: 'billingMeterUsageReportsTableExists',
      name: 'meter_usage_inspectability',
      label: 'Meter usage inspectability',
      table: 'billing_meter_usage_reports',
    },
    check3Key: 'workspaceUsageLimitsTableExists',
    coverageKeys: {
      usage_events: 'usageEventsTableExists',
      billing_meter_usage_reports: 'billingMeterUsageReportsTableExists',
      workspace_usage_limits: 'workspaceUsageLimitsTableExists',
    },
    readiness: 'review_readiness_signal',
    readinessLabel: 'Review readiness signal',
    readinessDetail:
      'Usage events, meter usage reports, and workspace usage limits support review readiness.',
    cap1: 'supportsUsageInspectabilitySignals',
    cap2: 'supportsMeterUsageInspectabilitySignals',
    guidance:
      'Production inspectability rollout validates usage inspectability, meter usage inspectability signals, workspace limit coverage, and review readiness before production inspectability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'usage_events',
        tableName: 'usage_events',
        requiredTables: ['usage_events'],
        sql: `SELECT COUNT(*)::text AS count FROM usage_events WHERE workspace_id = $1`,
      },
      {
        domain: 'billing_meter_usage_reports',
        tableName: 'billing_meter_usage_reports',
        requiredTables: ['billing_meter_usage_reports'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_meter_usage_reports WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      usage_events: 'Usage events',
      billing_meter_usage_reports: 'Meter usage reports',
    },
    signalsHint: 'Run outcomes, usage events, and meter usage reports',
  },
  {
    name: 'explainability',
    Name: 'Explainability',
    percent: 'explainabilityPercent',
    metricDomain: 'moderator_syntheses',
    metricLabel: 'Moderator synthesis explainability',
    metricDetail: 'moderator synthesis explainability',
    action: 'refresh_explainability_summary',
    tables: ['moderator_syntheses', 'agent_outputs', 'artifacts'],
    check1: {
      key: 'moderatorSynthesesTableExists',
      name: 'synthesis_explainability',
      label: 'Synthesis explainability',
      table: 'moderator_syntheses',
    },
    check2: {
      key: 'agentOutputsTableExists',
      name: 'agent_output_explainability',
      label: 'Agent output explainability',
      table: 'agent_outputs',
    },
    check3Key: 'artifactsTableExists',
    coverageKeys: {
      moderator_syntheses: 'moderatorSynthesesTableExists',
      agent_outputs: 'agentOutputsTableExists',
      artifacts: 'artifactsTableExists',
    },
    readiness: 'interpretation_readiness_signal',
    readinessLabel: 'Interpretation readiness signal',
    readinessDetail:
      'Moderator syntheses, agent outputs, and persisted artifacts support interpretation readiness.',
    cap1: 'supportsSynthesisExplainabilitySignals',
    cap2: 'supportsAgentOutputExplainabilitySignals',
    guidance:
      'Production explainability rollout validates synthesis explainability, agent output explainability signals, artifact coverage, and interpretation readiness before production explainability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'moderator_syntheses',
        tableName: 'moderator_syntheses',
        requiredTables: ['moderator_syntheses', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM moderator_syntheses ms INNER JOIN runs r ON r.run_id = ms.run_id WHERE r.workspace_id = $1`,
      },
      {
        domain: 'artifacts',
        tableName: 'artifacts',
        requiredTables: ['artifacts'],
        sql: `SELECT COUNT(*)::text AS count FROM artifacts WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      moderator_syntheses: 'Moderator syntheses',
      artifacts: 'Artifacts',
    },
    signalsHint: 'Run outcomes, moderator syntheses, and artifacts',
  },
  {
    name: 'demonstrability',
    Name: 'Demonstrability',
    percent: 'demonstrabilityPercent',
    metricDomain: 'run_workflows',
    metricLabel: 'Workflow demonstrability',
    metricDetail: 'workflow demonstrability',
    action: 'refresh_demonstrability_summary',
    tables: ['run_workflows', 'artifacts', 'billing_notifications'],
    check1: {
      key: 'runWorkflowsTableExists',
      name: 'workflow_demonstrability',
      label: 'Workflow demonstrability',
      table: 'run_workflows',
    },
    check2: {
      key: 'artifactsTableExists',
      name: 'artifact_demonstrability',
      label: 'Artifact demonstrability',
      table: 'artifacts',
    },
    check3Key: 'billingNotificationsTableExists',
    coverageKeys: {
      run_workflows: 'runWorkflowsTableExists',
      artifacts: 'artifactsTableExists',
      billing_notifications: 'billingNotificationsTableExists',
    },
    readiness: 'presentation_readiness_signal',
    readinessLabel: 'Presentation readiness signal',
    readinessDetail:
      'Run workflows, persisted artifacts, and billing notifications support presentation readiness.',
    cap1: 'supportsWorkflowDemonstrabilitySignals',
    cap2: 'supportsArtifactDemonstrabilitySignals',
    guidance:
      'Production demonstrability rollout validates workflow demonstrability, artifact demonstrability signals, billing notification coverage, and presentation readiness before production demonstrability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'run_workflows',
        tableName: 'run_workflows',
        requiredTables: ['run_workflows'],
        sql: `SELECT COUNT(*)::text AS count FROM run_workflows WHERE workspace_id = $1`,
      },
      {
        domain: 'billing_notifications',
        tableName: 'billing_notifications',
        requiredTables: ['billing_notifications'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_notifications WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      run_workflows: 'Run workflows',
      billing_notifications: 'Billing notifications',
    },
    signalsHint: 'Run outcomes, run workflows, and billing notifications',
  },
  {
    name: 'justifiability',
    Name: 'Justifiability',
    percent: 'justifiabilityPercent',
    metricDomain: 'shield_reviews',
    metricLabel: 'Shield review justifiability',
    metricDetail: 'shield review justifiability',
    action: 'refresh_justifiability_summary',
    tables: ['shield_scans', 'billing_webhook_events', 'idempotency_keys'],
    check1: {
      key: 'shieldScansTableExists',
      name: 'shield_review_justifiability',
      label: 'Shield review justifiability',
      table: 'shield_scans',
    },
    check2: {
      key: 'billingWebhookEventsTableExists',
      name: 'billing_webhook_justifiability',
      label: 'Billing webhook justifiability',
      table: 'billing_webhook_events',
    },
    check3Key: 'idempotencyKeysTableExists',
    coverageKeys: {
      shield_scans: 'shieldScansTableExists',
      billing_webhook_events: 'billingWebhookEventsTableExists',
      idempotency_keys: 'idempotencyKeysTableExists',
    },
    readiness: 'rationale_readiness_signal',
    readinessLabel: 'Rationale readiness signal',
    readinessDetail:
      'Shield reviews, billing webhook events, and idempotency keys support rationale readiness.',
    cap1: 'supportsShieldReviewJustifiabilitySignals',
    cap2: 'supportsBillingWebhookJustifiabilitySignals',
    guidance:
      'Production justifiability rollout validates shield review justifiability, billing webhook justifiability signals, idempotency coverage, and rationale readiness before production justifiability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'shield_reviews',
        tableName: 'shield_scans',
        requiredTables: ['shield_scans', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM shield_scans ss INNER JOIN runs r ON r.run_id = ss.run_id WHERE r.workspace_id = $1`,
      },
      {
        domain: 'idempotency_keys',
        tableName: 'idempotency_keys',
        requiredTables: ['idempotency_keys'],
        sql: `SELECT COUNT(*)::text AS count FROM idempotency_keys WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      shield_reviews: 'Shield reviews',
      idempotency_keys: 'Idempotency keys',
    },
    signalsHint: 'Run outcomes, shield reviews, and idempotency keys',
  },
  {
    name: 'reviewability',
    Name: 'Reviewability',
    percent: 'reviewabilityPercent',
    metricDomain: 'artifacts',
    metricLabel: 'Artifact reviewability',
    metricDetail: 'artifact reviewability',
    action: 'refresh_reviewability_summary',
    tables: ['artifacts', 'agent_outputs', 'billing_invoices'],
    check1: {
      key: 'artifactsTableExists',
      name: 'artifact_reviewability',
      label: 'Artifact reviewability',
      table: 'artifacts',
    },
    check2: {
      key: 'agentOutputsTableExists',
      name: 'agent_output_reviewability',
      label: 'Agent output reviewability',
      table: 'agent_outputs',
    },
    check3Key: 'billingInvoicesTableExists',
    coverageKeys: {
      artifacts: 'artifactsTableExists',
      agent_outputs: 'agentOutputsTableExists',
      billing_invoices: 'billingInvoicesTableExists',
    },
    readiness: 'assessment_readiness_signal',
    readinessLabel: 'Assessment readiness signal',
    readinessDetail:
      'Persisted artifacts, agent outputs, and billing invoices support assessment readiness.',
    cap1: 'supportsArtifactReviewabilitySignals',
    cap2: 'supportsAgentOutputReviewabilitySignals',
    guidance:
      'Production reviewability rollout validates artifact reviewability, agent output reviewability signals, billing invoice coverage, and assessment readiness before production reviewability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'artifacts',
        tableName: 'artifacts',
        requiredTables: ['artifacts'],
        sql: `SELECT COUNT(*)::text AS count FROM artifacts WHERE workspace_id = $1`,
      },
      {
        domain: 'billing_invoices',
        tableName: 'billing_invoices',
        requiredTables: ['billing_invoices'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_invoices WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      artifacts: 'Artifacts',
      billing_invoices: 'Billing invoices',
    },
    signalsHint: 'Run outcomes, artifacts, and billing invoices',
  },
]

export const patchMeta = milestones.map((m, index) => ({
  ...m,
  version: `v5.${71 + index}`,
  domainFormatter: `format${m.Name}Domain`,
}))
