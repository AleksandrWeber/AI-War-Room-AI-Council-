export const milestones = [
  {
    name: 'readability',
    Name: 'Readability',
    percent: 'readabilityPercent',
    metricDomain: 'artifacts',
    metricLabel: 'Artifact readability',
    metricDetail: 'artifact readability',
    action: 'refresh_readability_summary',
    tables: ['artifacts', 'agent_outputs', 'moderator_syntheses'],
    check1: {
      key: 'artifactsTableExists',
      name: 'artifact_readability',
      label: 'Artifact readability',
      table: 'artifacts',
    },
    check2: {
      key: 'agentOutputsTableExists',
      name: 'agent_output_readability',
      label: 'Agent output readability',
      table: 'agent_outputs',
    },
    check3Key: 'moderatorSynthesesTableExists',
    coverageKeys: {
      artifacts: 'artifactsTableExists',
      agent_outputs: 'agentOutputsTableExists',
      moderator_syntheses: 'moderatorSynthesesTableExists',
    },
    readiness: 'readability_readiness_signal',
    readinessLabel: 'Readability readiness signal',
    readinessDetail:
      'Artifacts, agent outputs, and moderator syntheses support readability readiness.',
    cap1: 'supportsArtifactReadabilitySignals',
    cap2: 'supportsAgentOutputReadabilitySignals',
    guidance:
      'Production readability rollout validates artifact readability, agent output readability signals, synthesis coverage, and readability readiness before production readability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'artifacts',
        tableName: 'artifacts',
        requiredTables: ['artifacts'],
        sql: `SELECT COUNT(*)::text AS count FROM artifacts WHERE workspace_id = $1`,
      },
      {
        domain: 'agent_outputs',
        tableName: 'agent_outputs',
        requiredTables: ['agent_outputs', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM agent_outputs ao INNER JOIN runs r ON r.run_id = ao.run_id WHERE r.workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      artifacts: 'Artifacts',
      agent_outputs: 'Agent outputs',
    },
    signalsHint: 'Run outcomes, artifacts, and agent outputs',
  },
  {
    name: 'clarity',
    Name: 'Clarity',
    percent: 'clarityPercent',
    metricDomain: 'moderator_syntheses',
    metricLabel: 'Moderator synthesis clarity',
    metricDetail: 'moderator synthesis clarity',
    action: 'refresh_clarity_summary',
    tables: ['moderator_syntheses', 'agent_outputs', 'artifacts'],
    check1: {
      key: 'moderatorSynthesesTableExists',
      name: 'synthesis_clarity',
      label: 'Synthesis clarity',
      table: 'moderator_syntheses',
    },
    check2: {
      key: 'agentOutputsTableExists',
      name: 'agent_output_clarity',
      label: 'Agent output clarity',
      table: 'agent_outputs',
    },
    check3Key: 'artifactsTableExists',
    coverageKeys: {
      moderator_syntheses: 'moderatorSynthesesTableExists',
      agent_outputs: 'agentOutputsTableExists',
      artifacts: 'artifactsTableExists',
    },
    readiness: 'clarity_readiness_signal',
    readinessLabel: 'Clarity readiness signal',
    readinessDetail:
      'Moderator syntheses, agent outputs, and persisted artifacts support clarity readiness.',
    cap1: 'supportsSynthesisClaritySignals',
    cap2: 'supportsAgentOutputClaritySignals',
    guidance:
      'Production clarity rollout validates synthesis clarity, agent output clarity signals, artifact coverage, and clarity readiness before production clarity tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'moderator_syntheses',
        tableName: 'moderator_syntheses',
        requiredTables: ['moderator_syntheses', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM moderator_syntheses ms INNER JOIN runs r ON r.run_id = ms.run_id WHERE r.workspace_id = $1`,
      },
      {
        domain: 'agent_outputs',
        tableName: 'agent_outputs',
        requiredTables: ['agent_outputs', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM agent_outputs ao INNER JOIN runs r ON r.run_id = ao.run_id WHERE r.workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      moderator_syntheses: 'Moderator syntheses',
      agent_outputs: 'Agent outputs',
    },
    signalsHint: 'Run outcomes, moderator syntheses, and agent outputs',
  },
  {
    name: 'simplicity',
    Name: 'Simplicity',
    percent: 'simplicityPercent',
    metricDomain: 'run_workflows',
    metricLabel: 'Workflow simplicity',
    metricDetail: 'workflow simplicity',
    action: 'refresh_simplicity_summary',
    tables: ['run_workflows', 'idempotency_keys', 'usage_events'],
    check1: {
      key: 'runWorkflowsTableExists',
      name: 'workflow_simplicity',
      label: 'Workflow simplicity',
      table: 'run_workflows',
    },
    check2: {
      key: 'idempotencyKeysTableExists',
      name: 'idempotency_key_simplicity',
      label: 'Idempotency key simplicity',
      table: 'idempotency_keys',
    },
    check3Key: 'usageEventsTableExists',
    coverageKeys: {
      run_workflows: 'runWorkflowsTableExists',
      idempotency_keys: 'idempotencyKeysTableExists',
      usage_events: 'usageEventsTableExists',
    },
    readiness: 'simplicity_readiness_signal',
    readinessLabel: 'Simplicity readiness signal',
    readinessDetail:
      'Run workflows, idempotency keys, and usage events support simplicity readiness.',
    cap1: 'supportsWorkflowSimplicitySignals',
    cap2: 'supportsIdempotencyKeySimplicitySignals',
    guidance:
      'Production simplicity rollout validates workflow simplicity, idempotency key simplicity signals, usage event coverage, and simplicity readiness before production simplicity tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'run_workflows',
        tableName: 'run_workflows',
        requiredTables: ['run_workflows'],
        sql: `SELECT COUNT(*)::text AS count FROM run_workflows WHERE workspace_id = $1`,
      },
      {
        domain: 'idempotency_keys',
        tableName: 'idempotency_keys',
        requiredTables: ['idempotency_keys'],
        sql: `SELECT COUNT(*)::text AS count FROM idempotency_keys WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      run_workflows: 'Run workflows',
      idempotency_keys: 'Idempotency keys',
    },
    signalsHint: 'Run outcomes, run workflows, and idempotency keys',
  },
  {
    name: 'negotiability',
    Name: 'Negotiability',
    percent: 'negotiabilityPercent',
    metricDomain: 'billing_invoices',
    metricLabel: 'Billing invoice negotiability',
    metricDetail: 'billing invoice negotiability',
    action: 'refresh_negotiability_summary',
    tables: ['billing_invoices', 'billing_records', 'billing_webhook_events'],
    check1: {
      key: 'billingInvoicesTableExists',
      name: 'billing_invoice_negotiability',
      label: 'Billing invoice negotiability',
      table: 'billing_invoices',
    },
    check2: {
      key: 'billingRecordsTableExists',
      name: 'billing_record_negotiability',
      label: 'Billing record negotiability',
      table: 'billing_records',
    },
    check3Key: 'billingWebhookEventsTableExists',
    coverageKeys: {
      billing_invoices: 'billingInvoicesTableExists',
      billing_records: 'billingRecordsTableExists',
      billing_webhook_events: 'billingWebhookEventsTableExists',
    },
    readiness: 'negotiation_readiness_signal',
    readinessLabel: 'Negotiation readiness signal',
    readinessDetail:
      'Billing invoices, billing records, and billing webhook events support negotiation readiness.',
    cap1: 'supportsBillingInvoiceNegotiabilitySignals',
    cap2: 'supportsBillingRecordNegotiabilitySignals',
    guidance:
      'Production negotiability rollout validates billing invoice negotiability, billing record negotiability signals, billing webhook coverage, and negotiation readiness before production negotiability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'billing_invoices',
        tableName: 'billing_invoices',
        requiredTables: ['billing_invoices'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_invoices WHERE workspace_id = $1`,
      },
      {
        domain: 'billing_records',
        tableName: 'billing_records',
        requiredTables: ['billing_records'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_records WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      billing_invoices: 'Billing invoices',
      billing_records: 'Billing records',
    },
    signalsHint: 'Run outcomes, billing invoices, and billing records',
  },
  {
    name: 'comprehensibility',
    Name: 'Comprehensibility',
    percent: 'comprehensibilityPercent',
    metricDomain: 'agent_outputs',
    metricLabel: 'Agent output comprehensibility',
    metricDetail: 'agent output comprehensibility',
    action: 'refresh_comprehensibility_summary',
    tables: ['agent_outputs', 'moderator_syntheses', 'artifacts'],
    check1: {
      key: 'agentOutputsTableExists',
      name: 'agent_output_comprehensibility',
      label: 'Agent output comprehensibility',
      table: 'agent_outputs',
    },
    check2: {
      key: 'moderatorSynthesesTableExists',
      name: 'synthesis_comprehensibility',
      label: 'Synthesis comprehensibility',
      table: 'moderator_syntheses',
    },
    check3Key: 'artifactsTableExists',
    coverageKeys: {
      agent_outputs: 'agentOutputsTableExists',
      moderator_syntheses: 'moderatorSynthesesTableExists',
      artifacts: 'artifactsTableExists',
    },
    readiness: 'comprehension_readiness_signal',
    readinessLabel: 'Comprehension readiness signal',
    readinessDetail:
      'Agent outputs, moderator syntheses, and persisted artifacts support comprehension readiness.',
    cap1: 'supportsAgentOutputComprehensibilitySignals',
    cap2: 'supportsSynthesisComprehensibilitySignals',
    guidance:
      'Production comprehensibility rollout validates agent output comprehensibility, synthesis comprehensibility signals, artifact coverage, and comprehension readiness before production comprehensibility tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'agent_outputs',
        tableName: 'agent_outputs',
        requiredTables: ['agent_outputs', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM agent_outputs ao INNER JOIN runs r ON r.run_id = ao.run_id WHERE r.workspace_id = $1`,
      },
      {
        domain: 'moderator_syntheses',
        tableName: 'moderator_syntheses',
        requiredTables: ['moderator_syntheses', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM moderator_syntheses ms INNER JOIN runs r ON r.run_id = ms.run_id WHERE r.workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      agent_outputs: 'Agent outputs',
      moderator_syntheses: 'Moderator syntheses',
    },
    signalsHint: 'Run outcomes, agent outputs, and moderator syntheses',
  },
]

export const patchMeta = milestones.map((m, index) => ({
  ...m,
  version: `v5.${141 + index}`,
  domainFormatter: `format${m.Name}Domain`,
}))
