export const milestones = [
  {
    name: 'validity',
    Name: 'Validity',
    percent: 'validityPercent',
    metricDomain: 'agent_outputs',
    metricLabel: 'Agent output validity',
    metricDetail: 'agent output validity',
    action: 'refresh_validity_summary',
    tables: ['agent_outputs', 'artifacts', 'shield_scans'],
    check1: {
      key: 'agentOutputsTableExists',
      name: 'agent_output_validity',
      label: 'Agent output validity',
      table: 'agent_outputs',
    },
    check2: {
      key: 'artifactsTableExists',
      name: 'artifact_content_validity',
      label: 'Artifact content validity',
      table: 'artifacts',
    },
    check3Key: 'shieldScansTableExists',
    coverageKeys: {
      agent_outputs: 'agentOutputsTableExists',
      artifacts: 'artifactsTableExists',
      shield_scans: 'shieldScansTableExists',
    },
    readiness: 'validation_readiness_signal',
    readinessLabel: 'Validation readiness signal',
    readinessDetail:
      'Agent outputs, persisted artifacts, and shield scans support validation readiness.',
    cap1: 'supportsAgentOutputValiditySignals',
    cap2: 'supportsArtifactValiditySignals',
    guidance:
      'Production validity rollout validates agent output validity, artifact content validity signals, shield scan coverage, and validation readiness before production validity tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'agent_outputs',
        tableName: 'agent_outputs',
        requiredTables: ['agent_outputs', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM agent_outputs ao INNER JOIN runs r ON r.run_id = ao.run_id WHERE r.workspace_id = $1`,
      },
      {
        domain: 'artifacts',
        tableName: 'artifacts',
        requiredTables: ['artifacts'],
        sql: `SELECT COUNT(*)::text AS count FROM artifacts WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      agent_outputs: 'Agent outputs',
      artifacts: 'Artifacts',
    },
    signalsHint: 'Run outcomes, agent outputs, and artifacts',
  },
  {
    name: 'credibility',
    Name: 'Credibility',
    percent: 'credibilityPercent',
    metricDomain: 'billing_invoices',
    metricLabel: 'Billing invoice credibility',
    metricDetail: 'billing invoice credibility',
    action: 'refresh_credibility_summary',
    tables: ['billing_invoices', 'billing_records', 'billing_meter_usage_reports'],
    check1: {
      key: 'billingInvoicesTableExists',
      name: 'billing_invoice_credibility',
      label: 'Billing invoice credibility',
      table: 'billing_invoices',
    },
    check2: {
      key: 'billingRecordsTableExists',
      name: 'billing_record_credibility',
      label: 'Billing record credibility',
      table: 'billing_records',
    },
    check3Key: 'billingMeterUsageReportsTableExists',
    coverageKeys: {
      billing_invoices: 'billingInvoicesTableExists',
      billing_records: 'billingRecordsTableExists',
      billing_meter_usage_reports: 'billingMeterUsageReportsTableExists',
    },
    readiness: 'trust_readiness_signal',
    readinessLabel: 'Trust readiness signal',
    readinessDetail:
      'Billing invoices, billing records, and meter usage reports support trust readiness.',
    cap1: 'supportsBillingInvoiceCredibilitySignals',
    cap2: 'supportsBillingRecordCredibilitySignals',
    guidance:
      'Production credibility rollout validates billing invoice credibility, billing record credibility signals, meter usage reporting coverage, and trust readiness before production credibility tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'billing_invoices',
        tableName: 'billing_invoices',
        requiredTables: ['billing_invoices'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_invoices WHERE workspace_id = $1`,
      },
      {
        domain: 'billing_records',
        tableName: 'billing_records',
        requiredTables: ['billing_records'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_records WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      billing_invoices: 'Billing invoices',
      billing_records: 'Billing records',
    },
    signalsHint: 'Run outcomes, billing invoices, and billing records',
  },
  {
    name: 'reproducibility',
    Name: 'Reproducibility',
    percent: 'reproducibilityPercent',
    metricDomain: 'idempotency_keys',
    metricLabel: 'Idempotency reproducibility',
    metricDetail: 'idempotency reproducibility',
    action: 'refresh_reproducibility_summary',
    tables: ['idempotency_keys', 'run_workflows', 'agent_outputs'],
    check1: {
      key: 'idempotencyKeysTableExists',
      name: 'idempotency_reproducibility',
      label: 'Idempotency reproducibility',
      table: 'idempotency_keys',
    },
    check2: {
      key: 'runWorkflowsTableExists',
      name: 'workflow_reproducibility',
      label: 'Workflow reproducibility',
      table: 'run_workflows',
    },
    check3Key: 'agentOutputsTableExists',
    coverageKeys: {
      idempotency_keys: 'idempotencyKeysTableExists',
      run_workflows: 'runWorkflowsTableExists',
      agent_outputs: 'agentOutputsTableExists',
    },
    readiness: 'repeatability_readiness_signal',
    readinessLabel: 'Repeatability readiness signal',
    readinessDetail:
      'Idempotency keys, run workflows, and agent outputs support repeatability readiness.',
    cap1: 'supportsIdempotencyReproducibilitySignals',
    cap2: 'supportsWorkflowReproducibilitySignals',
    guidance:
      'Production reproducibility rollout validates idempotency reproducibility, workflow reproducibility signals, agent output coverage, and repeatability readiness before production reproducibility tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'idempotency_keys',
        tableName: 'idempotency_keys',
        requiredTables: ['idempotency_keys'],
        sql: `SELECT COUNT(*)::text AS count FROM idempotency_keys WHERE workspace_id = $1`,
      },
      {
        domain: 'run_workflows',
        tableName: 'run_workflows',
        requiredTables: ['run_workflows'],
        sql: `SELECT COUNT(*)::text AS count FROM run_workflows WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      idempotency_keys: 'Idempotency keys',
      run_workflows: 'Run workflows',
    },
    signalsHint: 'Run outcomes, idempotency keys, and run workflows',
  },
  {
    name: 'defensibility',
    Name: 'Defensibility',
    percent: 'defensibilityPercent',
    metricDomain: 'shield_reviews',
    metricLabel: 'Shield review defensibility',
    metricDetail: 'shield review defensibility',
    action: 'refresh_defensibility_summary',
    tables: ['shield_scans', 'artifacts', 'moderator_syntheses'],
    check1: {
      key: 'shieldScansTableExists',
      name: 'shield_review_defensibility',
      label: 'Shield review defensibility',
      table: 'shield_scans',
    },
    check2: {
      key: 'artifactsTableExists',
      name: 'artifact_defensibility',
      label: 'Artifact defensibility',
      table: 'artifacts',
    },
    check3Key: 'moderatorSynthesesTableExists',
    coverageKeys: {
      shield_scans: 'shieldScansTableExists',
      artifacts: 'artifactsTableExists',
      moderator_syntheses: 'moderatorSynthesesTableExists',
    },
    readiness: 'justification_readiness_signal',
    readinessLabel: 'Justification readiness signal',
    readinessDetail:
      'Shield reviews, persisted artifacts, and moderator syntheses support justification readiness.',
    cap1: 'supportsShieldReviewDefensibilitySignals',
    cap2: 'supportsArtifactDefensibilitySignals',
    guidance:
      'Production defensibility rollout validates shield review defensibility, artifact defensibility signals, moderator synthesis coverage, and justification readiness before production defensibility tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'shield_reviews',
        tableName: 'shield_scans',
        requiredTables: ['shield_scans', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM shield_scans ss INNER JOIN runs r ON r.run_id = ss.run_id WHERE r.workspace_id = $1`,
      },
      {
        domain: 'artifacts',
        tableName: 'artifacts',
        requiredTables: ['artifacts'],
        sql: `SELECT COUNT(*)::text AS count FROM artifacts WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      shield_reviews: 'Shield reviews',
      artifacts: 'Artifacts',
    },
    signalsHint: 'Run outcomes, shield reviews, and artifacts',
  },
  {
    name: 'auditability',
    Name: 'Auditability',
    percent: 'auditabilityPercent',
    metricDomain: 'usage_events',
    metricLabel: 'Usage auditability',
    metricDetail: 'usage auditability',
    action: 'refresh_auditability_summary',
    tables: ['usage_events', 'billing_webhook_events', 'billing_notifications'],
    check1: {
      key: 'usageEventsTableExists',
      name: 'usage_auditability',
      label: 'Usage auditability',
      table: 'usage_events',
    },
    check2: {
      key: 'billingWebhookEventsTableExists',
      name: 'billing_webhook_auditability',
      label: 'Billing webhook auditability',
      table: 'billing_webhook_events',
    },
    check3Key: 'billingNotificationsTableExists',
    coverageKeys: {
      usage_events: 'usageEventsTableExists',
      billing_webhook_events: 'billingWebhookEventsTableExists',
      billing_notifications: 'billingNotificationsTableExists',
    },
    readiness: 'examination_readiness_signal',
    readinessLabel: 'Examination readiness signal',
    readinessDetail:
      'Usage events, billing webhook events, and billing notifications support examination readiness.',
    cap1: 'supportsUsageAuditabilitySignals',
    cap2: 'supportsBillingWebhookAuditabilitySignals',
    guidance:
      'Production auditability rollout validates usage auditability, billing webhook auditability signals, billing notification coverage, and examination readiness before production auditability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'usage_events',
        tableName: 'usage_events',
        requiredTables: ['usage_events'],
        sql: `SELECT COUNT(*)::text AS count FROM usage_events WHERE workspace_id = $1`,
      },
      {
        domain: 'billing_webhook_events',
        tableName: 'billing_webhook_events',
        requiredTables: ['billing_webhook_events'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_webhook_events WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      usage_events: 'Usage events',
      billing_webhook_events: 'Billing webhook events',
    },
    signalsHint: 'Run outcomes, usage events, and webhook events',
  },
]

export const patchMeta = milestones.map((m, index) => ({
  ...m,
  version: `v5.${66 + index}`,
  domainFormatter: `format${m.Name}Domain`,
}))
