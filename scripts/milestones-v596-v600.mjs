export const milestones = [
  {
    name: 'compatibility',
    Name: 'Compatibility',
    percent: 'compatibilityPercent',
    metricDomain: 'workspace_provider_credentials',
    metricLabel: 'Provider credential compatibility',
    metricDetail: 'provider credential compatibility',
    action: 'refresh_compatibility_summary',
    tables: [
      'workspace_provider_credentials',
      'workspace_usage_limits',
      'billing_records',
    ],
    check1: {
      key: 'workspaceProviderCredentialsTableExists',
      name: 'provider_credential_compatibility',
      label: 'Provider credential compatibility',
      table: 'workspace_provider_credentials',
    },
    check2: {
      key: 'workspaceUsageLimitsTableExists',
      name: 'workspace_limit_compatibility',
      label: 'Workspace limit compatibility',
      table: 'workspace_usage_limits',
    },
    check3Key: 'billingRecordsTableExists',
    coverageKeys: {
      workspace_provider_credentials: 'workspaceProviderCredentialsTableExists',
      workspace_usage_limits: 'workspaceUsageLimitsTableExists',
      billing_records: 'billingRecordsTableExists',
    },
    readiness: 'compatibility_readiness_signal',
    readinessLabel: 'Compatibility readiness signal',
    readinessDetail:
      'Workspace provider credentials, usage limits, and billing records support compatibility readiness.',
    cap1: 'supportsProviderCredentialCompatibilitySignals',
    cap2: 'supportsWorkspaceLimitCompatibilitySignals',
    guidance:
      'Production compatibility rollout validates provider credential compatibility, workspace limit compatibility signals, billing record coverage, and compatibility readiness before production compatibility tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'workspace_provider_credentials',
        tableName: 'workspace_provider_credentials',
        requiredTables: ['workspace_provider_credentials'],
        sql: `SELECT COUNT(*)::text AS count FROM workspace_provider_credentials WHERE workspace_id = $1`,
      },
      {
        domain: 'billing_records',
        tableName: 'billing_records',
        requiredTables: ['billing_records'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_records WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      workspace_provider_credentials: 'Provider credentials',
      billing_records: 'Billing records',
    },
    signalsHint: 'Run outcomes, provider credentials, and billing records',
  },
  {
    name: 'adaptability',
    Name: 'Adaptability',
    percent: 'adaptabilityPercent',
    metricDomain: 'billing_webhook_events',
    metricLabel: 'Billing webhook adaptability',
    metricDetail: 'billing webhook adaptability',
    action: 'refresh_adaptability_summary',
    tables: ['billing_webhook_events', 'billing_notifications', 'idempotency_keys'],
    check1: {
      key: 'billingWebhookEventsTableExists',
      name: 'billing_webhook_adaptability',
      label: 'Billing webhook adaptability',
      table: 'billing_webhook_events',
    },
    check2: {
      key: 'billingNotificationsTableExists',
      name: 'billing_notification_adaptability',
      label: 'Billing notification adaptability',
      table: 'billing_notifications',
    },
    check3Key: 'idempotencyKeysTableExists',
    coverageKeys: {
      billing_webhook_events: 'billingWebhookEventsTableExists',
      billing_notifications: 'billingNotificationsTableExists',
      idempotency_keys: 'idempotencyKeysTableExists',
    },
    readiness: 'adaptation_readiness_signal',
    readinessLabel: 'Adaptation readiness signal',
    readinessDetail:
      'Billing webhook events, billing notifications, and idempotency keys support adaptation readiness.',
    cap1: 'supportsBillingWebhookAdaptabilitySignals',
    cap2: 'supportsBillingNotificationAdaptabilitySignals',
    guidance:
      'Production adaptability rollout validates billing webhook adaptability, billing notification adaptability signals, idempotency coverage, and adaptation readiness before production adaptability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'billing_webhook_events',
        tableName: 'billing_webhook_events',
        requiredTables: ['billing_webhook_events'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_webhook_events WHERE workspace_id = $1`,
      },
      {
        domain: 'billing_notifications',
        tableName: 'billing_notifications',
        requiredTables: ['billing_notifications'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_notifications WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      billing_webhook_events: 'Billing webhook events',
      billing_notifications: 'Billing notifications',
    },
    signalsHint: 'Run outcomes, billing webhook events, and billing notifications',
  },
  {
    name: 'flexibility',
    Name: 'Flexibility',
    percent: 'flexibilityPercent',
    metricDomain: 'run_workflows',
    metricLabel: 'Workflow flexibility',
    metricDetail: 'workflow flexibility',
    action: 'refresh_flexibility_summary',
    tables: ['run_workflows', 'usage_events', 'shield_scans'],
    check1: {
      key: 'runWorkflowsTableExists',
      name: 'workflow_flexibility',
      label: 'Workflow flexibility',
      table: 'run_workflows',
    },
    check2: {
      key: 'usageEventsTableExists',
      name: 'usage_event_flexibility',
      label: 'Usage event flexibility',
      table: 'usage_events',
    },
    check3Key: 'shieldScansTableExists',
    coverageKeys: {
      run_workflows: 'runWorkflowsTableExists',
      usage_events: 'usageEventsTableExists',
      shield_scans: 'shieldScansTableExists',
    },
    readiness: 'flexibility_readiness_signal',
    readinessLabel: 'Flexibility readiness signal',
    readinessDetail:
      'Run workflows, usage events, and shield scans support flexibility readiness.',
    cap1: 'supportsWorkflowFlexibilitySignals',
    cap2: 'supportsUsageEventFlexibilitySignals',
    guidance:
      'Production flexibility rollout validates workflow flexibility, usage event flexibility signals, shield scan coverage, and flexibility readiness before production flexibility tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'run_workflows',
        tableName: 'run_workflows',
        requiredTables: ['run_workflows'],
        sql: `SELECT COUNT(*)::text AS count FROM run_workflows WHERE workspace_id = $1`,
      },
      {
        domain: 'usage_events',
        tableName: 'usage_events',
        requiredTables: ['usage_events'],
        sql: `SELECT COUNT(*)::text AS count FROM usage_events WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      run_workflows: 'Run workflows',
      usage_events: 'Usage events',
    },
    signalsHint: 'Run outcomes, run workflows, and usage events',
  },
  {
    name: 'extensibility',
    Name: 'Extensibility',
    percent: 'extensibilityPercent',
    metricDomain: 'agent_outputs',
    metricLabel: 'Agent output extensibility',
    metricDetail: 'agent output extensibility',
    action: 'refresh_extensibility_summary',
    tables: ['agent_outputs', 'artifacts', 'moderator_syntheses'],
    check1: {
      key: 'agentOutputsTableExists',
      name: 'agent_output_extensibility',
      label: 'Agent output extensibility',
      table: 'agent_outputs',
    },
    check2: {
      key: 'artifactsTableExists',
      name: 'artifact_extensibility',
      label: 'Artifact extensibility',
      table: 'artifacts',
    },
    check3Key: 'moderatorSynthesesTableExists',
    coverageKeys: {
      agent_outputs: 'agentOutputsTableExists',
      artifacts: 'artifactsTableExists',
      moderator_syntheses: 'moderatorSynthesesTableExists',
    },
    readiness: 'extension_readiness_signal',
    readinessLabel: 'Extension readiness signal',
    readinessDetail:
      'Agent outputs, persisted artifacts, and moderator syntheses support extension readiness.',
    cap1: 'supportsAgentOutputExtensibilitySignals',
    cap2: 'supportsArtifactExtensibilitySignals',
    guidance:
      'Production extensibility rollout validates agent output extensibility, artifact extensibility signals, synthesis coverage, and extension readiness before production extensibility tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'agent_outputs',
        tableName: 'agent_outputs',
        requiredTables: ['agent_outputs', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM agent_outputs ao INNER JOIN runs r ON r.run_id = ao.run_id WHERE r.workspace_id = $1`,
      },
      {
        domain: 'artifacts',
        tableName: 'artifacts',
        requiredTables: ['artifacts'],
        sql: `SELECT COUNT(*)::text AS count FROM artifacts WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      agent_outputs: 'Agent outputs',
      artifacts: 'Artifacts',
    },
    signalsHint: 'Run outcomes, agent outputs, and artifacts',
  },
  {
    name: 'modifiability',
    Name: 'Modifiability',
    percent: 'modifiabilityPercent',
    metricDomain: 'idempotency_keys',
    metricLabel: 'Idempotency key modifiability',
    metricDetail: 'idempotency key modifiability',
    action: 'refresh_modifiability_summary',
    tables: ['idempotency_keys', 'billing_records', 'workspace_memberships'],
    check1: {
      key: 'idempotencyKeysTableExists',
      name: 'idempotency_key_modifiability',
      label: 'Idempotency key modifiability',
      table: 'idempotency_keys',
    },
    check2: {
      key: 'billingRecordsTableExists',
      name: 'billing_record_modifiability',
      label: 'Billing record modifiability',
      table: 'billing_records',
    },
    check3Key: 'workspaceMembershipsTableExists',
    coverageKeys: {
      idempotency_keys: 'idempotencyKeysTableExists',
      billing_records: 'billingRecordsTableExists',
      workspace_memberships: 'workspaceMembershipsTableExists',
    },
    readiness: 'modification_readiness_signal',
    readinessLabel: 'Modification readiness signal',
    readinessDetail:
      'Idempotency keys, billing records, and workspace memberships support modification readiness.',
    cap1: 'supportsIdempotencyKeyModifiabilitySignals',
    cap2: 'supportsBillingRecordModifiabilitySignals',
    guidance:
      'Production modifiability rollout validates idempotency key modifiability, billing record modifiability signals, workspace membership coverage, and modification readiness before production modifiability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'idempotency_keys',
        tableName: 'idempotency_keys',
        requiredTables: ['idempotency_keys'],
        sql: `SELECT COUNT(*)::text AS count FROM idempotency_keys WHERE workspace_id = $1`,
      },
      {
        domain: 'workspace_memberships',
        tableName: 'workspace_memberships',
        requiredTables: ['workspace_memberships'],
        sql: `SELECT COUNT(*)::text AS count FROM workspace_memberships WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      idempotency_keys: 'Idempotency keys',
      workspace_memberships: 'Workspace memberships',
    },
    signalsHint: 'Run outcomes, idempotency keys, and workspace memberships',
  },
]

export const patchMeta = milestones.map((m, index) => ({
  ...m,
  version: `v5.${96 + index}`,
  domainFormatter: `format${m.Name}Domain`,
}))
