export const milestones = [
  {
    name: 'discernibility',
    Name: 'Discernibility',
    percent: 'discernibilityPercent',
    metricDomain: 'moderator_syntheses',
    metricLabel: 'Moderator synthesis discernibility',
    metricDetail: 'moderator synthesis discernibility',
    action: 'refresh_discernibility_summary',
    tables: ['moderator_syntheses', 'agent_outputs', 'artifacts'],
    check1: {
      key: 'moderatorSynthesesTableExists',
      name: 'synthesis_discernibility',
      label: 'Synthesis discernibility',
      table: 'moderator_syntheses',
    },
    check2: {
      key: 'agentOutputsTableExists',
      name: 'agent_output_discernibility',
      label: 'Agent output discernibility',
      table: 'agent_outputs',
    },
    check3Key: 'artifactsTableExists',
    coverageKeys: {
      moderator_syntheses: 'moderatorSynthesesTableExists',
      agent_outputs: 'agentOutputsTableExists',
      artifacts: 'artifactsTableExists',
    },
    readiness: 'discernment_readiness_signal',
    readinessLabel: 'Discernment readiness signal',
    readinessDetail:
      'Moderator syntheses, agent outputs, and persisted artifacts support discernment readiness.',
    cap1: 'supportsSynthesisDiscernibilitySignals',
    cap2: 'supportsAgentOutputDiscernibilitySignals',
    guidance:
      'Production discernibility rollout validates synthesis discernibility, agent output discernibility signals, artifact coverage, and discernment readiness before production discernibility tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'moderator_syntheses',
        tableName: 'moderator_syntheses',
        requiredTables: ['moderator_syntheses', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM moderator_syntheses ms INNER JOIN runs r ON r.run_id = ms.run_id WHERE r.workspace_id = $1`,
      },
      {
        domain: 'agent_outputs',
        tableName: 'agent_outputs',
        requiredTables: ['agent_outputs', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM agent_outputs ao INNER JOIN runs r ON r.run_id = ao.run_id WHERE r.workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      moderator_syntheses: 'Moderator syntheses',
      agent_outputs: 'Agent outputs',
    },
    signalsHint: 'Run outcomes, moderator syntheses, and agent outputs',
  },
  {
    name: 'distinctiveness',
    Name: 'Distinctiveness',
    percent: 'distinctivenessPercent',
    metricDomain: 'idempotency_keys',
    metricLabel: 'Idempotency key distinctiveness',
    metricDetail: 'idempotency key distinctiveness',
    action: 'refresh_distinctiveness_summary',
    tables: ['idempotency_keys', 'usage_events', 'billing_webhook_events'],
    check1: {
      key: 'idempotencyKeysTableExists',
      name: 'idempotency_key_distinctiveness',
      label: 'Idempotency key distinctiveness',
      table: 'idempotency_keys',
    },
    check2: {
      key: 'usageEventsTableExists',
      name: 'usage_event_distinctiveness',
      label: 'Usage event distinctiveness',
      table: 'usage_events',
    },
    check3Key: 'billingWebhookEventsTableExists',
    coverageKeys: {
      idempotency_keys: 'idempotencyKeysTableExists',
      usage_events: 'usageEventsTableExists',
      billing_webhook_events: 'billingWebhookEventsTableExists',
    },
    readiness: 'distinction_readiness_signal',
    readinessLabel: 'Distinction readiness signal',
    readinessDetail:
      'Idempotency keys, usage events, and billing webhook events support distinction readiness.',
    cap1: 'supportsIdempotencyKeyDistinctivenessSignals',
    cap2: 'supportsUsageEventDistinctivenessSignals',
    guidance:
      'Production distinctiveness rollout validates idempotency key distinctiveness, usage event distinctiveness signals, billing webhook coverage, and distinction readiness before production distinctiveness tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'idempotency_keys',
        tableName: 'idempotency_keys',
        requiredTables: ['idempotency_keys'],
        sql: `SELECT COUNT(*)::text AS count FROM idempotency_keys WHERE workspace_id = $1`,
      },
      {
        domain: 'usage_events',
        tableName: 'usage_events',
        requiredTables: ['usage_events'],
        sql: `SELECT COUNT(*)::text AS count FROM usage_events WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      idempotency_keys: 'Idempotency keys',
      usage_events: 'Usage events',
    },
    signalsHint: 'Run outcomes, idempotency keys, and usage events',
  },
  {
    name: 'conspicuousness',
    Name: 'Conspicuousness',
    percent: 'conspicuousnessPercent',
    metricDomain: 'workspace_memberships',
    metricLabel: 'Membership conspicuousness',
    metricDetail: 'membership conspicuousness',
    action: 'refresh_conspicuousness_summary',
    tables: ['workspace_memberships', 'usage_events', 'billing_notifications'],
    check1: {
      key: 'workspaceMembershipsTableExists',
      name: 'membership_conspicuousness',
      label: 'Membership conspicuousness',
      table: 'workspace_memberships',
    },
    check2: {
      key: 'usageEventsTableExists',
      name: 'usage_event_conspicuousness',
      label: 'Usage event conspicuousness',
      table: 'usage_events',
    },
    check3Key: 'billingNotificationsTableExists',
    coverageKeys: {
      workspace_memberships: 'workspaceMembershipsTableExists',
      usage_events: 'usageEventsTableExists',
      billing_notifications: 'billingNotificationsTableExists',
    },
    readiness: 'conspicuousness_readiness_signal',
    readinessLabel: 'Conspicuousness readiness signal',
    readinessDetail:
      'Workspace memberships, usage events, and billing notifications support conspicuousness readiness.',
    cap1: 'supportsMembershipConspicuousnessSignals',
    cap2: 'supportsUsageEventConspicuousnessSignals',
    guidance:
      'Production conspicuousness rollout validates membership conspicuousness, usage event conspicuousness signals, billing notification coverage, and conspicuousness readiness before production conspicuousness tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'workspace_memberships',
        tableName: 'workspace_memberships',
        requiredTables: ['workspace_memberships'],
        sql: `SELECT COUNT(*)::text AS count FROM workspace_memberships WHERE workspace_id = $1`,
      },
      {
        domain: 'usage_events',
        tableName: 'usage_events',
        requiredTables: ['usage_events'],
        sql: `SELECT COUNT(*)::text AS count FROM usage_events WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      workspace_memberships: 'Workspace memberships',
      usage_events: 'Usage events',
    },
    signalsHint: 'Run outcomes, workspace memberships, and usage events',
  },
  {
    name: 'detectability',
    Name: 'Detectability',
    percent: 'detectabilityPercent',
    metricDomain: 'billing_webhook_events',
    metricLabel: 'Billing webhook detectability',
    metricDetail: 'billing webhook detectability',
    action: 'refresh_detectability_summary',
    tables: ['billing_webhook_events', 'billing_notifications', 'idempotency_keys'],
    check1: {
      key: 'billingWebhookEventsTableExists',
      name: 'billing_webhook_detectability',
      label: 'Billing webhook detectability',
      table: 'billing_webhook_events',
    },
    check2: {
      key: 'billingNotificationsTableExists',
      name: 'billing_notification_detectability',
      label: 'Billing notification detectability',
      table: 'billing_notifications',
    },
    check3Key: 'idempotencyKeysTableExists',
    coverageKeys: {
      billing_webhook_events: 'billingWebhookEventsTableExists',
      billing_notifications: 'billingNotificationsTableExists',
      idempotency_keys: 'idempotencyKeysTableExists',
    },
    readiness: 'detection_readiness_signal',
    readinessLabel: 'Detection readiness signal',
    readinessDetail:
      'Billing webhook events, billing notifications, and idempotency keys support detection readiness.',
    cap1: 'supportsBillingWebhookDetectabilitySignals',
    cap2: 'supportsBillingNotificationDetectabilitySignals',
    guidance:
      'Production detectability rollout validates billing webhook detectability, billing notification detectability signals, idempotency key coverage, and detection readiness before production detectability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'billing_webhook_events',
        tableName: 'billing_webhook_events',
        requiredTables: ['billing_webhook_events'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_webhook_events WHERE workspace_id = $1`,
      },
      {
        domain: 'billing_notifications',
        tableName: 'billing_notifications',
        requiredTables: ['billing_notifications'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_notifications WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      billing_webhook_events: 'Billing webhook events',
      billing_notifications: 'Billing notifications',
    },
    signalsHint: 'Run outcomes, billing webhook events, and billing notifications',
  },
  {
    name: 'describability',
    Name: 'Describability',
    percent: 'describabilityPercent',
    metricDomain: 'run_workflows',
    metricLabel: 'Workflow describability',
    metricDetail: 'workflow describability',
    action: 'refresh_describability_summary',
    tables: ['run_workflows', 'agent_outputs', 'moderator_syntheses'],
    check1: {
      key: 'runWorkflowsTableExists',
      name: 'workflow_describability',
      label: 'Workflow describability',
      table: 'run_workflows',
    },
    check2: {
      key: 'agentOutputsTableExists',
      name: 'agent_output_describability',
      label: 'Agent output describability',
      table: 'agent_outputs',
    },
    check3Key: 'moderatorSynthesesTableExists',
    coverageKeys: {
      run_workflows: 'runWorkflowsTableExists',
      agent_outputs: 'agentOutputsTableExists',
      moderator_syntheses: 'moderatorSynthesesTableExists',
    },
    readiness: 'description_readiness_signal',
    readinessLabel: 'Description readiness signal',
    readinessDetail:
      'Run workflows, agent outputs, and moderator syntheses support description readiness.',
    cap1: 'supportsWorkflowDescribabilitySignals',
    cap2: 'supportsAgentOutputDescribabilitySignals',
    guidance:
      'Production describability rollout validates workflow describability, agent output describability signals, synthesis coverage, and description readiness before production describability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'run_workflows',
        tableName: 'run_workflows',
        requiredTables: ['run_workflows'],
        sql: `SELECT COUNT(*)::text AS count FROM run_workflows WHERE workspace_id = $1`,
      },
      {
        domain: 'agent_outputs',
        tableName: 'agent_outputs',
        requiredTables: ['agent_outputs', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM agent_outputs ao INNER JOIN runs r ON r.run_id = ao.run_id WHERE r.workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      run_workflows: 'Run workflows',
      agent_outputs: 'Agent outputs',
    },
    signalsHint: 'Run outcomes, run workflows, and agent outputs',
  },
]

export const patchMeta = milestones.map((m, index) => ({
  ...m,
  version: `v5.${156 + index}`,
  domainFormatter: `format${m.Name}Domain`,
}))
