export const milestones = [
  {
    name: 'typifiability',
    Name: 'Typifiability',
    percent: 'typifiabilityPercent',
    metricDomain: 'workspace_usage_limits',
    metricLabel: 'Workspace limit typifiability',
    metricDetail: 'workspace limit typifiability',
    action: 'refresh_typifiability_summary',
    tables: ['workspace_usage_limits', 'usage_events', 'billing_records'],
    check1: {
      key: 'workspaceUsageLimitsTableExists',
      name: 'workspace_limit_typifiability',
      label: 'Workspace limit typifiability',
      table: 'workspace_usage_limits',
    },
    check2: {
      key: 'usageEventsTableExists',
      name: 'usage_event_typifiability',
      label: 'Usage event typifiability',
      table: 'usage_events',
    },
    check3Key: 'billingRecordsTableExists',
    coverageKeys: {
      workspace_usage_limits: 'workspaceUsageLimitsTableExists',
      usage_events: 'usageEventsTableExists',
      billing_records: 'billingRecordsTableExists',
    },
    readiness: 'typification_readiness_signal',
    readinessLabel: 'Typification readiness signal',
    readinessDetail:
      'Workspace usage limits, usage events, and billing records support typification readiness.',
    cap1: 'supportsWorkspaceLimitTypifiabilitySignals',
    cap2: 'supportsUsageEventTypifiabilitySignals',
    guidance:
      'Production typifiability rollout validates workspace limit typifiability, usage event typifiability signals, billing record coverage, and typification readiness before production typifiability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'workspace_usage_limits',
        tableName: 'workspace_usage_limits',
        requiredTables: ['workspace_usage_limits'],
        sql: `SELECT COUNT(*)::text AS count FROM workspace_usage_limits WHERE workspace_id = $1`,
      },
      {
        domain: 'usage_events',
        tableName: 'usage_events',
        requiredTables: ['usage_events'],
        sql: `SELECT COUNT(*)::text AS count FROM usage_events WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      workspace_usage_limits: 'Workspace usage limits',
      usage_events: 'Usage events',
    },
    signalsHint: 'Run outcomes, workspace usage limits, and usage events',
  },
  {
    name: 'metaphorizability',
    Name: 'Metaphorizability',
    percent: 'metaphorizabilityPercent',
    metricDomain: 'workspace_provider_credentials',
    metricLabel: 'Provider credential metaphorizability',
    metricDetail: 'provider credential metaphorizability',
    action: 'refresh_metaphorizability_summary',
    tables: [
      'workspace_provider_credentials',
      'model_registry_entries',
      'billing_webhook_events',
    ],
    check1: {
      key: 'workspaceProviderCredentialsTableExists',
      name: 'provider_credential_metaphorizability',
      label: 'Provider credential metaphorizability',
      table: 'workspace_provider_credentials',
    },
    check2: {
      key: 'modelRegistryEntriesTableExists',
      name: 'model_registry_metaphorizability',
      label: 'Model registry metaphorizability',
      table: 'model_registry_entries',
    },
    check3Key: 'billingWebhookEventsTableExists',
    coverageKeys: {
      workspace_provider_credentials: 'workspaceProviderCredentialsTableExists',
      model_registry_entries: 'modelRegistryEntriesTableExists',
      billing_webhook_events: 'billingWebhookEventsTableExists',
    },
    readiness: 'metaphorization_readiness_signal',
    readinessLabel: 'Metaphorization readiness signal',
    readinessDetail:
      'Workspace provider credentials, model registry entries, and billing webhook events support metaphorization readiness.',
    cap1: 'supportsProviderCredentialMetaphorizabilitySignals',
    cap2: 'supportsModelRegistryMetaphorizabilitySignals',
    guidance:
      'Production metaphorizability rollout validates provider credential metaphorizability, model registry metaphorizability signals, billing webhook coverage, and metaphorization readiness before production metaphorizability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'workspace_provider_credentials',
        tableName: 'workspace_provider_credentials',
        requiredTables: ['workspace_provider_credentials'],
        sql: `SELECT COUNT(*)::text AS count FROM workspace_provider_credentials WHERE workspace_id = $1`,
      },
      {
        domain: 'billing_webhook_events',
        tableName: 'billing_webhook_events',
        requiredTables: ['billing_webhook_events'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_webhook_events WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      workspace_provider_credentials: 'Provider credentials',
      billing_webhook_events: 'Billing webhook events',
    },
    signalsHint: 'Run outcomes, provider credentials, and billing webhook events',
  },
  {
    name: 'dramatizability',
    Name: 'Dramatizability',
    percent: 'dramatizabilityPercent',
    metricDomain: 'artifacts',
    metricLabel: 'Artifact dramatizability',
    metricDetail: 'artifact dramatizability',
    action: 'refresh_dramatizability_summary',
    tables: ['artifacts', 'agent_outputs', 'moderator_syntheses'],
    check1: {
      key: 'artifactsTableExists',
      name: 'artifact_dramatizability',
      label: 'Artifact dramatizability',
      table: 'artifacts',
    },
    check2: {
      key: 'agentOutputsTableExists',
      name: 'agent_output_dramatizability',
      label: 'Agent output dramatizability',
      table: 'agent_outputs',
    },
    check3Key: 'moderatorSynthesesTableExists',
    coverageKeys: {
      artifacts: 'artifactsTableExists',
      agent_outputs: 'agentOutputsTableExists',
      moderator_syntheses: 'moderatorSynthesesTableExists',
    },
    readiness: 'dramatization_readiness_signal',
    readinessLabel: 'Dramatization readiness signal',
    readinessDetail:
      'Artifacts, agent outputs, and moderator syntheses support dramatization readiness.',
    cap1: 'supportsArtifactDramatizabilitySignals',
    cap2: 'supportsAgentOutputDramatizabilitySignals',
    guidance:
      'Production dramatizability rollout validates artifact dramatizability, agent output dramatizability signals, synthesis coverage, and dramatization readiness before production dramatizability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'artifacts',
        tableName: 'artifacts',
        requiredTables: ['artifacts'],
        sql: `SELECT COUNT(*)::text AS count FROM artifacts WHERE workspace_id = $1`,
      },
      {
        domain: 'agent_outputs',
        tableName: 'agent_outputs',
        requiredTables: ['agent_outputs', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM agent_outputs ao INNER JOIN runs r ON r.run_id = ao.run_id WHERE r.workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      artifacts: 'Artifacts',
      agent_outputs: 'Agent outputs',
    },
    signalsHint: 'Run outcomes, artifacts, and agent outputs',
  },
  {
    name: 'personifiability',
    Name: 'Personifiability',
    percent: 'personifiabilityPercent',
    metricDomain: 'agent_outputs',
    metricLabel: 'Agent output personifiability',
    metricDetail: 'agent output personifiability',
    action: 'refresh_personifiability_summary',
    tables: ['agent_outputs', 'moderator_syntheses', 'artifacts'],
    check1: {
      key: 'agentOutputsTableExists',
      name: 'agent_output_personifiability',
      label: 'Agent output personifiability',
      table: 'agent_outputs',
    },
    check2: {
      key: 'moderatorSynthesesTableExists',
      name: 'synthesis_personifiability',
      label: 'Synthesis personifiability',
      table: 'moderator_syntheses',
    },
    check3Key: 'artifactsTableExists',
    coverageKeys: {
      agent_outputs: 'agentOutputsTableExists',
      moderator_syntheses: 'moderatorSynthesesTableExists',
      artifacts: 'artifactsTableExists',
    },
    readiness: 'personification_readiness_signal',
    readinessLabel: 'Personification readiness signal',
    readinessDetail:
      'Agent outputs, moderator syntheses, and persisted artifacts support personification readiness.',
    cap1: 'supportsAgentOutputPersonifiabilitySignals',
    cap2: 'supportsSynthesisPersonifiabilitySignals',
    guidance:
      'Production personifiability rollout validates agent output personifiability, synthesis personifiability signals, artifact coverage, and personification readiness before production personifiability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'agent_outputs',
        tableName: 'agent_outputs',
        requiredTables: ['agent_outputs', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM agent_outputs ao INNER JOIN runs r ON r.run_id = ao.run_id WHERE r.workspace_id = $1`,
      },
      {
        domain: 'moderator_syntheses',
        tableName: 'moderator_syntheses',
        requiredTables: ['moderator_syntheses', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM moderator_syntheses ms INNER JOIN runs r ON r.run_id = ms.run_id WHERE r.workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      agent_outputs: 'Agent outputs',
      moderator_syntheses: 'Moderator syntheses',
    },
    signalsHint: 'Run outcomes, agent outputs, and moderator syntheses',
  },
  {
    name: 'materializability',
    Name: 'Materializability',
    percent: 'materializabilityPercent',
    metricDomain: 'run_workflows',
    metricLabel: 'Workflow materializability',
    metricDetail: 'workflow materializability',
    action: 'refresh_materializability_summary',
    tables: ['run_workflows', 'artifacts', 'billing_notifications'],
    check1: {
      key: 'runWorkflowsTableExists',
      name: 'workflow_materializability',
      label: 'Workflow materializability',
      table: 'run_workflows',
    },
    check2: {
      key: 'artifactsTableExists',
      name: 'artifact_materializability',
      label: 'Artifact materializability',
      table: 'artifacts',
    },
    check3Key: 'billingNotificationsTableExists',
    coverageKeys: {
      run_workflows: 'runWorkflowsTableExists',
      artifacts: 'artifactsTableExists',
      billing_notifications: 'billingNotificationsTableExists',
    },
    readiness: 'materialization_readiness_signal',
    readinessLabel: 'Materialization readiness signal',
    readinessDetail:
      'Run workflows, artifacts, and billing notifications support materialization readiness.',
    cap1: 'supportsWorkflowMaterializabilitySignals',
    cap2: 'supportsArtifactMaterializabilitySignals',
    guidance:
      'Production materializability rollout validates workflow materializability, artifact materializability signals, billing notification coverage, and materialization readiness before production materializability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'run_workflows',
        tableName: 'run_workflows',
        requiredTables: ['run_workflows'],
        sql: `SELECT COUNT(*)::text AS count FROM run_workflows WHERE workspace_id = $1`,
      },
      {
        domain: 'artifacts',
        tableName: 'artifacts',
        requiredTables: ['artifacts'],
        sql: `SELECT COUNT(*)::text AS count FROM artifacts WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      run_workflows: 'Run workflows',
      artifacts: 'Artifacts',
    },
    signalsHint: 'Run outcomes, run workflows, and artifacts',
  },
]

export const patchMeta = milestones.map((m, index) => ({
  ...m,
  version: `v5.${176 + index}`,
  domainFormatter: `format${m.Name}Domain`,
}))
