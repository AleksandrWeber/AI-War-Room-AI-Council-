export const milestones = [
  {
    name: 'attributability',
    Name: 'Attributability',
    percent: 'attributabilityPercent',
    metricDomain: 'agent_outputs',
    metricLabel: 'Agent output attributability',
    metricDetail: 'agent output attributability',
    action: 'refresh_attributability_summary',
    tables: ['agent_outputs', 'moderator_syntheses', 'artifacts'],
    check1: {
      key: 'agentOutputsTableExists',
      name: 'agent_output_attributability',
      label: 'Agent output attributability',
      table: 'agent_outputs',
    },
    check2: {
      key: 'moderatorSynthesesTableExists',
      name: 'synthesis_attributability',
      label: 'Synthesis attributability',
      table: 'moderator_syntheses',
    },
    check3Key: 'artifactsTableExists',
    coverageKeys: {
      agent_outputs: 'agentOutputsTableExists',
      moderator_syntheses: 'moderatorSynthesesTableExists',
      artifacts: 'artifactsTableExists',
    },
    readiness: 'attribution_readiness_signal',
    readinessLabel: 'Attribution readiness signal',
    readinessDetail:
      'Agent outputs, moderator syntheses, and persisted artifacts support attribution readiness.',
    cap1: 'supportsAgentOutputAttributabilitySignals',
    cap2: 'supportsSynthesisAttributabilitySignals',
    guidance:
      'Production attributability rollout validates agent output attributability, synthesis attributability signals, artifact coverage, and attribution readiness before production attributability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'agent_outputs',
        tableName: 'agent_outputs',
        requiredTables: ['agent_outputs', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM agent_outputs ao INNER JOIN runs r ON r.run_id = ao.run_id WHERE r.workspace_id = $1`,
      },
      {
        domain: 'artifacts',
        tableName: 'artifacts',
        requiredTables: ['artifacts'],
        sql: `SELECT COUNT(*)::text AS count FROM artifacts WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      agent_outputs: 'Agent outputs',
      artifacts: 'Artifacts',
    },
    signalsHint: 'Run outcomes, agent outputs, and artifacts',
  },
  {
    name: 'identifiability',
    Name: 'Identifiability',
    percent: 'identifiabilityPercent',
    metricDomain: 'idempotency_keys',
    metricLabel: 'Idempotency key identifiability',
    metricDetail: 'idempotency key identifiability',
    action: 'refresh_identifiability_summary',
    tables: ['idempotency_keys', 'workspace_provider_credentials', 'usage_events'],
    check1: {
      key: 'idempotencyKeysTableExists',
      name: 'idempotency_key_identifiability',
      label: 'Idempotency key identifiability',
      table: 'idempotency_keys',
    },
    check2: {
      key: 'workspaceProviderCredentialsTableExists',
      name: 'provider_credential_identifiability',
      label: 'Provider credential identifiability',
      table: 'workspace_provider_credentials',
    },
    check3Key: 'usageEventsTableExists',
    coverageKeys: {
      idempotency_keys: 'idempotencyKeysTableExists',
      workspace_provider_credentials: 'workspaceProviderCredentialsTableExists',
      usage_events: 'usageEventsTableExists',
    },
    readiness: 'identity_readiness_signal',
    readinessLabel: 'Identity readiness signal',
    readinessDetail:
      'Idempotency keys, workspace provider credentials, and usage events support identity readiness.',
    cap1: 'supportsIdempotencyKeyIdentifiabilitySignals',
    cap2: 'supportsProviderCredentialIdentifiabilitySignals',
    guidance:
      'Production identifiability rollout validates idempotency key identifiability, provider credential identifiability signals, usage event coverage, and identity readiness before production identifiability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'idempotency_keys',
        tableName: 'idempotency_keys',
        requiredTables: ['idempotency_keys'],
        sql: `SELECT COUNT(*)::text AS count FROM idempotency_keys WHERE workspace_id = $1`,
      },
      {
        domain: 'usage_events',
        tableName: 'usage_events',
        requiredTables: ['usage_events'],
        sql: `SELECT COUNT(*)::text AS count FROM usage_events WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      idempotency_keys: 'Idempotency keys',
      usage_events: 'Usage events',
    },
    signalsHint: 'Run outcomes, idempotency keys, and usage events',
  },
  {
    name: 'comparability',
    Name: 'Comparability',
    percent: 'comparabilityPercent',
    metricDomain: 'billing_invoices',
    metricLabel: 'Billing invoice comparability',
    metricDetail: 'billing invoice comparability',
    action: 'refresh_comparability_summary',
    tables: ['billing_invoices', 'billing_records', 'billing_meter_usage_reports'],
    check1: {
      key: 'billingInvoicesTableExists',
      name: 'billing_invoice_comparability',
      label: 'Billing invoice comparability',
      table: 'billing_invoices',
    },
    check2: {
      key: 'billingRecordsTableExists',
      name: 'billing_record_comparability',
      label: 'Billing record comparability',
      table: 'billing_records',
    },
    check3Key: 'billingMeterUsageReportsTableExists',
    coverageKeys: {
      billing_invoices: 'billingInvoicesTableExists',
      billing_records: 'billingRecordsTableExists',
      billing_meter_usage_reports: 'billingMeterUsageReportsTableExists',
    },
    readiness: 'comparison_readiness_signal',
    readinessLabel: 'Comparison readiness signal',
    readinessDetail:
      'Billing invoices, billing records, and meter usage reports support comparison readiness.',
    cap1: 'supportsBillingInvoiceComparabilitySignals',
    cap2: 'supportsBillingRecordComparabilitySignals',
    guidance:
      'Production comparability rollout validates billing invoice comparability, billing record comparability signals, meter usage coverage, and comparison readiness before production comparability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'billing_invoices',
        tableName: 'billing_invoices',
        requiredTables: ['billing_invoices'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_invoices WHERE workspace_id = $1`,
      },
      {
        domain: 'billing_meter_usage_reports',
        tableName: 'billing_meter_usage_reports',
        requiredTables: ['billing_meter_usage_reports'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_meter_usage_reports WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      billing_invoices: 'Billing invoices',
      billing_meter_usage_reports: 'Meter usage reports',
    },
    signalsHint: 'Run outcomes, billing invoices, and meter usage reports',
  },
  {
    name: 'distinguishability',
    Name: 'Distinguishability',
    percent: 'distinguishabilityPercent',
    metricDomain: 'moderator_syntheses',
    metricLabel: 'Moderator synthesis distinguishability',
    metricDetail: 'moderator synthesis distinguishability',
    action: 'refresh_distinguishability_summary',
    tables: ['moderator_syntheses', 'agent_outputs', 'run_workflows'],
    check1: {
      key: 'moderatorSynthesesTableExists',
      name: 'synthesis_distinguishability',
      label: 'Synthesis distinguishability',
      table: 'moderator_syntheses',
    },
    check2: {
      key: 'agentOutputsTableExists',
      name: 'agent_output_distinguishability',
      label: 'Agent output distinguishability',
      table: 'agent_outputs',
    },
    check3Key: 'runWorkflowsTableExists',
    coverageKeys: {
      moderator_syntheses: 'moderatorSynthesesTableExists',
      agent_outputs: 'agentOutputsTableExists',
      run_workflows: 'runWorkflowsTableExists',
    },
    readiness: 'differentiation_readiness_signal',
    readinessLabel: 'Differentiation readiness signal',
    readinessDetail:
      'Moderator syntheses, agent outputs, and run workflows support differentiation readiness.',
    cap1: 'supportsSynthesisDistinguishabilitySignals',
    cap2: 'supportsAgentOutputDistinguishabilitySignals',
    guidance:
      'Production distinguishability rollout validates synthesis distinguishability, agent output distinguishability signals, run workflow coverage, and differentiation readiness before production distinguishability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'moderator_syntheses',
        tableName: 'moderator_syntheses',
        requiredTables: ['moderator_syntheses', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM moderator_syntheses ms INNER JOIN runs r ON r.run_id = ms.run_id WHERE r.workspace_id = $1`,
      },
      {
        domain: 'run_workflows',
        tableName: 'run_workflows',
        requiredTables: ['run_workflows'],
        sql: `SELECT COUNT(*)::text AS count FROM run_workflows WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      moderator_syntheses: 'Moderator syntheses',
      run_workflows: 'Run workflows',
    },
    signalsHint: 'Run outcomes, moderator syntheses, and run workflows',
  },
  {
    name: 'assignability',
    Name: 'Assignability',
    percent: 'assignabilityPercent',
    metricDomain: 'workspace_memberships',
    metricLabel: 'Workspace membership assignability',
    metricDetail: 'workspace membership assignability',
    action: 'refresh_assignability_summary',
    tables: ['workspace_memberships', 'workspace_provider_credentials', 'billing_notifications'],
    check1: {
      key: 'workspaceMembershipsTableExists',
      name: 'workspace_membership_assignability',
      label: 'Workspace membership assignability',
      table: 'workspace_memberships',
    },
    check2: {
      key: 'workspaceProviderCredentialsTableExists',
      name: 'provider_credential_assignability',
      label: 'Provider credential assignability',
      table: 'workspace_provider_credentials',
    },
    check3Key: 'billingNotificationsTableExists',
    coverageKeys: {
      workspace_memberships: 'workspaceMembershipsTableExists',
      workspace_provider_credentials: 'workspaceProviderCredentialsTableExists',
      billing_notifications: 'billingNotificationsTableExists',
    },
    readiness: 'assignment_readiness_signal',
    readinessLabel: 'Assignment readiness signal',
    readinessDetail:
      'Workspace memberships, provider credentials, and billing notifications support assignment readiness.',
    cap1: 'supportsWorkspaceMembershipAssignabilitySignals',
    cap2: 'supportsProviderCredentialAssignabilitySignals',
    guidance:
      'Production assignability rollout validates workspace membership assignability, provider credential assignability signals, billing notification coverage, and assignment readiness before production assignability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'workspace_memberships',
        tableName: 'workspace_memberships',
        requiredTables: ['workspace_memberships'],
        sql: `SELECT COUNT(*)::text AS count FROM workspace_memberships WHERE workspace_id = $1`,
      },
      {
        domain: 'billing_notifications',
        tableName: 'billing_notifications',
        requiredTables: ['billing_notifications'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_notifications WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      workspace_memberships: 'Workspace memberships',
      billing_notifications: 'Billing notifications',
    },
    signalsHint: 'Run outcomes, workspace memberships, and billing notifications',
  },
]

export const patchMeta = milestones.map((m, index) => ({
  ...m,
  version: `v5.${81 + index}`,
  domainFormatter: `format${m.Name}Domain`,
}))
