export const milestones = [
  {
    name: 'learnability',
    Name: 'Learnability',
    percent: 'learnabilityPercent',
    metricDomain: 'agent_outputs',
    metricLabel: 'Agent output learnability',
    metricDetail: 'agent output learnability',
    action: 'refresh_learnability_summary',
    tables: ['agent_outputs', 'artifacts', 'moderator_syntheses'],
    check1: {
      key: 'agentOutputsTableExists',
      name: 'agent_output_learnability',
      label: 'Agent output learnability',
      table: 'agent_outputs',
    },
    check2: {
      key: 'artifactsTableExists',
      name: 'artifact_learnability',
      label: 'Artifact learnability',
      table: 'artifacts',
    },
    check3Key: 'moderatorSynthesesTableExists',
    coverageKeys: {
      agent_outputs: 'agentOutputsTableExists',
      artifacts: 'artifactsTableExists',
      moderator_syntheses: 'moderatorSynthesesTableExists',
    },
    readiness: 'learning_readiness_signal',
    readinessLabel: 'Learning readiness signal',
    readinessDetail:
      'Agent outputs, persisted artifacts, and moderator syntheses support learning readiness.',
    cap1: 'supportsAgentOutputLearnabilitySignals',
    cap2: 'supportsArtifactLearnabilitySignals',
    guidance:
      'Production learnability rollout validates agent output learnability, artifact learnability signals, synthesis coverage, and learning readiness before production learnability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'agent_outputs',
        tableName: 'agent_outputs',
        requiredTables: ['agent_outputs', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM agent_outputs ao INNER JOIN runs r ON r.run_id = ao.run_id WHERE r.workspace_id = $1`,
      },
      {
        domain: 'artifacts',
        tableName: 'artifacts',
        requiredTables: ['artifacts'],
        sql: `SELECT COUNT(*)::text AS count FROM artifacts WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      agent_outputs: 'Agent outputs',
      artifacts: 'Artifacts',
    },
    signalsHint: 'Run outcomes, agent outputs, and artifacts',
  },
  {
    name: 'deliverability',
    Name: 'Deliverability',
    percent: 'deliverabilityPercent',
    metricDomain: 'billing_notifications',
    metricLabel: 'Billing notification deliverability',
    metricDetail: 'billing notification deliverability',
    action: 'refresh_deliverability_summary',
    tables: ['billing_notifications', 'billing_webhook_events', 'usage_events'],
    check1: {
      key: 'billingNotificationsTableExists',
      name: 'billing_notification_deliverability',
      label: 'Billing notification deliverability',
      table: 'billing_notifications',
    },
    check2: {
      key: 'billingWebhookEventsTableExists',
      name: 'billing_webhook_deliverability',
      label: 'Billing webhook deliverability',
      table: 'billing_webhook_events',
    },
    check3Key: 'usageEventsTableExists',
    coverageKeys: {
      billing_notifications: 'billingNotificationsTableExists',
      billing_webhook_events: 'billingWebhookEventsTableExists',
      usage_events: 'usageEventsTableExists',
    },
    readiness: 'delivery_readiness_signal',
    readinessLabel: 'Delivery readiness signal',
    readinessDetail:
      'Billing notifications, billing webhook events, and usage events support delivery readiness.',
    cap1: 'supportsBillingNotificationDeliverabilitySignals',
    cap2: 'supportsBillingWebhookDeliverabilitySignals',
    guidance:
      'Production deliverability rollout validates billing notification deliverability, billing webhook deliverability signals, usage event coverage, and delivery readiness before production deliverability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'billing_notifications',
        tableName: 'billing_notifications',
        requiredTables: ['billing_notifications'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_notifications WHERE workspace_id = $1`,
      },
      {
        domain: 'billing_webhook_events',
        tableName: 'billing_webhook_events',
        requiredTables: ['billing_webhook_events'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_webhook_events WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      billing_notifications: 'Billing notifications',
      billing_webhook_events: 'Billing webhook events',
    },
    signalsHint: 'Run outcomes, billing notifications, and billing webhook events',
  },
  {
    name: 'understandability',
    Name: 'Understandability',
    percent: 'understandabilityPercent',
    metricDomain: 'moderator_syntheses',
    metricLabel: 'Moderator synthesis understandability',
    metricDetail: 'moderator synthesis understandability',
    action: 'refresh_understandability_summary',
    tables: ['moderator_syntheses', 'agent_outputs', 'artifacts'],
    check1: {
      key: 'moderatorSynthesesTableExists',
      name: 'synthesis_understandability',
      label: 'Synthesis understandability',
      table: 'moderator_syntheses',
    },
    check2: {
      key: 'agentOutputsTableExists',
      name: 'agent_output_understandability',
      label: 'Agent output understandability',
      table: 'agent_outputs',
    },
    check3Key: 'artifactsTableExists',
    coverageKeys: {
      moderator_syntheses: 'moderatorSynthesesTableExists',
      agent_outputs: 'agentOutputsTableExists',
      artifacts: 'artifactsTableExists',
    },
    readiness: 'understanding_readiness_signal',
    readinessLabel: 'Understanding readiness signal',
    readinessDetail:
      'Moderator syntheses, agent outputs, and persisted artifacts support understanding readiness.',
    cap1: 'supportsSynthesisUnderstandabilitySignals',
    cap2: 'supportsAgentOutputUnderstandabilitySignals',
    guidance:
      'Production understandability rollout validates synthesis understandability, agent output understandability signals, artifact coverage, and understanding readiness before production understandability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'moderator_syntheses',
        tableName: 'moderator_syntheses',
        requiredTables: ['moderator_syntheses', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM moderator_syntheses ms INNER JOIN runs r ON r.run_id = ms.run_id WHERE r.workspace_id = $1`,
      },
      {
        domain: 'agent_outputs',
        tableName: 'agent_outputs',
        requiredTables: ['agent_outputs', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM agent_outputs ao INNER JOIN runs r ON r.run_id = ao.run_id WHERE r.workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      moderator_syntheses: 'Moderator syntheses',
      agent_outputs: 'Agent outputs',
    },
    signalsHint: 'Run outcomes, moderator syntheses, and agent outputs',
  },
  {
    name: 'memorability',
    Name: 'Memorability',
    percent: 'memorabilityPercent',
    metricDomain: 'artifacts',
    metricLabel: 'Artifact memorability',
    metricDetail: 'artifact memorability',
    action: 'refresh_memorability_summary',
    tables: ['artifacts', 'run_workflows', 'idempotency_keys'],
    check1: {
      key: 'artifactsTableExists',
      name: 'artifact_memorability',
      label: 'Artifact memorability',
      table: 'artifacts',
    },
    check2: {
      key: 'runWorkflowsTableExists',
      name: 'workflow_memorability',
      label: 'Workflow memorability',
      table: 'run_workflows',
    },
    check3Key: 'idempotencyKeysTableExists',
    coverageKeys: {
      artifacts: 'artifactsTableExists',
      run_workflows: 'runWorkflowsTableExists',
      idempotency_keys: 'idempotencyKeysTableExists',
    },
    readiness: 'memory_readiness_signal',
    readinessLabel: 'Memory readiness signal',
    readinessDetail:
      'Artifacts, run workflows, and idempotency keys support memory readiness.',
    cap1: 'supportsArtifactMemorabilitySignals',
    cap2: 'supportsWorkflowMemorabilitySignals',
    guidance:
      'Production memorability rollout validates artifact memorability, workflow memorability signals, idempotency key coverage, and memory readiness before production memorability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'artifacts',
        tableName: 'artifacts',
        requiredTables: ['artifacts'],
        sql: `SELECT COUNT(*)::text AS count FROM artifacts WHERE workspace_id = $1`,
      },
      {
        domain: 'run_workflows',
        tableName: 'run_workflows',
        requiredTables: ['run_workflows'],
        sql: `SELECT COUNT(*)::text AS count FROM run_workflows WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      artifacts: 'Artifacts',
      run_workflows: 'Run workflows',
    },
    signalsHint: 'Run outcomes, artifacts, and run workflows',
  },
  {
    name: 'teachability',
    Name: 'Teachability',
    percent: 'teachabilityPercent',
    metricDomain: 'run_workflows',
    metricLabel: 'Workflow teachability',
    metricDetail: 'workflow teachability',
    action: 'refresh_teachability_summary',
    tables: ['run_workflows', 'agent_outputs', 'workspace_memberships'],
    check1: {
      key: 'runWorkflowsTableExists',
      name: 'workflow_teachability',
      label: 'Workflow teachability',
      table: 'run_workflows',
    },
    check2: {
      key: 'agentOutputsTableExists',
      name: 'agent_output_teachability',
      label: 'Agent output teachability',
      table: 'agent_outputs',
    },
    check3Key: 'workspaceMembershipsTableExists',
    coverageKeys: {
      run_workflows: 'runWorkflowsTableExists',
      agent_outputs: 'agentOutputsTableExists',
      workspace_memberships: 'workspaceMembershipsTableExists',
    },
    readiness: 'teaching_readiness_signal',
    readinessLabel: 'Teaching readiness signal',
    readinessDetail:
      'Run workflows, agent outputs, and workspace memberships support teaching readiness.',
    cap1: 'supportsWorkflowTeachabilitySignals',
    cap2: 'supportsAgentOutputTeachabilitySignals',
    guidance:
      'Production teachability rollout validates workflow teachability, agent output teachability signals, membership coverage, and teaching readiness before production teachability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'run_workflows',
        tableName: 'run_workflows',
        requiredTables: ['run_workflows'],
        sql: `SELECT COUNT(*)::text AS count FROM run_workflows WHERE workspace_id = $1`,
      },
      {
        domain: 'agent_outputs',
        tableName: 'agent_outputs',
        requiredTables: ['agent_outputs', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM agent_outputs ao INNER JOIN runs r ON r.run_id = ao.run_id WHERE r.workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      run_workflows: 'Run workflows',
      agent_outputs: 'Agent outputs',
    },
    signalsHint: 'Run outcomes, run workflows, and agent outputs',
  },
]

export const patchMeta = milestones.map((m, index) => ({
  ...m,
  version: `v5.${136 + index}`,
  domainFormatter: `format${m.Name}Domain`,
}))
