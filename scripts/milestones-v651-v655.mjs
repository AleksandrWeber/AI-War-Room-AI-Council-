export const milestones = [
  {
    name: 'recognizability',
    Name: 'Recognizability',
    percent: 'recognizabilityPercent',
    metricDomain: 'artifacts',
    metricLabel: 'Artifact recognizability',
    metricDetail: 'artifact recognizability',
    action: 'refresh_recognizability_summary',
    tables: ['artifacts', 'run_workflows', 'billing_notifications'],
    check1: {
      key: 'artifactsTableExists',
      name: 'artifact_recognizability',
      label: 'Artifact recognizability',
      table: 'artifacts',
    },
    check2: {
      key: 'runWorkflowsTableExists',
      name: 'workflow_recognizability',
      label: 'Workflow recognizability',
      table: 'run_workflows',
    },
    check3Key: 'billingNotificationsTableExists',
    coverageKeys: {
      artifacts: 'artifactsTableExists',
      run_workflows: 'runWorkflowsTableExists',
      billing_notifications: 'billingNotificationsTableExists',
    },
    readiness: 'recognition_readiness_signal',
    readinessLabel: 'Recognition readiness signal',
    readinessDetail:
      'Artifacts, run workflows, and billing notifications support recognition readiness.',
    cap1: 'supportsArtifactRecognizabilitySignals',
    cap2: 'supportsWorkflowRecognizabilitySignals',
    guidance:
      'Production recognizability rollout validates artifact recognizability, workflow recognizability signals, billing notification coverage, and recognition readiness before production recognizability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'artifacts',
        tableName: 'artifacts',
        requiredTables: ['artifacts'],
        sql: `SELECT COUNT(*)::text AS count FROM artifacts WHERE workspace_id = $1`,
      },
      {
        domain: 'run_workflows',
        tableName: 'run_workflows',
        requiredTables: ['run_workflows'],
        sql: `SELECT COUNT(*)::text AS count FROM run_workflows WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      artifacts: 'Artifacts',
      run_workflows: 'Run workflows',
    },
    signalsHint: 'Run outcomes, artifacts, and run workflows',
  },
  {
    name: 'interpretability',
    Name: 'Interpretability',
    percent: 'interpretabilityPercent',
    metricDomain: 'agent_outputs',
    metricLabel: 'Agent output interpretability',
    metricDetail: 'agent output interpretability',
    action: 'refresh_interpretability_summary',
    tables: ['agent_outputs', 'moderator_syntheses', 'artifacts'],
    check1: {
      key: 'agentOutputsTableExists',
      name: 'agent_output_interpretability',
      label: 'Agent output interpretability',
      table: 'agent_outputs',
    },
    check2: {
      key: 'moderatorSynthesesTableExists',
      name: 'synthesis_interpretability',
      label: 'Synthesis interpretability',
      table: 'moderator_syntheses',
    },
    check3Key: 'artifactsTableExists',
    coverageKeys: {
      agent_outputs: 'agentOutputsTableExists',
      moderator_syntheses: 'moderatorSynthesesTableExists',
      artifacts: 'artifactsTableExists',
    },
    readiness: 'interpretability_readiness_signal',
    readinessLabel: 'Interpretability readiness signal',
    readinessDetail:
      'Agent outputs, moderator syntheses, and persisted artifacts support interpretability readiness.',
    cap1: 'supportsAgentOutputInterpretabilitySignals',
    cap2: 'supportsSynthesisInterpretabilitySignals',
    guidance:
      'Production interpretability rollout validates agent output interpretability, synthesis interpretability signals, artifact coverage, and interpretability readiness before production interpretability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'agent_outputs',
        tableName: 'agent_outputs',
        requiredTables: ['agent_outputs', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM agent_outputs ao INNER JOIN runs r ON r.run_id = ao.run_id WHERE r.workspace_id = $1`,
      },
      {
        domain: 'moderator_syntheses',
        tableName: 'moderator_syntheses',
        requiredTables: ['moderator_syntheses', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM moderator_syntheses ms INNER JOIN runs r ON r.run_id = ms.run_id WHERE r.workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      agent_outputs: 'Agent outputs',
      moderator_syntheses: 'Moderator syntheses',
    },
    signalsHint: 'Run outcomes, agent outputs, and moderator syntheses',
  },
  {
    name: 'scannability',
    Name: 'Scannability',
    percent: 'scannabilityPercent',
    metricDomain: 'shield_scans',
    metricLabel: 'Shield scan scannability',
    metricDetail: 'shield scan scannability',
    action: 'refresh_scannability_summary',
    tables: [
      'shield_scans',
      'workspace_provider_credentials',
      'billing_webhook_events',
    ],
    check1: {
      key: 'shieldScansTableExists',
      name: 'shield_scan_scannability',
      label: 'Shield scan scannability',
      table: 'shield_scans',
    },
    check2: {
      key: 'workspaceProviderCredentialsTableExists',
      name: 'provider_credential_scannability',
      label: 'Provider credential scannability',
      table: 'workspace_provider_credentials',
    },
    check3Key: 'billingWebhookEventsTableExists',
    coverageKeys: {
      shield_scans: 'shieldScansTableExists',
      workspace_provider_credentials: 'workspaceProviderCredentialsTableExists',
      billing_webhook_events: 'billingWebhookEventsTableExists',
    },
    readiness: 'scanning_readiness_signal',
    readinessLabel: 'Scanning readiness signal',
    readinessDetail:
      'Shield scans, workspace provider credentials, and billing webhook events support scanning readiness.',
    cap1: 'supportsShieldScanScannabilitySignals',
    cap2: 'supportsProviderCredentialScannabilitySignals',
    guidance:
      'Production scannability rollout validates shield scan scannability, provider credential scannability signals, billing webhook coverage, and scanning readiness before production scannability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'shield_scans',
        tableName: 'shield_scans',
        requiredTables: ['shield_scans', 'runs'],
        sql: `SELECT COUNT(*)::text AS count FROM shield_scans ss INNER JOIN runs r ON r.run_id = ss.run_id WHERE r.workspace_id = $1`,
      },
      {
        domain: 'workspace_provider_credentials',
        tableName: 'workspace_provider_credentials',
        requiredTables: ['workspace_provider_credentials'],
        sql: `SELECT COUNT(*)::text AS count FROM workspace_provider_credentials WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      shield_scans: 'Shield scans',
      workspace_provider_credentials: 'Provider credentials',
    },
    signalsHint: 'Run outcomes, shield scans, and provider credentials',
  },
  {
    name: 'perceptibility',
    Name: 'Perceptibility',
    percent: 'perceptibilityPercent',
    metricDomain: 'usage_events',
    metricLabel: 'Usage event perceptibility',
    metricDetail: 'usage event perceptibility',
    action: 'refresh_perceptibility_summary',
    tables: [
      'usage_events',
      'billing_meter_usage_reports',
      'workspace_usage_limits',
    ],
    check1: {
      key: 'usageEventsTableExists',
      name: 'usage_event_perceptibility',
      label: 'Usage event perceptibility',
      table: 'usage_events',
    },
    check2: {
      key: 'billingMeterUsageReportsTableExists',
      name: 'meter_usage_perceptibility',
      label: 'Meter usage perceptibility',
      table: 'billing_meter_usage_reports',
    },
    check3Key: 'workspaceUsageLimitsTableExists',
    coverageKeys: {
      usage_events: 'usageEventsTableExists',
      billing_meter_usage_reports: 'billingMeterUsageReportsTableExists',
      workspace_usage_limits: 'workspaceUsageLimitsTableExists',
    },
    readiness: 'perception_readiness_signal',
    readinessLabel: 'Perception readiness signal',
    readinessDetail:
      'Usage events, meter usage reports, and workspace usage limits support perception readiness.',
    cap1: 'supportsUsageEventPerceptibilitySignals',
    cap2: 'supportsMeterUsagePerceptibilitySignals',
    guidance:
      'Production perceptibility rollout validates usage event perceptibility, meter usage perceptibility signals, workspace limit coverage, and perception readiness before production perceptibility tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'usage_events',
        tableName: 'usage_events',
        requiredTables: ['usage_events'],
        sql: `SELECT COUNT(*)::text AS count FROM usage_events WHERE workspace_id = $1`,
      },
      {
        domain: 'billing_meter_usage_reports',
        tableName: 'billing_meter_usage_reports',
        requiredTables: ['billing_meter_usage_reports'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_meter_usage_reports WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      usage_events: 'Usage events',
      billing_meter_usage_reports: 'Meter usage reports',
    },
    signalsHint: 'Run outcomes, usage events, and meter usage reports',
  },
  {
    name: 'noticeability',
    Name: 'Noticeability',
    percent: 'noticeabilityPercent',
    metricDomain: 'billing_notifications',
    metricLabel: 'Billing notification noticeability',
    metricDetail: 'billing notification noticeability',
    action: 'refresh_noticeability_summary',
    tables: ['billing_notifications', 'billing_webhook_events', 'usage_events'],
    check1: {
      key: 'billingNotificationsTableExists',
      name: 'billing_notification_noticeability',
      label: 'Billing notification noticeability',
      table: 'billing_notifications',
    },
    check2: {
      key: 'billingWebhookEventsTableExists',
      name: 'billing_webhook_noticeability',
      label: 'Billing webhook noticeability',
      table: 'billing_webhook_events',
    },
    check3Key: 'usageEventsTableExists',
    coverageKeys: {
      billing_notifications: 'billingNotificationsTableExists',
      billing_webhook_events: 'billingWebhookEventsTableExists',
      usage_events: 'usageEventsTableExists',
    },
    readiness: 'notice_readiness_signal',
    readinessLabel: 'Notice readiness signal',
    readinessDetail:
      'Billing notifications, billing webhook events, and usage events support notice readiness.',
    cap1: 'supportsBillingNotificationNoticeabilitySignals',
    cap2: 'supportsBillingWebhookNoticeabilitySignals',
    guidance:
      'Production noticeability rollout validates billing notification noticeability, billing webhook noticeability signals, usage event coverage, and notice readiness before production noticeability tooling.',
    domains: [
      {
        domain: 'completed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'completed'`,
      },
      {
        domain: 'failed_runs',
        tableName: 'runs',
        requiredTables: ['runs'],
        sql: `SELECT COUNT(*)::text AS count FROM runs WHERE workspace_id = $1 AND status = 'failed'`,
      },
      {
        domain: 'billing_notifications',
        tableName: 'billing_notifications',
        requiredTables: ['billing_notifications'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_notifications WHERE workspace_id = $1`,
      },
      {
        domain: 'billing_webhook_events',
        tableName: 'billing_webhook_events',
        requiredTables: ['billing_webhook_events'],
        sql: `SELECT COUNT(*)::text AS count FROM billing_webhook_events WHERE workspace_id = $1`,
      },
    ],
    domainLabels: {
      completed_runs: 'Completed runs',
      failed_runs: 'Failed runs',
      billing_notifications: 'Billing notifications',
      billing_webhook_events: 'Billing webhook events',
    },
    signalsHint: 'Run outcomes, billing notifications, and billing webhook events',
  },
]

export const patchMeta = milestones.map((m, index) => ({
  ...m,
  version: `v5.${151 + index}`,
  domainFormatter: `format${m.Name}Domain`,
}))
